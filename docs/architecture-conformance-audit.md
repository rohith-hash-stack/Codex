# Codex — Architecture Conformance Audit

> Produced per the Architecture Conformance, Research Traceability & Ordered Implementation Directive. Covers HLRD v1.0, TAD v1.0, the existing `src/codex/` code, tests, and `docs/research/provider-formats.md`. No production code was written or modified while producing this audit.

Audited: 2026-08-30 (commit `13bbaf6`).

**Update 2026-08-30 (post-reconciliation):** C-1, C-2, and C-3 below are now **RESOLVED** — see `docs/architecture-reconciliation.md` for the full resolution and `docs/HLRD.md`/`docs/TAD.md` for the amended specification text. ADR-012 and ADR-013 are now formally **CLOSED/SUPERSEDED** in TAD §77 itself, not just recommended for closure here. This update did not change any code — resolutions confirmed existing code was already conformant (C-1, C-2) or don't yet apply to any code (C-3). Sections B, D, E, G, and H below are annotated accordingly rather than rewritten, so the original audit trail stays visible.

**Update 2026-08-30 (post-D1):** the Phase D directive's D1 (ProviderAdapter Contract) is now **IMPLEMENTED** — `codex.provider.{capability,contract}`, 21 behavioral tests, 100% coverage, clean ruff/mypy. Sections B, F, G, and H are annotated accordingly. D2 (Capability Registry) is next; per the directive, implementation stops here pending review.

**Update 2026-08-30 (post-D2):** D2 (Capability Registry) is now **IMPLEMENTED** — `codex.registry.{models,scoring,registry}`, 27 new behavioral tests, 100% coverage, clean ruff/mypy. Sections B, F, G, and H are annotated accordingly. **New finding, not silently resolved:** TAD §31's `ProviderScore` formula needs five `[0,1]`-normalized inputs, but the (approved, closed) D1 `ProviderAdapter` contract only defines a source for two of them. See §I (superseded by the entry below).

**Update 2026-08-30 (ADR-018 resolved):** the finding above is now **RESOLVED** — see §I. `evidence_quality`/`cost_factor` are supplied via a `ProviderScoreProfile` attached once at `CapabilityRegistry.register()` (not per query); `freshness` is Registry-derived from the adapter's timestamp via a documented default decay function (`default_freshness_score`). `rank()` was refactored to take no caller-supplied scoring values at all — 14 more behavioral tests added/rewritten (79 total), 100% coverage maintained, clean ruff/mypy. D3 (Git Adapter) is next; per the directive, implementation stops here pending review.

**Update 2026-08-30 (post-D3):** D3 (Git Adapter) is now **IMPLEMENTED** — `codex.provider.git_adapter.GitAdapter`, the first concrete `ProviderAdapter`. Two capabilities, matching HLRD §13 exactly: `HISTORY` (tip-commit diff → `RepositorySymbol` entities carrying `lifecycle_status`) and `CO_CHANGE` (bounded commit-window co-occurrence → `CO_CHANGED_WITH` evidence). 30 new behavioral tests (109 total), 100% coverage on `git_adapter.py`, clean ruff/mypy, all built against real temporary git repositories (none depend on this repository's own history). Design note, not a new ADR: file lifecycle is a unary fact and doesn't fit `Evidence`'s S-P-O shape, so `HISTORY` emits entities only, provenance carried by `EvidenceCohort` — the same pattern already flagged for CodeQL's single-entity findings. No Capability Registry integration, SCIP/CodeQL/Sourcegraph adapters, or ingestion pipeline (D4) — all correctly out of D3's scope. Sections B, F, G, and H are annotated accordingly. D4 (Ingestion Pipeline) is next; per the directive, implementation stops here pending review.

**Update 2026-08-30 (post-D4):** D4 (Ingestion Pipeline) is now **IMPLEMENTED** — `codex.ingestion.{models,pipeline}`, wiring D1/D2/D3 into `PROVIDERS -> INGESTION PIPELINE -> EVIDENCE NORMALIZATION -> CANONICAL GRAPH` per TAD §72-73/HLRD §23. `IngestionPipeline.run()` uses the Capability Registry's `evaluate()` for provider discovery/capability selection/eligibility (no provider-name branching anywhere, no scoring logic duplicated — `rank()` is intentionally not used since ingestion runs every AVAILABLE/PARTIAL provider per capability rather than picking a single top-ranked one, so that TAD §73's Reconciliation Engine has multiple providers' evidence to eventually reconcile), isolates each provider's `extract()`/`normalize()` behind a per-provider try/except (one provider's `ProviderExtractionError` or any unexpected exception never discards another's already-committed evidence), and accumulates entities/evidence in memory per repository so each `run()` builds on everything committed by prior runs rather than rescanning (HLRD §23). Every evidence record observed for a given `(subject, predicate, object)` triple is appended to `CanonicalRelationship.supporting_evidence_ids` without judgment — no contradiction detection, no status/confidence assignment, since the Entity Resolution + Reconciliation Engine (TAD component #7) remains `NOT_IMPLEMENTED` and D4 must not invent its logic. Each `run()` produces one new, immutable, published `GraphVersion` (deterministic `version_id` from repository_id + revision + provider_versions, per TAD §19's actual composite key) and a freshly materialized `InMemoryGraphStore` — a prior run's returned store/version is never mutated. 22 new behavioral tests (131 total: 19 with a dedicated deterministic fake provider, 3 against the real `GitAdapter` in temporary git repos), 100% coverage on both new modules, clean ruff/mypy. No SCIP/CodeQL/Sourcegraph/Runtime adapters, Query Understanding, Planner, Ranking, LLM/SLM, or Verification Engine — all correctly out of D4's scope. No new resource consulted (pure orchestration of already-researched D1/D2/D3 pieces); no ADR created (no genuine new architectural decision — see the pipeline module's docstring for the documented implementation-detail choices). No cross-document contradiction found. Sections B, F, G, and H are annotated accordingly. D5 (SCIP Adapter) is next; per the directive, implementation stops here pending review.

**Update 2026-08-30 (post-D5):** D5 (SCIP Adapter) is now **IMPLEMENTED** — `codex.provider.scip_adapter.SCIPAdapter`, built on a new `codex.provider.scip` package (`wire.py`, `index.py`, `mapping.py`): an independently-written, dependency-free SCIP protobuf decoder (no `protobuf` package dependency, no vendored/generated code from `scip.proto` — see `docs/resources.md`'s SCIP row for the full dependency-avoidance reasoning). Four capabilities, determined via a pre-coding mapping table (now in the adapter's own module docstring) and validated against a real `scip-typescript`-generated `.scip` artifact (`tests/fixtures/scip/typescript_sample.scip`): `SYMBOL_DEFINITION` (Definition-role occurrences → `RepositorySymbol` entities, base type via `Kind`/descriptor-suffix inference), `SYMBOL_REFERENCE` (other occurrences → `REFERENCES`/`IMPORTS` evidence from the referencing file), `IMPLEMENTATION` (`Relationship.is_implementation` → `IMPLEMENTS`), `TYPE_RELATIONSHIP` (`Relationship.is_type_definition` → `REFERENCES`). **`CALL_RELATIONSHIP` and `EXTENDS` are deliberately not produced** — real SCIP output (confirmed empirically, not assumed) has no "this is a call" bit and no way to distinguish "extends" from "implements," so inventing either would violate the directive's explicit "do not treat every reference as CALLS" / "do not infer unless deterministically established" rules. External (not-locally-defined) symbols become `EXTERNAL_LIBRARY` entities with package-qualified identity and a revision-independent canonical id. Identity converges with `GitAdapter`'s FILE entities (same `build_canonical_id` call for the same repo+revision+path). 30 new behavioral tests (260 total across the wire/index/mapping/adapter test files, 99 SCIP-specific), 100% coverage on all four new modules, clean ruff/mypy — a mix of handcrafted fixtures (a small test-only protobuf *encoder* mirroring the decoder) and the real artifact, including malformed-input, capability-failure-isolation, determinism/idempotency (including through the D4 `IngestionPipeline`), duplicate-record, and conflicting-relationship-flag tests. Two real bugs were found and fixed during development via the real-artifact validation before any test was written against them: (1) `SymbolInformation.kind` is unpopulated by a real producer, requiring the descriptor-suffix fallback rather than trusting `kind` alone; (2) a file's SYMBOL_DEFINITION-derived entity and its SYMBOL_REFERENCE-derived entity were initially computing different `qualified_name`s for the same physical file, which would have split one file into two graph nodes — fixed by normalizing SCIP's backtick-escaped descriptor path back to a plain repo-relative path. No CodeQL/Sourcegraph/RepoGraph/Runtime adapters, Query Understanding, SLM/LLM, Ranking, Planning, Verification, or Reconciliation Engine — all correctly out of D5's scope. No ADR created (no genuine new architectural decision, per the deliberate choice to avoid a `protobuf` dependency question entirely rather than needing to resolve one). No cross-document contradiction found. D1–D4 were not bypassed or reopened: SCIPAdapter implements the exact same D1 `ProviderAdapter` protocol as `GitAdapter`, is selected/evaluated purely through the unmodified D2 `CapabilityRegistry`, and reaches the graph purely through the unmodified D4 `IngestionPipeline` (proven by two dedicated pipeline-integration tests). Sections B, C, F, G, and H are annotated accordingly. D6 is next; per the directive, implementation stops here pending review.

**Update 2026-08-30 (post-D6):** D6 (CodeQL Adapter) is now **IMPLEMENTED** — `codex.provider.codeql_adapter.CodeQLAdapter`, the third concrete `ProviderAdapter`, one capability (`DATA_FLOW`). Consumes an already-generated SARIF 2.1.0 file only — **never invokes the `codeql` CLI, never generates a CodeQL database** — matching the exact "read a pre-generated artifact" pattern D5's `SCIPAdapter` already established, applied here because the CodeQL CLI's own Terms and Conditions (fetched and read directly, not assumed) restrict free use to academic research, demonstration, and analysis of an Open-Source Codebase, with a paid GHAS license required otherwise; SARIF itself is a separate OASIS open standard, unrestricted. Two representational branches, both determined and documented (capability-mapping table in the adapter's own module docstring) before coding: a path-problem result (`codeFlows` with >= 2 distinct locations) produces one `REFERENCES` `Evidence` record between the flow's source and sink FILE entities (Class B, deterministically derived from the codeFlow's own explicit endpoints); a plain "problem"-kind result (the majority of real CodeQL output, no `codeFlows`) becomes a `codeql:finding:<ruleId>` role on the FILE entity at its location, with **no fabricated `Evidence` record** — Codex's `Evidence` model requires two distinct endpoints a single-location finding doesn't have, and no `BaseEntityType` represents a bare "finding" concept, so this is recorded as a real, documented V1 limitation rather than worked around. **`CALL_RELATIONSHIP`/`EXTENDS`/`IMPLEMENTS`/`DEPENDS_ON` are never produced** — no SARIF field asserts any of them, and inferring one from a codeFlow's per-step `threadFlowLocation.kinds` was considered and rejected: that field is optional, freeform, and unpopulated in every real fixture inspected. Validated against six real SARIF artifacts fetched from `github/codeql-action`'s own test suite (genuine "CodeQL command-line toolchain"/"LGTM.com" output) plus two handcrafted, schema-faithful fixtures for scenarios no real one covered (a path-problem result; a multi-run file with one malformed run to prove per-run failure isolation within one capability). Two more real, load-bearing findings surfaced only by validating against real data: SARIF's own schema permits `results` to be omitted entirely ("a run solely exporting rules metadata") — not malformed, initially mishandled as an error and fixed; and `artifactLocation.uri` must be a `uri-reference`, so a non-conforming URI string (confirmed against a real malformed fixture) is now rejected rather than accepted as a fabricated file path. 45 new behavioral tests (275 total), 100% coverage on `codeql_adapter.py`, clean ruff/mypy, including two tests proving Git+CodeQL and SCIP+CodeQL evidence coexist in one `IngestionPipeline` run without either provider's evidence being discarded or adjudicated. No new ADR — ADR-005 (CodeQL integration strategy) is resolved directly by this implementation, the same disposition as D5's ADR-004. No cross-document contradiction found. D1–D5 were not bypassed or reopened: `CodeQLAdapter` implements the unmodified D1 `ProviderAdapter` protocol, is selected/evaluated purely through the unmodified D2 `CapabilityRegistry`, and reaches the graph purely through the unmodified D4 `IngestionPipeline`. Sections B, C, D, F, G, and H are annotated accordingly. D7 is next; per the directive, implementation stops here pending review.

---

## 0. A note on scope: DTD-01..05 do not exist as separate documents

The directive asks this audit to inspect "DTD-01" through "DTD-05" as distinct documents. **They don't exist as separate files.** The only artifacts in this repository are `docs/HLRD.md` and `docs/TAD.md`. "DTD-01" (Canonical Graph), "DTD-02" (Query Understanding), "DTD-03" (Query Planner), "DTD-04" (Evidence Selection/MSS), and "DTD-05" (Verification) exist **only as inline labels inside TAD's own pipeline diagram and component sections** (TAD §5, §22, §29, §46, §78, §80).

This is reported here rather than papered over: I am not going to fabricate five documents' worth of content that was never written. Everywhere below, "DTD-0x" means "the corresponding TAD section(s)," not an independent spec. Whether standalone DTD documents should be authored, or whether TAD's inline sections are meant to serve that role permanently, is itself an open item (see **C-5** below).

---

## A. Architecture Status

| Document | Status |
|---|---|
| HLRD v1.0 | Frozen baseline (`docs/HLRD.md`, commit `4bd76c4`). Read in full for this audit. |
| DTD-01..05 | Do not exist as separate documents — see §0. TAD's inline sections stand in for them. |
| TAD v1.0 | Architecture baseline (`docs/TAD.md`, commit `bce94fa`). Re-read in full for this audit. |
| ADRs | None exist as decisions. TAD §77 lists 17 candidate ADR titles; zero have been drafted or resolved. |
| Research resources | Partially consulted — see §C. SCIP, CodeQL SARIF, both RepoGraph implementations researched (`docs/research/provider-formats.md`). Sourcegraph, Tree-sitter, GraphRAG, TransE, LangChain/LlamaIndex, OpenTelemetry, scikit-learn calibration not yet researched (correctly deprioritized for most — see table). |
| Implementation | Phase 1 (Foundation): `codex.ontology`, `codex.evidence`, `codex.graph`, `codex.repository`. Plus Phase D: `codex.provider` (D1 contract, D3 `GitAdapter` — the first concrete adapter) and `codex.registry` (D2 Capability Registry, ADR-018 resolved). 0 of TAD's 18 components (§6) beyond these six have any code. |

---

## B. Requirement Traceability Matrix

Only major architectural requirements are shown (not every clause). "Module" is blank where nothing implements the requirement.

| Req ID | HLRD | TAD | Module | Test(s) | Status | Evidence |
|---|---|---|---|---|---|---|
| Base Type + Role ontology | §16-17 | §12-13 | `codex.ontology.entities` | `test_ontology.py` | **IMPLEMENTED** | `BaseEntityType`, free-string `roles[]`, `RepositorySymbol.has_role()` |
| Canonical relationship types | §16 | §14 | `codex.ontology.relationships` | none dedicated | **PARTIALLY_IMPLEMENTED** | All TAD §14 types + HLRD's extras implemented. **C-1 now RESOLVED**: `TESTED_BY` is canonical (HLRD §16 amended to match); code required no change. Still partial only because no dedicated test exists yet. |
| Canonical identity / join key | §18 | §12 | `codex.ontology.entities.build_canonical_id` | `test_ontology.py` | **IMPLEMENTED** (join key only) | Deterministic hash of repo+revision+type+language+qualified_name |
| Entity Resolution (multi-provider reconciliation) | §19 | component #7 | — | — | **NOT_IMPLEMENTED** | Only the join-key primitive exists; no reconciliation logic, and moot with zero providers |
| Evidence model (13 fields) | §15 (implied) | §15 | `codex.evidence.model.Evidence` | `test_evidence.py` | **IMPLEMENTED** | All TAD §15 fields present verbatim |
| Evidence independence default | — | §16 | `Evidence.effective_independence_group` | `test_evidence.py` | **IMPLEMENTED** | Defaults to `provider_default:{provider}` when unset |
| EvidenceCohort (successful/failed/partial capability lists) | — | §17 | `codex.evidence.model.EvidenceCohort` | `test_evidence.py` | **PARTIALLY_IMPLEMENTED** | Data container correct; nothing yet *consumes* it to drive negative-query `INCONCLUSIVE` logic (no Planner exists) |
| Evidence Status 6-way taxonomy, DISPUTED ≠ UNRESOLVED | — | §18 | `codex.evidence.model.EvidenceStatus` | none dedicated | **PARTIALLY_IMPLEMENTED** | Enum shape correct and distinct; no reconciliation logic yet assigns these, so the *behavioral* distinction is unverified |
| `raw_reference` resolvable URI (artifact://, s3://, file://) | — | §16, §52 | `Evidence.raw_reference` (`validate_raw_reference`), `ExtractionResult.raw_reference` | `test_provider_contract.py::test_raw_reference_must_be_resolvable_uri` | **PARTIALLY_IMPLEMENTED** | Scheme is now validated (rejects arbitrary strings) as of D1. Still no Artifact Store or resolution *service* — the URI is checked, not resolved. |
| GraphVersion composite key | §19 (via TAD) | §19 | `codex.graph.version.GraphVersion` | `test_graph_store.py`, `test_ingestion_pipeline.py` (version-id determinism/provider-version-change tests) | **PARTIALLY_IMPLEMENTED** | **C-2 now RESOLVED**: generic `provider_versions` dict is canonical (TAD §19 amended to match); code required no change. D4's `IngestionPipeline` now actually populates the composite key end to end (`repository_revision` + `provider_versions`, deterministic `version_id`), proven idempotent/deterministic by test. Still partial only pending full version-locking flow (see below). |
| Graph version immutable once published | invariant | §20, invariant #4 | `GraphVersion.publish()` | `test_graph_store.py` | **PARTIALLY_IMPLEMENTED** | Model-level copy-on-publish works; nothing enforces immutability at the store level |
| Graph version locking through Planning→Retrieval→MSS→Verification | — | §20 | — | — | **NOT_IMPLEMENTED** | No Planner/Retrieval/Verification exist yet to lock a version through |
| Graph mutation boundary (ingestion-only writes) | — | §62, invariants #6-8 | `codex.graph.store.{GraphReader,GraphStore}`, `codex.ingestion.pipeline.IngestionPipeline` | none dedicated | **PARTIALLY_IMPLEMENTED** | Interfaces are structurally split; nothing runtime-enforces it (typing convention only). D4's `IngestionPipeline` is now the one concrete caller of `upsert_entity`/`upsert_relationship` in the codebase, consistent with the boundary; still moot as *enforcement* until Query/Verification code exists to have something to be excluded from writing |
| Historical storage = snapshot + diffs | — | §21 | — | — | **NOT_IMPLEMENTED** | No snapshotting or reconstruction exists |
| Repository Manager: register/clone/revision/diff | — | §7 | `codex.repository.manager.RepositoryManager` | `test_repository_manager.py` (3 tests, real git repos, no network) | **PARTIALLY_IMPLEMENTED** | All done except "triggering indexing" (explicit TAD §7 responsibility) — an ingestion pipeline now exists (D4, `codex.ingestion.pipeline.IngestionPipeline`) but nothing yet wires `RepositoryManager`'s own methods to *call* it; that wiring (e.g. on `clone()`/a detected revision change) is not part of D4's orchestration scope and remains open |
| Repository Manager does not interpret queries | — | §7 | (absence of query code in this module) | — | **IMPLEMENTED** | Satisfied by construction |
| Provider Adapter contract (`extract/validate/normalize`, capabilities) | — | §8-9, §64; directive §11-13 | `codex.provider.contract.ProviderAdapter` (+ `Capability`, `ProviderHealthStatus`, `ValidationResult`, `ProviderEligibility`, `ExtractionResult`, `NormalizedEvidence`, `ProviderExtractionError`) | `tests/test_provider_contract.py` (25 tests) via `tests/fake_provider_adapter.py` | **IMPLEMENTED** | Behaviorally proven, not just declared: identity/version/capability declaration, supported vs. unsupported capability, successful/empty/partial/failed capability outcomes kept distinct, total provider failure raising `ProviderExtractionError` (distinct from a capability failure), provenance, `independence_group` default, source revision, snapshot identity, `raw_reference` validation, eligibility metadata, and revision association across runs. Now implemented by two concrete adapters (`GitAdapter` D3, `SCIPAdapter` D5) without modification — contract stayed closed. |
| Git Adapter: `HISTORY` and `CO_CHANGE` capabilities (first concrete `ProviderAdapter`) | — | §13; directive D3 §3-9, §13-16 | `codex.provider.git_adapter.GitAdapter` (uses `GitPython`, `codex.repository.models.RepositoryMetadata`) | `tests/test_git_adapter.py` (30 tests, real temporary git repos, never this repo's own history) | **IMPLEMENTED** | Exactly the two capabilities matching HLRD §13's stated Git scope — no more. `HISTORY`: tip-commit diff (initial-commit empty-tree diff included) → `RepositorySymbol` entities carrying `lifecycle_status` (ACTIVE for added/modified, DELETED, RENAMED), rename detection empirically confirmed against real GitPython behavior before being relied on. `CO_CHANGE`: bounded commit-window co-occurrence → `CO_CHANGED_WITH` evidence with saturation-based confidence. Capability-level failure isolation proven (one capability failing/timing out does not fail the other or raise); total-provider failure (`UNAVAILABLE`: no git executable, not a repo, invalid revision) kept distinct from a capability failure, per the D3 failure-semantics taxonomy. Security: argument-array-only GitPython APIs, no `shell=True`, empirically confirmed no shell-injection risk via a malicious-revision-string test. No Capability Registry integration or ingestion pipeline wiring — correctly out of D3's scope (D4). |
| Ingestion Pipeline: provider orchestration -> evidence normalization -> canonical graph update | — | §72-73; HLRD §23; directive D4 §1-16 | `codex.ingestion.pipeline.IngestionPipeline` (+ `codex.ingestion.models.{IngestionResult,ProviderRunOutcome,ProviderRunStatus}`) | `tests/test_ingestion_pipeline.py` (22 tests: 19 with a dedicated deterministic fake adapter, 3 against the real `GitAdapter` in temporary git repos) | **IMPLEMENTED** | Wires D1 (`ProviderAdapter`), D2 (`CapabilityRegistry`), and D3 (`GitAdapter`) end to end with **no provider-specific logic** — every selection decision goes through `CapabilityRegistry.evaluate()`, never a hard-coded provider name (directive D4 §3-4, proven by a test with an ineligible fake provider that is never `extract()`-ed). Failure isolation proven behaviorally: a provider's `ProviderExtractionError`, an unexpected exception from `extract()`/`normalize()`, or a pipeline-level validation mismatch (cohort provider/revision not matching the request) each fail only that provider, never discarding another's already-committed entities/evidence (directive D4 §6, §14). Contradictory-evidence preservation proven: two fake providers asserting the same `(subject, predicate, object)` both land in `supporting_evidence_ids`, `contradicting_evidence_ids` stays empty, and `CanonicalRelationship.status` stays `UNRESOLVED` — D4 assigns no reconciliation verdict (directive D4 §11, TAD §73; Entity Resolution + Reconciliation Engine remains `NOT_IMPLEMENTED`, TAD component #7). Idempotency and determinism proven: two runs against the same revision (fake and real `GitAdapter`) produce the same `GraphVersion.version_id`, no duplicate evidence records (`InMemoryEvidenceStore.add_evidence`'s `evidence_id`-keyed overwrite), and no duplicate `supporting_evidence_ids` entries; a provider-version change between two runs of the same revision produces a different `version_id`, per TAD §19's actual composite key. Incremental accumulation proven with the real `GitAdapter` across two revisions: a file introduced at revision 1 is still present in the graph after ingesting revision 2's tip-vs-parent-only diff — no rescanning (HLRD §23). Each `run()` returns a freshly materialized, published `GraphVersion`/`GraphStore` pair; a previously returned result is never mutated by a later (including a failing) run. No Capability Registry changes, no SCIP/CodeQL/Sourcegraph adapters, no Query/Planner/Ranking/LLM/Verification code — all correctly out of D4's scope. D5 confirms it generalizes: `SCIPAdapter` reaches the graph through this exact same, unmodified pipeline. |
| SCIP Adapter: `SYMBOL_DEFINITION`, `SYMBOL_REFERENCE`, `IMPLEMENTATION`, `TYPE_RELATIONSHIP` capabilities (second concrete `ProviderAdapter`) | HLRD Resource Map §62 | §8-9; directive D5 §1-19 | `codex.provider.scip_adapter.SCIPAdapter` (uses `codex.provider.scip.{wire,index,mapping}`, an independently-written, dependency-free SCIP decoder) | `tests/test_scip_wire.py`, `test_scip_index.py`, `test_scip_mapping.py`, `test_scip_adapter.py` (99 tests total, incl. a real `scip-typescript`-generated `.scip` fixture) | **IMPLEMENTED** | Capability/evidence mapping determined and documented (adapter module docstring) before coding, per directive D5 §4. `SYMBOL_DEFINITION`: Definition-role occurrences → `RepositorySymbol` entities, base type via `Kind` enum or (empirically necessary, since real producers leave `kind` unset) descriptor-suffix inference, `source_location` preserved. `SYMBOL_REFERENCE`: other occurrences → `REFERENCES`/`IMPORTS` evidence from the referencing file. `IMPLEMENTATION`/`TYPE_RELATIONSHIP`: `Relationship.is_implementation`/`is_type_definition` → `IMPLEMENTS`/`REFERENCES`. `CALL_RELATIONSHIP` and `EXTENDS` are **not** produced — real SCIP output has no "call" bit and no bit distinguishing extends from implements, confirmed empirically, not assumed (directive D5 §8). External symbols become `EXTERNAL_LIBRARY` entities with a revision-independent canonical id (directive D5 §10); identity converges with `GitAdapter`'s FILE entities via the same `build_canonical_id` call (directive D5 §9). Capability-level failure isolation, malformed/missing-artifact handling, deterministic IDs, idempotency (including through the unmodified D4 pipeline), duplicate-record handling, and conflicting-relationship-flag preservation all behaviorally proven. Two real bugs found and fixed via real-artifact validation before shipping: unpopulated `kind`, and a FILE-entity identity split between the definition and reference code paths. |
| CodeQL Adapter: `DATA_FLOW` capability (third concrete `ProviderAdapter`) | HLRD Resource Map §62 | §8-9, §10; directive D6 §1-22 | `codex.provider.codeql_adapter.CodeQLAdapter` (stdlib `json` only — no `protobuf`-style decoder package needed, SARIF is plain JSON) | `tests/test_codeql_adapter.py` (45 tests, incl. 6 real SARIF artifacts fetched from `github/codeql-action`'s own test suite) | **IMPLEMENTED** | Capability/evidence mapping determined and documented (adapter module docstring) before coding, per directive D6's required table. Consumes an already-generated SARIF file only — never invokes `codeql`, never generates a database — because the CodeQL CLI's own Terms (fetched and read directly) restrict free use to academic research/OSS analysis, requiring a paid GHAS license otherwise; this mirrors D5's exact "consume, don't generate" precedent, so it did not require a STOP (directive D6's own condition for one). A path-problem result (`codeFlows` with ≥2 distinct locations) → `REFERENCES` `Evidence` between source/sink FILE entities (Class B, derived from the codeFlow's own explicit endpoints, confidence from an explicit `level`→confidence policy mapping). A plain "problem"-kind result (no `codeFlows`, the majority of real CodeQL output) → a `codeql:finding:<ruleId>` role on the FILE entity, no `Evidence` record — `Evidence` needs two endpoints a single-location finding doesn't have, and no `BaseEntityType` fits "a finding," so this is a documented V1 limitation, not a workaround. `CALL_RELATIONSHIP`/`EXTENDS`/`IMPLEMENTS`/`DEPENDS_ON` never produced — no SARIF field asserts any of them; per-step `threadFlowLocation.kinds` (the only field that could) is optional, freeform, and empty in every real fixture inspected. Two more real bugs found via real-artifact validation, both fixed before shipping: SARIF's schema permits `results` to be omitted entirely (rules-metadata-only runs — initially mishandled as malformed), and non-conforming `artifactLocation.uri` values (confirmed against a real malformed fixture) are now rejected rather than accepted as fabricated file paths. Per-run failure isolation within the single `DATA_FLOW` capability proven via `partial_capabilities` (a field D3/D5 never needed to populate, since neither has SARIF's multi-run structure). Coexistence with Git and SCIP evidence in one `IngestionPipeline` run proven directly. |

**Canonical interpretation of TAD §9's `health_status`/`availability` (approved 2026-08-30, D1 review):** TAD §9 lists these as two flat, undistinguished attributes. Resolved as: `health_status` (`ProviderHealthStatus`: `HEALTHY`/`DEGRADED`/`UNHEALTHY`/`UNKNOWN`) is the operational condition of the provider itself; `availability(capability, repository) -> float` in `[0.0, 1.0]` is whether the provider is currently usable for a *specific capability* in a *specific repository/environment* — an adapter-reported fact, not an aggregated selection score (that aggregation is D2's job, per TAD §10/§31). The two are structurally independent: nothing in the contract derives one from the other, and `test_healthy_provider_can_report_zero_availability`/`test_unhealthy_provider_can_report_nonzero_availability` prove a HEALTHY provider can report 0.0 availability (e.g. missing license for this repository) and an UNHEALTHY one can still report full availability. This was an ambiguity in a single document (TAD §9), not a cross-document contradiction — it did not require the C-1/C-2/C-3-style reconciliation process, only this recorded interpretation.
| Capability Registry: registration, capability discovery | — | §10 | `codex.registry.registry.CapabilityRegistry.{register,unregister,registered_providers,providers_for}` | `test_capability_registry.py` (11 tests) | **IMPLEMENTED** | Register/replace/unregister, static declared-capability lookup, empty-result behavior for unknown capabilities/no providers — all behaviorally tested |
| Capability Registry: eligibility/availability/health evaluation | — | §9-10; directive D1 §11, D2 §6 | `CapabilityRegistry.evaluate()`, `ProviderEvaluationStatus` (AVAILABLE/PARTIAL/UNAVAILABLE/INELIGIBLE/FAILED) | `test_capability_registry.py` (7 tests) | **IMPLEMENTED** | Every status path behaviorally distinguished, including the case that specifically decouples `health_status` (UNHEALTHY) from `validate()` (still ok) to prove the registry checks each independently rather than treating one as a proxy for the other |
| Capability Registry: provider selection/scoring | — | §31; directive D2 §4; ADR-018 | `CapabilityRegistry.rank()`, `codex.registry.scoring.{ProviderScoreInputs,ProviderScoreProfile,provider_score,default_freshness_score,PROVIDER_SCORE_WEIGHTS}` | `test_capability_registry.py` (10 tests), `test_provider_scoring.py` (11 tests) | **IMPLEMENTED** | Formula/weights implemented exactly per TAD §31 (0.40/0.20/0.15/0.15/0.10), `capability_match=0` exclusion proven, deterministic tie-broken ranking proven, and — per **ADR-018 (RESOLVED, see §I)** — `evidence_quality`/`cost_factor` come from a `ProviderScoreProfile` attached once at `register()` time (not per query, so ranking is caller-independent) and `freshness` is derived from the adapter's own timestamp via a documented, provider-neutral default decay function. `rank()` takes no caller-supplied scoring values at all. |
| Relationship Reconciliation / contradiction score | §20 | §38, §73 | `CanonicalRelationship` (shape only) | `test_evidence.py` (key only) | **PARTIALLY_IMPLEMENTED** | Data model exists (`supporting_evidence_ids`, `contradicting_evidence_ids`, `status`); zero logic computes them |
| External library package-qualified identity | — | §56 | `BaseEntityType.EXTERNAL_LIBRARY`, populated by `codex.provider.scip_adapter` (D5) | `test_scip_adapter.py::test_external_symbol_becomes_external_library_entity`, `::test_external_library_identity_independent_of_repository_revision` | **PARTIALLY_IMPLEMENTED** | D5 is the first real populator: `<manager>:<name>@<version>` qualified names (e.g. `npm:typescript@5.9.3`), revision-independent canonical id. Still partial: only SCIP populates it so far (no CodeQL/Sourcegraph adapter yet to cross-check), and nothing yet *resolves* an external library id to real package metadata. |
| DTD-02 Query Understanding (Tier-0 + SLM) | §24-28 | §22-28 | — | — | **NOT_IMPLEMENTED** | — |
| DTD-03 Query Planner + Planner/LLM boundary | §29-30 | §29-34 | — | — | **NOT_IMPLEMENTED** (boundary therefore **UNVERIFIED**, not proven) | Cannot test a boundary that doesn't exist yet |
| DTD-04 Retrieval/Ranking/MSS | §32-39 | §35-41 | — | — | **NOT_IMPLEMENTED** | — |
| LLM Gateway + structured claims | §41-45 | §42-45 | — | — | **NOT_IMPLEMENTED** | — |
| DTD-05 Verification Engine | §42-43 | §46-51 | — | — | **NOT_IMPLEMENTED** — spec is now consistent (**C-3 RESOLVED**), no longer contradicted | TAD §50's 6-state enum is now explicitly canonical, with tested mapping functions required to HLRD's 4-value presentation label and TAD §5's 3-bucket routing view. Safe to implement against once this phase starts. |
| Telemetry Store | §52 | §65 | — | — | **NOT_IMPLEMENTED** | — |
| Artifact Store | — | §52-53 | — | — | **NOT_IMPLEMENTED** | — |

**Summary (updated post-D5):** 10 of ~28 major requirements IMPLEMENTED, 10 PARTIALLY_IMPLEMENTED, 8 NOT_IMPLEMENTED, 0 CONTRADICTED at the spec level. Nothing is marked implemented merely because a class exists — every "IMPLEMENTED" row has a passing behavioral test.

---

## C. Research Resource Audit

| Resource | Purpose | Codex adopts | Codex does NOT adopt | Section | Impact | Status |
|---|---|---|---|---|---|---|
| SCIP (`sourcegraph/scip`) | Precise code indexing format | The `scip.proto` schema as the wire format to normalize from; independently re-implemented as a dependency-free decoder rather than a `protobuf`-based one | SCIP as the only indexing source; its 86-value `Kind` enum 1:1 (collapses into our 16 base types + roles, with a descriptor-suffix fallback since real producers leave `kind` unset); `CALLS`/`EXTENDS` (no deterministic SCIP signal for either) | HLRD §11, TAD §8-9 | ADR-004 (resolved by implementation, see §D) | **IMPLEMENTED** — `codex.provider.scip_adapter.SCIPAdapter` (D5), `SYMBOL_DEFINITION`/`SYMBOL_REFERENCE`/`IMPLEMENTATION`/`TYPE_RELATIONSHIP` capabilities, validated against a real `scip-typescript`-generated artifact |
| CodeQL | Static analysis / data-flow evidence | The authoritative OASIS SARIF v2.1.0 schema as the output format to parse (independently implemented, stdlib `json` only); real SARIF artifacts from `github/codeql-action`'s own test suite for validation | CodeQL as a call-graph source (SCIP is cheaper for that); raw SARIF severity as confidence directly (explicit policy mapping instead); the `codeql` CLI/engine itself (never invoked — see finding below); per-step data-flow semantics from `threadFlowLocation.kinds` (unpopulated in every real fixture inspected) | HLRD §12, TAD §8-9 | ADR-005 (resolved by implementation, see §D) | **IMPLEMENTED** — `codex.provider.codeql_adapter.CodeQLAdapter` (D6), `DATA_FLOW` capability |
| Git | Historical evidence, revision/diff detection | GitPython as the concrete library (already a dependency); commit/diff model | Treating git history as semantic truth (HLRD §13 explicit) | HLRD §13, TAD §7 | none open | **IMPLEMENTED** — `RepositoryManager` (Phase 1, revision/diff) and `GitAdapter` (D3, `HISTORY`/`CO_CHANGE` evidence-emitting capabilities) |
| RepoGraph (`chokevin/repograph`, `SillySerpent/Repograph`) | Reference repository-graph implementations | Ontology validation (their node/edge sets corroborate ours); architectural precedent for a tree-sitter fallback and for Kuzu as storage | Their code directly — no vendoring/wrapping, license unreviewed | HLRD §10, TAD §8 | ADR-001 (Kuzu candidate), ADR-006 | **RESEARCHED**; 0 code |
| Sourcegraph (docs, Code Navigation API) | Repository intelligence provider (search, precise nav, xrefs) | Concept of a Sourcegraph Adapter behind the `ProviderAdapter` contract | Sourcegraph as mandatory — HLRD §8 allows "any qualified repository intelligence/search provider" | HLRD §9, TAD §8 | ADR-006 | **NOT researched** — `sourcegraph.com` is blocked by this environment's egress proxy. Open gap. |
| Tree-sitter | Incremental parsing, referenced for a from-scratch graph adapter and for incremental updates | Candidate engine for a fallback adapter when no Sourcegraph/SCIP source is available | Not currently a dependency; no code uses it | HLRD §23, §62 | ADR-006 (fallback strategy) | **Indirectly touched** via the RepoGraph research, not researched directly; correctly out of scope until a fallback adapter is actually needed |
| Microsoft GraphRAG | General graph-based RAG pattern | The general "build a graph → retrieve a subgraph → ground the LLM" pattern, as validation of Codex's own DTD-04 approach | Its community-detection/summarization algorithms; not a dependency | HLRD §62 | none (background only) | **NOT researched**; informs philosophy only |
| TransE / KG embeddings paper | Reference for future relational embeddings | Nothing in V1 — HLRD §34 / TAD §3 make embeddings explicitly optional/non-mandatory; this is V2 research (TAD §79) | No embedding code planned for V1 | HLRD §62 | ADR-009, but P3 | **Correctly deprioritized**, not researched |
| LangChain / LlamaIndex | LLM orchestration reference | Potentially the LLM Gateway's structured-output/tool-calling plumbing (Phase 8, not started) | Not a dependency yet; no code exists to use them | HLRD §62 | ADR-008 | **Correctly out of scope** until Phase 8 |
| OpenTelemetry | Runtime Adapter source + general observability pattern | Nothing yet — Runtime Adapter is optional, Telemetry Store isn't built | Not a dependency | HLRD §14, TAD §57, §65 | ADR-014, Telemetry design | **Correctly out of scope** for Phase 1 |
| scikit-learn calibration (Platt/isotonic) | SLM confidence calibration reference | Nothing yet — no SLM exists | Not a dependency | HLRD §62, TAD §25 | ADR-007 | **Correctly out of scope** until Phase 4 |

**Important:** none of the above have been added as dependencies simply because they're mentioned in the architecture documents, per the directive's explicit instruction. `pyproject.toml` still only declares `networkx`, `pydantic`, `GitPython` — the three things actually used by Phase 1 code.

---

## D. ADR Audit

TAD §77 lists 17 candidate ADR titles. Classifying each per the directive's scheme (A: decided by HLRD/DTD, B: decided by TAD, C: genuine unresolved decision, D: implementation detail — not an ADR, E: duplicate, F: superseded, G: research item):

| ADR | Title | Class | Reasoning |
|---|---|---|---|
| ADR-001 | Graph Storage Technology | **C** | Genuinely open (TAD §39/§53/§75 explicitly defer it). Now better-informed: Neo4j/Dgraph/NetworkX (TAD's own list) + **Kuzu** (from RepoGraph research). |
| ADR-002 | Evidence Storage Technology | **C** | Genuinely open (TAD §53: "Technology selection remains an ADR"). |
| ADR-003 | Artifact Storage Technology | **C** | Genuinely open (TAD §52-53). |
| ADR-004 | SCIP Integration Strategy | **C**, now **RESOLVED by D5 implementation, no ADR needed** | The wire format and integration strategy are both now settled: `codex.provider.scip_adapter.SCIPAdapter` (D5) documents the full capability/evidence mapping in its own module docstring, resolved directly from the finalized HLRD/TAD text plus empirical validation against real SCIP output — no genuine architectural ambiguity remained unresolved by those sources, so per directive D5 §21 no ADR was created. Notably: call-edge derivation does **not** live in this adapter at all — SCIP's wire format has no deterministic call signal, so `CALL_RELATIONSHIP` is simply not produced (see the D5 top-level update note and `docs/resources.md`'s SCIP row). |
| ADR-005 | CodeQL Integration Strategy | **C**, now **RESOLVED by D6 implementation, no ADR needed** | The single-entity-finding evidence-shape question is resolved: a "problem"-kind result becomes a role on the existing FILE entity, not a fabricated `Evidence` record (`Evidence` needs two endpoints; a unary finding has one, and no `BaseEntityType` represents "a finding" — extending the ontology would be a genuine new architectural decision outside D6's scope, so this is documented as a V1 limitation instead). The integration mechanism is also resolved: `codex.provider.codeql_adapter.CodeQLAdapter` consumes an already-generated SARIF file only, never invokes `codeql`, mirroring D5's ADR-004 precedent — see the D6 top-level update note and `docs/resources.md`'s CodeQL row for the full licensing reasoning. |
| ADR-006 | Sourcegraph/RepoGraph Integration Strategy | **C** | Open, and blocked on the Sourcegraph API research gap (see §C). |
| ADR-007 | SLM Selection | **C + G** | Genuinely open per TAD §84 ("SLM/LLM selection 🟡 ADR"), but realistically needs benchmark data before it can be decided (research item as well as a decision). |
| ADR-008 | LLM Selection | **C + G** | Same reasoning as ADR-007. |
| ADR-009 | Embedding Strategy | **B (partly) + C (partly)** | *Whether* embeddings are mandatory is **already decided by TAD** (§3, §27, §34: optional, not mandatory) — do not reopen that question. *Which* embedding technology, if ever adopted, remains open but is P3/deferred (V1 doesn't need it at all). |
| ADR-010 | Search/Ranking Engine | **B (formula) + C (infra, narrower than the title suggests)** | The ranking *algorithm* is already decided by TAD §36-38 (BM25 + structural + proximity + Jaccard, with explicit formulas) — do not reopen the formula. What's genuinely open is narrower: in-process BM25 vs. standing up dedicated search infrastructure. Picking a BM25 *library* for the in-process case is implementation detail (**D**), not an ADR. |
| ADR-011 | Cache Technology | **C** | Genuinely open (TAD §53-54 defer technology, not architecture — the cache *architecture* is closed). |
| ADR-012 | Graph Versioning Strategy | **B — CLOSED/SUPERSEDED** (now formal in TAD §77 itself, post-reconciliation) | TAD §19-21 and §71 already fully specify the strategy (composite version key, snapshot+diff, immutable-once-published, locked-version reads), and TAD §84 marks "Versioning" and "Historical graph" 🟢 CLOSED. Not a genuine open ADR. The only residual question — which storage engine holds snapshots/diffs — folds into ADR-001/ADR-002, making a separate ADR-012 partly a duplicate (**E**) of those. |
| ADR-013 | Historical Graph Reconstruction | **B — CLOSED/SUPERSEDED** (now formal in TAD §77 itself, post-reconciliation) | TAD §21 already specifies "nearest snapshot + changesets," marked 🟢 CLOSED in §84. Residual storage-tech question folds into ADR-001/002. |
| ADR-014 | Runtime Adapter Strategy | **B (policy) + C (which providers, P3)** | "Optional, provider-specific, no universal instrumentation" is already decided (HLRD §14, TAD §57, and directive §8-9 explicitly forbid reopening this). What's open — which concrete runtime provider(s), if any, V1 actually integrates — is real but low priority since the capability itself is optional. |
| ADR-015 | API Protocol | **C** | Genuinely open (TAD §69 explicitly defers it to "a dedicated technical design deliverable"). Research added **MCP** as a candidate alongside REST/GraphQL/gRPC. |
| ADR-016 | Authentication/Authorization | **C** | Genuinely open — neither HLRD's "Security and Trust" (§50) nor TAD's "Security Boundary" (§61) go beyond stating the LLM-access constraint; no auth model is specified anywhere. |
| ADR-017 | Deployment Architecture | **C** | Genuinely open (TAD §84: "Deployment technology 🟡 ADR"; §70 gives only a logical sketch). |
| ADR-018 *(new, D2)* | Provider Scoring Factor Sourcing (`evidence_quality`, normalized `freshness`, `cost_factor`) | **RESOLVED 2026-08-30** | Not TAD's own list — surfaced by implementing D2, closed same-phase by explicit decision. `capability_match`/`availability` are Registry-derived; `evidence_quality`/`cost_factor` are supplied provider metadata (`ProviderScoreProfile`, attached once at `register()`, not per query); `freshness` is Registry-derived from the adapter's timestamp via a documented default decay function. Full resolution and rationale: §I. |

**Net effect:** 14 of 17 TAD-listed candidates remain genuine open decisions (several narrower or lower-priority than their titles suggest), 2 (ADR-012, ADR-013) should be treated as already closed by TAD and folded into ADR-001/002 rather than reopened, and 1 (ADR-010) is half-closed (formula fixed, only infrastructure choice open). No ADR should currently be marked D outright, but ADR-010's sub-question of "which BM25 library" is implementation detail within an otherwise-real ADR. D2 surfaced one genuinely new candidate not in TAD's original 17 (ADR-018) — now resolved.

---

## E. Cross-Document Contradiction Report

| ID | Doc A | Doc B | Conflict | Impact | Resolution | Code change needed | Doc change needed |
|---|---|---|---|---|---|---|---|
| **C-1** | HLRD §16 (`TESTS`) | TAD §14 (`TESTED_BY`) | Same concept, **inverted direction**, not just a naming difference: HLRD implies subject=test → object=code; TAD implies subject=code → object=test. | Medium — affects every test-coverage/adapter edge and TAD's own `TEST_COVERAGE` query intent (HLRD §28). | **RESOLVED 2026-08-30** (`docs/architecture-reconciliation.md` §2): `TESTED_BY` is canonical, direction = production code → test. No separate `TESTS` relationship; inverse queries traverse `TESTED_BY` by object at query time. | None — code (`codex.ontology.relationships.RelationshipType.TESTED_BY`) already correct. | **Done** — HLRD §16 amended to `TESTED_BY` with a resolution note. |
| **C-2** | TAD §19 (literal struct: `scip_index_version`, `codeql_snapshot_version`, `runtime_version`) | TAD invariant #1 / §8 / §75 ("canonical graph is provider-independent," "must never depend directly on provider-specific schemas") | TAD contradicts itself: §19's literal `GraphVersion` example hardcodes three specific provider names as struct fields, which a 4th provider would require a schema migration to accommodate — directly against the provider-independence invariant. | High if taken literally — silently reintroduces provider coupling into the one place (`GraphVersion`) that's supposed to be provider-agnostic. | **RESOLVED 2026-08-30** (`docs/architecture-reconciliation.md` §3): generic `provider_versions: dict[str, str]` is canonical; TAD §19's named-field struct was illustrative and is superseded. | None — code (`codex.graph.version.GraphVersion.provider_versions`) already correct. | **Done** — TAD §19 amended to the generic form with a resolution note. |
| **C-3** | HLRD §42-43 (`FULLY_VERIFIED, PARTIALLY_VERIFIED, UNVERIFIED, CONTRADICTED` — 4 values) | TAD §50 (`VERIFIED, PARTIALLY_VERIFIED, QUALIFIED, DISPUTED, INCONCLUSIVE, REJECTED` — 6 values) **and** TAD §5's pipeline diagram (`VERIFIED, QUALIFIED, ABSTAIN` — 3 buckets) | **Three different, unreconciled enumerations of the same "final answer/verification state" concept**, with no stated mapping between them. | **High** — directly gates Phase 9 (Verification Engine, DTD-05) and Phase 8's claim schema. | **RESOLVED 2026-08-30** (`docs/architecture-reconciliation.md` §4, explicit human decision): TAD §50's 6-value enum is canonical and internal. HLRD §42-43's 4 labels and TAD §5's 3 routing buckets are both derived, tested mapping functions from that one enum — never independently defined or re-derived ad hoc. | None yet — binding on Phase 9 when it's built, not on any current code. | **Done** — TAD §50 marked canonical with both mapping tables; TAD §5 diagram annotated; HLRD §42-43 marked as a presentation mapping with a pointer to TAD §50. |
| **C-4** | TAD §15 (`Evidence.confidence`, provider-reported) | TAD §25 (SLM confidence, calibrated probability), §31 (`evidence_quality`, registry-level), §48 (Verification `V`, composite formula) | Not a logical contradiction, but the word "confidence"/"quality" is reused across at least 4 distinct scoped values with different owners and formulas, with no namespacing convention. | Medium — documentation-clarity risk today (only `Evidence.confidence` exists in code); real implementation-bug risk once the other three exist (e.g. feeding `Evidence.confidence` where Verification's `V` is expected). | Recommend a terminology appendix in the TAD distinguishing the four scopes. No decision forced now. | None. | Suggested TAD addition, not required immediately. |
| **C-5** | This directive (assumes DTD-01..05 are separate documents) | Actual repo state (`docs/HLRD.md`, `docs/TAD.md` only) | See §0. Not an HLRD/TAD contradiction, but a mismatch between what's being asked for and what exists. | Low urgency for code, but blocks treating "DTD audit" as literally complete — there is nothing beyond TAD's own inline sections to audit. | **RESOLVED 2026-08-30** (`docs/architecture-reconciliation.md` §5, explicit decision): DTD-01..05 stay embedded in TAD as section ranges — no separate documents. A precise TAD-section mapping for each DTD is now recorded. Standalone files would require their own ADR first if ever pursued. | None. | **Done** — no doc split; mapping table lives in the reconciliation doc. |
| **C-6** | TAD §12 (`RepositorySymbol` example: 7 fields) | HLRD §18 (Canonical Identity: repo, revision, source location, qualified name, provider IDs, language, entity type) | Not a true contradiction — TAD §12 is explicitly "Example node," not an exhaustive schema — but worth recording so the extra fields in code (`repository_id`, `repository_revision`, `language`, `provider_ids`, `created_at`) read as a deliberate synthesis of HLRD §18, not an ungrounded addition. | Low. | Code is correct as-is. | None. | Suggested one-line note in TAD §12: "see HLRD §18 for the full identity field set." |

No contradiction above was silently resolved by picking a side without recording it — C-1 and C-2 recommend keeping the code's existing (correct) behavior but require a documentation update to make that an explicit, recorded decision rather than silent drift; C-3 is left genuinely open pending a human decision, per the directive's own conflict rule (§51).

---

## F. Current Code Architecture

| Module | Responsibility | Spec section |
|---|---|---|
| `codex.ontology.entities` | Base entity types, roles, `RepositorySymbol` node model, canonical ID hashing | HLRD §16-18, TAD §12-13 |
| `codex.ontology.relationships` | `RelationshipType` enum + derived-relationship registry | HLRD §16, TAD §14 (see **C-1**) |
| `codex.evidence.model` | `Evidence`, `EvidenceCohort`, `EvidenceStatus`, `CoverageStatus`, `CanonicalRelationship` | TAD §15-18, §73 |
| `codex.evidence.store` | `EvidenceStore` protocol + `InMemoryEvidenceStore` | TAD §53 (Evidence Store) — storage tech deferred (ADR-002) |
| `codex.graph.version` | `GraphVersion` (composite version, immutability via `.publish()`) | TAD §19-20, invariant #4 (see **C-2**) |
| `codex.graph.store` | `GraphReader`/`GraphStore` protocols (read/write split) | TAD §12, §62, invariants #6-8 |
| `codex.graph.memory_store` | `InMemoryGraphStore` (NetworkX-backed) | TAD §53 (Canonical Graph Store) — storage tech deferred (ADR-001) |
| `codex.repository.manager` | `RepositoryManager`: register/clone, HEAD revision, changed-file diff | TAD §7 |
| `codex.repository.models` | `RepositoryMetadata`, `ChangeSet` | TAD §7, §72 |
| `codex.provider.capability` | `Capability` (D1) | TAD §9-10 |
| `codex.provider.contract` | `ProviderAdapter` protocol + supporting types (D1) | TAD §8-9, §64; directive §11-13 |
| `codex.provider.git_adapter` | `GitAdapter` — first concrete `ProviderAdapter` (`HISTORY`, `CO_CHANGE`) (D3) | HLRD §13; TAD §7, §14, §72; directive D3 |
| `codex.provider.scip.wire` | Generic, dependency-free protobuf wire-format reader (D5) | (implementation detail — generic binary format, not a spec section) |
| `codex.provider.scip.index` | SCIP-specific typed decoder built on `wire.py` (D5) | `scip.proto` (`sourcegraph/scip`, Apache-2.0) |
| `codex.provider.scip.mapping` | SCIP -> Codex ontology mapping helpers: `Kind`/suffix -> `BaseEntityType`, symbol-string parsing (D5) | HLRD §16-18; TAD §12-13; directive D5 §4, §7-10 |
| `codex.provider.scip_adapter` | `SCIPAdapter` — second concrete `ProviderAdapter` (`SYMBOL_DEFINITION`, `SYMBOL_REFERENCE`, `IMPLEMENTATION`, `TYPE_RELATIONSHIP`) (D5) | HLRD Resource Map §62; TAD §8-9; directive D5 |
| `codex.provider.codeql_adapter` | `CodeQLAdapter` — third concrete `ProviderAdapter` (`DATA_FLOW`) (D6) | HLRD Resource Map §62; TAD §8-9, §10; directive D6 |
| `codex.registry.models` | `ProviderEvaluationStatus`, `ProviderEvaluation` (D2) | TAD §10 |
| `codex.registry.scoring` | `ProviderScoreInputs`, `ProviderScoreProfile`, `provider_score()`, `default_freshness_score()`, `PROVIDER_SCORE_WEIGHTS` (D2, ADR-018) | TAD §31; directive D2 §4; ADR-018 |
| `codex.registry.registry` | `CapabilityRegistry` (D2) | TAD §10, §31 |
| `codex.ingestion.models` | `IngestionResult`, `ProviderRunOutcome`, `ProviderRunStatus` (D4) | TAD §17, §19-20, §64, §72-73 |
| `codex.ingestion.pipeline` | `IngestionPipeline` — PROVIDERS -> INGESTION PIPELINE -> EVIDENCE NORMALIZATION -> CANONICAL GRAPH (D4) | TAD §72-73; HLRD §23; directive D4 |

**Zero code exists** for 12 of TAD's 18 named components (§6): Entity Resolution Engine, Query Understanding Engine, SLM Gateway, Query Planner, Retrieval Engine, Ranking Engine, MSS Builder, LLM Gateway, Verification Engine, Telemetry Store, Artifact Store, Offline Calibration Pipeline. Provider Adapter Manager now has a contract (`codex.provider.contract`, D1) and three concrete adapters (`GitAdapter` D3, `SCIPAdapter` D5, `CodeQLAdapter` D6) — all three live inside the existing Provider Adapter Manager component, none is a new one. `codex.ingestion` (D4) is the orchestration TAD §72's lifecycle diagram describes: it wires "Evidence Normalizer" (component #4, realized as each adapter's own `normalize()` — a D1 design choice already approved) and "Canonical Graph Engine" (component #5, `codex.graph`, Phase 1) together, plus "Provider Adapter Manager" (#2) and "Capability Registry" (#3) — it is not itself a new named TAD component, so the 12-component "zero code" count is unchanged even though real, tested orchestration logic now spans those four, and now three independent provider adapters (Git, SCIP, CodeQL) all flow through it unmodified. 6 of 18 named components have code (4 from Phase 1 + `codex.provider` + `codex.registry`).

---

## G. Implementation Gap List

**P0 — architectural correctness blockers: all RESOLVED as of 2026-08-30 (see `docs/architecture-reconciliation.md`).**
- ~~Resolve **C-1**~~ — done: `TESTED_BY` canonical, HLRD §16 amended.
- ~~Resolve **C-2**~~ — done: generic `provider_versions` dict canonical, TAD §19 amended.
- ~~Resolve **C-3**~~ — done: TAD §50's 6-value enum canonical, mapping tables added to TAD §50/§5 and HLRD §42-43.
- ~~Decide **C-5**~~ — done: DTD-01..05 stay embedded in TAD; section mapping recorded.

No P0 items remain open. Proceed to P1.

**P1 — V1 functionality blockers (TAD §78 "Mandatory"):**
- ~~`ProviderAdapter` contract/protocol (TAD §9)~~ — ✅ **done (D1)**: `codex.provider`, 21 behavioral tests, 100% coverage.
- ~~Git Adapter proper (evidence-emitting: `CO_CHANGED_WITH`, introductions/deletions — beyond `RepositoryManager`'s revision/diff plumbing, which is a prerequisite, not the adapter)~~ — ✅ **done (D3)**: `codex.provider.git_adapter.GitAdapter`, 30 behavioral tests, 100% coverage. `HISTORY`/`CO_CHANGE` — see §B above.
- ~~Capability Registry (TAD §10)~~ — ✅ **done (D2, ADR-018 resolved)**: `codex.registry`, 79 behavioral tests, 100% coverage. Scoring fully implemented and sourced end-to-end — see §I.
- ~~Ingestion pipeline: provider extraction → graph upsert (TAD §72)~~ — ✅ **done (D4)**: `codex.ingestion.{models,pipeline}`, 22 behavioral tests, 100% coverage. Wiring `RepositoryManager.detect_changed_files()` directly into a change-detection *trigger* for `IngestionPipeline.run()` remains open (see the Repository Manager row in §B above) — D4 orchestrates a given target revision, it does not decide *when* to ingest.
- ~~SCIP Adapter~~ — ✅ **done (D5)**: `codex.provider.scip_adapter.SCIPAdapter`, 99 SCIP-specific tests, 100% coverage on all four new modules. `SYMBOL_DEFINITION`/`SYMBOL_REFERENCE`/`IMPLEMENTATION`/`TYPE_RELATIONSHIP` — see §B above.
- ~~CodeQL Adapter (resolve the single-entity-finding evidence-shape question as part of this work, per ADR-005)~~ — ✅ **done (D6)**: `codex.provider.codeql_adapter.CodeQLAdapter`, 45 CodeQL-specific tests, 100% coverage. `DATA_FLOW` capability; evidence-shape question resolved by a role-only representation for single-location findings — see §B above. ADR-005 resolved, no new ADR needed.
- Entity Resolution + Reconciliation Engine (contradiction-score formula, TAD §38) — precondition now met (Git, SCIP, and CodeQL are all concrete providers, D3/D5/D6, whose evidence can genuinely land on the same relationship key), but the engine itself is still `NOT_IMPLEMENTED`.
- A repository-graph Adapter (Sourcegraph or RepoGraph-style) — blocked on the Sourcegraph research gap. **D7, next.**
- Telemetry Store, Artifact Store, LLM Gateway, DTD-02..05 — all `NOT_IMPLEMENTED`.

**P2 — important, non-blocking for a minimal vertical slice:**
- Runtime Adapter (explicitly optional).
- Store-level enforcement of the graph mutation boundary (today it's a typing convention only).
- Artifact Store's actual URI-resolution service (`raw_reference` scheme is now validated as of D1, but nothing resolves the URI to bytes yet).
- Terminology cleanup for **C-4** (confidence overload).

**P3 — future optimization, correctly out of scope (TAD §3, §79):**
- Embeddings (technology choice only — "is it mandatory" is already closed: no).
- Relational/GNN embeddings, multi-repository federation, online learning beyond telemetry/cache tuning.

---

## H. Ordered Implementation Plan

Concretizes TAD §80's phases into the actual next steps given what exists today:

0. **Resolve P0 items** — ✅ done (`docs/architecture-reconciliation.md`; C-1/C-2/C-3/C-5 all resolved, HLRD/TAD amended). No code was needed.
1. **`ProviderAdapter` contract (D1)** — ✅ **done**: `codex.provider.{capability,contract}`, 21 behavioral tests, 100% coverage. See `docs/architecture-reconciliation.md`-style traceability above.
1b. **Capability Registry (D2)** — ✅ **done, including ADR-018 resolution**: `codex.registry.{models,scoring,registry}`, 79 behavioral tests, 100% coverage. All 5 `ProviderScore` factors now fully sourced (§I).
2. **Git Adapter (D3)** — ✅ **done**: `codex.provider.git_adapter.GitAdapter`, 30 behavioral tests, 100% coverage. First concrete `ProviderAdapter`, no external wire format, extends the already-tested `RepositoryManager`. See §B above.
3. **Ingestion pipeline (D4)** — ✅ **done**: `codex.ingestion.{models,pipeline}`, 22 behavioral tests, 100% coverage. `PROVIDERS -> INGESTION PIPELINE -> EVIDENCE NORMALIZATION -> CANONICAL GRAPH`, provider-independent orchestration through the Capability Registry, failure isolation, incremental accumulation, contradictory-evidence preservation, deterministic graph versioning. See §B above.
4. **SCIP Adapter (D5)** — ✅ **done**: `codex.provider.scip_adapter.SCIPAdapter`, 99 SCIP-specific behavioral tests, 100% coverage on all four new modules. Second concrete `ProviderAdapter`, no new dependency (independently-written decoder). See §B above.
5. **Entity Resolution + Reconciliation Engine** (contradiction score, `CanonicalRelationship.status` assignment) — precondition now met (Git + SCIP + CodeQL all exist), genuinely meaningful to build next but not started. Not part of D5's or D6's scope per the Phase D directive.
6. **CodeQL Adapter (D6)** — ✅ **done**: `codex.provider.codeql_adapter.CodeQLAdapter`, 45 behavioral tests, 100% coverage. Third concrete `ProviderAdapter`, `DATA_FLOW` capability, resolves the evidence-shape question from research as part of ADR-005 (no new ADR needed — resolved by implementation). Consumes a pre-generated SARIF file only, per the CodeQL CLI's own licensing terms. See §B above.
7. **Repository-graph Adapter** (Sourcegraph, or a RepoGraph-style tree-sitter fallback if the Sourcegraph API gap isn't closed by then). Not started, next per the Phase D directive's order (D7).
8. **Telemetry Store + Artifact Store** — needed before Query Understanding/Planning have anywhere to record decisions, and before adapters need `raw_reference` resolution.
9. **DTD-02 Query Understanding** (Tier-0 deterministic + SLM contract) — first Intelligence Engine component; needs a populated graph from steps 1-7.
10. **DTD-03 Query Planner** (Capability Registry-driven, model-independent, testably zero LLM/SLM imports) — needs DTD-02's `QueryContract`.
11. **DTD-04 Retrieval/Ranking/MSS** — needs DTD-03's `RetrievalPlan`.
12. **LLM Gateway + structured claims** (Phase 8) — needs DTD-04's `EvidencePackage`.
13. **DTD-05 Verification Engine** — **blocked on C-3** being resolved first.
14. **Offline Calibration Pipeline + benchmark corpus** (Phase 5/11) — needs an end-to-end pipeline to generate telemetry to calibrate against.
15. **Production hardening** (Phase 6).

Each step should land as its own reviewable commit with tests, per the directive's `audit → reconcile → trace → implement one phase → test → verify → checkpoint` loop — the same pattern already used for Phase 1.

---

## I. ADR-018 — Provider Scoring Factor Sourcing — RESOLVED 2026-08-30

**Status: RESOLVED.** Originally raised as a finding during D2 implementation (original writeup preserved below, unmodified, for the audit trail); closed by explicit architecture decision during D2 review. `CapabilityRegistry` and its tests were refactored to match — see the updated contract in "Resolution" below.

### Resolution

| Factor | Origin (final) |
|---|---|
| `capability_match` | Registry-derived: `1.0` for any provider `providers_for()` returns (it declares the capability), and such a provider is the only kind ever scored — non-declaring providers are excluded before scoring, never scored at `0.0`. |
| `availability` | Registry-derived: `adapter.availability(capability, repository)` (D1), read fresh on every `evaluate()`/`rank()` call. |
| `evidence_quality` | **Supplied metadata**, not computed: `ProviderScoreProfile.evidence_quality`, attached once via `CapabilityRegistry.register(adapter, profile)` — canonical per-provider configuration, not a per-query input. Pydantic-validated to `[0.0, 1.0]`. |
| `cost_factor` | **Supplied metadata**, same mechanism: `ProviderScoreProfile.cost_factor`, already normalized `[0.0, 1.0]` by whoever configures the profile — never a raw monetary value. |
| `freshness` | Registry-derived from the adapter's own `freshness` timestamp (D1) via `codex.registry.scoring.default_freshness_score()` — a single generic (provider-neutral) exponential decay with a documented, swappable half-life (`DEFAULT_FRESHNESS_HALF_LIFE`, 24h). Explicitly a calibration point (see below), not a claimed-final algorithm. |

**Why a profile, not per-call arguments:** the original D2 implementation required `rank()` callers to supply `evidence_quality`/`freshness_score`/`cost_factor` as dict arguments on every call — functionally correct (nothing was invented) but architecturally wrong, because two different callers could supply different values for the same provider/capability/repository and get different rankings. `ProviderScoreProfile` is now attached once at `register()` time (registry-owned state), so `rank(capability, repository)` — no scoring kwargs at all — is deterministic for a given repository regardless of caller. `test_ranking_does_not_depend_on_caller_supplied_scoring_values` proves the old signature is gone (calling with the removed kwargs raises `TypeError`).

**`evidence_quality`/`cost_factor` remain explicitly unresolved *sourcing policy*, by design:** ADR-018 decides *where the registry looks* (a profile attached at registration), not *how a real value gets computed* for a real provider (SCIP, CodeQL, ...) — that remains an open extension/calibration point, deliberately not decided in D2 per the directive's explicit instruction not to invent a universal quality formula or cost normalization. A provider registered without a profile is fully usable for discovery/evaluation (`providers_for()`, `evaluate()`); `rank()` raises a clear `ValueError` naming it if asked to score it, rather than guessing.

**Final `rank()` contract:**

```python
def rank(
    self, capability: Capability, repository: RepositoryMetadata, *, now: datetime | None = None
) -> list[ProviderEvaluation]: ...
```

`now` is accepted only to make freshness decay deterministic in tests; real callers omit it. No other parameters — this is the structural guarantee behind "ranking does not depend on arbitrary caller-provided scoring values."

**No cross-document contradiction found.** TAD §31 gives the aggregation weights and normalization requirement but never defined individual-factor sourcing; this decision fills that gap without changing the formula, the weights, the `capability_match=0` exclusion rule, or anything in the (still-untouched, still-approved) D1 `ProviderAdapter` contract. Provider-independence, eligibility-vs-availability separation, and deterministic tie-broken ordering are all unchanged and re-verified by the updated test suite.

---

### Original finding (2026-08-30, preserved for the audit trail)

Discovered while implementing D2 (`CapabilityRegistry.rank()`). Not silently resolved — flagged here per the Phase D directive's own instruction ("if D2 exposes an underspecified behavior that cannot be resolved from HLRD/TAD/reconciliation/resources, STOP and report it").

TAD §31 defines the `ProviderScore` aggregation:

```
ProviderScore =
    0.40 capability_match
  + 0.20 evidence_quality
  + 0.15 availability
  + 0.15 freshness
  + 0.10 cost_factor
```

...and states each factor is normalized to `[0.0, 1.0]`, with `capability_match = 0` excluding a provider before scoring. It does **not** define how any individual factor is computed — that was always going to be down to whatever implements the registry. D1 (already approved and closed) resolved two of the five:

| Factor | Source | Status |
|---|---|---|
| `capability_match` | `capability in adapter.supported_capabilities` — 1.0 if the provider is a candidate at all (it's never scored otherwise), 0.0 (excluded) otherwise | **Resolved**, derivable from D1 |
| `availability` | `adapter.availability(capability, repository)` — already `[0.0, 1.0]` per the D1 clarification | **Resolved**, direct D1 property |
| `evidence_quality` | — | **No source.** No adapter property, no formula in HLRD/TAD, no mention in the reconciliation docs. `Evidence.confidence` is per-*evidence-record* and only exists after extraction — it can't inform a pre-extraction provider-selection score for a query that hasn't retrieved anything yet, at least not without a historical/telemetry aggregation mechanism that doesn't exist yet (Telemetry Store, TAD §65, is itself `NOT_IMPLEMENTED`). |
| `freshness` (as a score) | — | **Half-resolved.** The *raw signal* exists (`adapter.freshness -> datetime \| None`, D1), but converting a timestamp into a `[0,1]` score needs a staleness/decay policy (how many hours/days maps to what score?) that is defined nowhere. |
| `cost_factor` | — | **No source.** No adapter property, no formula. TAD/HLRD mention "cost" only as this weighted factor name; an earlier turn's own guidance ("do not allow cost expressed in raw currency to contaminate the score") implies it should already be pre-normalized before use, but never says by whom or how. |

**What D2 does about it:** implements the formula and weights exactly as specified (`codex.registry.scoring`, tested independently of the registry in `test_provider_scoring.py`), and implements everything the registry *can* determine on its own (registration, capability discovery, eligibility/health/availability evaluation and classification). For the three unresolved factors, `CapabilityRegistry.rank()` requires them as **explicit, required, per-provider-name keyword arguments with no default** — calling `rank()` without an entry for every usable candidate raises `ValueError` naming the provider. Nothing in `codex.registry` guesses a number for any of the three.

**Two candidate resolutions, neither decided here:**
1. Extend the (already-approved) `ProviderAdapter` contract with `evidence_quality`/`cost_factor` properties (mirroring how `health_status`/`availability`/`freshness` already work) — plausible since TAD §9's own attribute list already established the pattern of adapters reporting simple facts about themselves.
2. Treat these as Telemetry/Offline-Calibration concerns (TAD §59, §65-66) — historical per-provider statistics computed from accumulated production evidence, fed into `rank()` by whatever calls the registry (a future D3+ Planner), rather than adapter-reported.

This does not block D3 (Git Adapter): `providers_for()`/`evaluate()`/registration work fully without it, and `rank()` is usable today by any caller (including future D3+ code) willing to supply the three inputs explicitly, exactly as the D2 tests do.

---

## J. D5 (SCIP Adapter) Closure Audit — 2026-08-31

Per the D7 directive's Phase A: re-verified the D5 report's claims directly against `src/codex/provider/scip_adapter.py`, `src/codex/provider/scip/{index,mapping,wire}.py`, `tests/test_scip_*.py`, and a fresh decode of the real fixture `tests/fixtures/scip/typescript_sample.scip` — not re-trusted from the earlier D5 report text. No SCIP implementation code was modified during this audit (per the directive's explicit instruction not to touch D5 code "merely to make the audit look complete"); 99/99 existing SCIP tests re-confirmed passing unchanged.

| D5 Item | Status | Evidence | Action |
|---|---|---|---|
| External library identity (`<manager>:<name>@<version>`) | **CLOSED** — deterministic and safe | `_resolve_symbol` (`scip_adapter.py:299-309`) only builds an `EXTERNAL_LIBRARY` identity when `parse_symbol()` (`mapping.py:225-235`) successfully splits a symbol into exactly 5 space-separated tokens; a symbol that doesn't parse returns `None` and is **skipped**, never fabricated. Re-decoded the real fixture: 8 genuine external symbols found (`npm typescript 5.9.3` — TypeScript's own bundled lib.d.ts, e.g. `console`, `Math.PI`), all with real, non-placeholder manager/name/version. | None required. |
| External metadata / version presence | **DEFERRED** (documented gap, not a defect) | SCIP's own symbol-string grammar uses a literal `"."` token as the placeholder for an unset manager/version (per `scip.proto`'s `Package` message comments) — `parse_symbol()` would faithfully carry that `"."` straight into `qualified_name` (e.g. `.:somepkg@.`) since it never special-cases the placeholder. No real fixture available in this environment reproduces a `"."`-sentinel symbol (the only real artifact on hand, `scip-typescript` output, always resolves manager/version to real values, even for TypeScript's own standard library), and no existing test constructs one synthetically either. The resulting identity would still be deterministic and non-colliding in practice (package `name` differs across real libraries even when manager/version don't), so this is not a correctness defect — but it is genuinely unverified. | Recommend a follow-up synthetic test asserting `"."`-sentinel symbols still produce a stable, non-crashing identity; not blocking for D7. |
| Cross-provider external resolution (Git/SCIP/CodeQL) | **DEFERRED** — not yet applicable | Confirmed by direct inspection: `GitAdapter` has no `EXTERNAL_LIBRARY`-producing capability at all (`HISTORY`/`CO_CHANGE` only, in-repo files); `CodeQLAdapter` never creates `EXTERNAL_LIBRARY` entities (confirmed in D6's own closing report and by grep — `DATA_FLOW` only touches FILE entities). SCIP is currently the **only** provider that asserts external-library identity, so there is no second, independent source to converge or reconcile against yet — forcing a convergence rule now would be inventing a decision the architecture doesn't yet need. | Revisit if/when a future provider (e.g. a package-manifest adapter) also asserts external-library identity; no action now. |
| `SingleLineRange` | **CLOSED** | `_decode_single_line_range` (`index.py:165-172`) reads field 8 per the documented, spec-confirmed field table; exercised by `test_decode_single_line_range_typed_field` (`test_scip_index.py:91-98`), a synthetic-but-schema-faithful byte-level fixture (not vendored from any external test suite). | None required. |
| `MultiLineRange` | **CLOSED** | `_decode_multi_line_range` (`index.py:175-183`) reads field 9; precedence over the deprecated packed field 1 is enforced in `_decode_occurrence` (`index.py:186-199`, checks 9 before 8 before 1) and directly matches the `scip.proto` comment quoted verbatim in `test_decode_multi_line_range_takes_precedence_over_deprecated_range` (`test_scip_index.py:111-115`): *"When both typed_range and the deprecated range field are present, the typed_range field should be used."* Re-decoding the real fixture found it uses **only** the deprecated packed-range field (field 1) for every occurrence inspected — `scip-typescript 0.4.0`'s actual output never emits fields 8/9 — so the precedence-ordering logic itself is validated only against the spec text and synthetic fixtures, never against a real single/multi-line-range-emitting producer. | Document as a known real-artifact coverage gap (this table); no code defect found, no action required unless a producer emitting fields 8/9 becomes available. |
| Range boundary convention (inclusive/exclusive) | **AMBIGUITY** (pre-existing, not introduced by D5) | `ScipRange`'s own docstring states "half-open `[start, end)`... 0-based" (`index.py:56-57`), and `SourceLocation` (`codex/ontology/entities.py:62-69`) simply stores `start_line`/`end_line`/`start_column`/`end_column` with **no documented numbering or boundary convention anywhere in HLRD/TAD/the ontology module itself**. D5 populates `SourceLocation` directly from SCIP's own half-open 0-based values without an explicit statement that this *is* the Codex-wide convention (there is currently only one populated example — SCIP — so nothing yet conflicts, but nothing is written down either). | Flagged, not blocking: recommend a one-line clarification in `codex/ontology/entities.py`'s `SourceLocation` docstring (and TAD §13-14) stating the convention explicitly (0-based, half-open, matching SCIP/LSP) the next time any document in that area is touched. Not severe enough to warrant a STOP — no second provider populates ranges with a conflicting convention yet. |
| Relationship derivation (no CALLS/EXTENDS/DEPENDS_ON from generic references) | **CLOSED** | Direct code inspection, not re-trusted from the D5 report: `scip_adapter.py` and `scip/mapping.py` use exactly three `RelationshipType` values — `REFERENCES`, `IMPORTS`, `IMPLEMENTS` — confirmed by grep across both files. `is_implementation` never becomes `EXTENDS` (mapping.py's own docstring documents why: SCIP can't distinguish the two). No `CALL_RELATIONSHIP`/`DEPENDS_ON` emission exists anywhere in the SCIP provider. | None required. |
| D1 (`ProviderAdapter`) integration | **CLOSED** | `SCIPAdapter` implements every member of the `ProviderAdapter` `Protocol` (`contract.py:127-167`: `provider_name`, `provider_version`, `supported_capabilities`, `health_status`, `availability`, `freshness`, plus `validate`/`check_eligibility`/`extract`/`normalize`) with matching signatures — structural conformance confirmed by a clean `mypy src` (re-run in this audit). | None required. |
| D2 (`CapabilityRegistry`) integration | **CLOSED** | `SCIPAdapter` is registered and evaluated through the real, unmodified `CapabilityRegistry` in `test_scip_adapter.py:533-592` — no SCIP-specific branching exists anywhere in `codex.registry`. | None required. |
| D4 (`IngestionPipeline`) integration | **CLOSED** | The same two tests run `SCIPAdapter` through the real, unmodified `IngestionPipeline` end-to-end (`test_scip_adapter.py:535, 592`) — both re-confirmed passing in this audit, not bypassed. | None required. |

**Summary:** 8 of 10 items **CLOSED** on direct re-verification (not merely re-asserted from the prior D5 report). 1 item (external metadata/version placeholder) and 1 item (cross-provider resolution) are **DEFERRED** — genuinely not yet applicable/verifiable, not silently skipped. 1 item (range boundary convention) is a pre-existing **AMBIGUITY** in the shared ontology, not a D5-introduced defect, flagged for future documentation rather than a STOP. **No DEFECT was found.** No D5 code was changed as part of this audit.

---

## K. D7 (Repository-Graph Adapter) Research, Overlap Analysis, and STOP Determination — 2026-08-31

Per the D7 directive's Phase B onward. This section documents the research, capability extraction, and provider-overlap analysis performed **before** any D7 code was written, and the STOP determination that resulted from it. No D7 implementation exists — this is a research-and-analysis-only pass, consistent with the directive's Rule 0.1 ("architecture before code") and its explicit instruction not to reverse the `SPECIFICATION → AUDIT → RESEARCH → LICENSE CHECK → CAPABILITY ANALYSIS → PROVIDER OVERLAP → INDEPENDENCE ANALYSIS → DESIGN → IMPLEMENTATION` order.

### K.1 Candidate identification

HLRD §8 requires V1 to include "at least one qualified repository intelligence/search provider," satisfiable by "Sourcegraph, a qualified open-source repository graph implementation, or another provider satisfying Codex capability requirements." HLRD §9-10 and `docs/resources.md` (as of the D5/D6 research passes) name exactly three candidates — no others are introduced here, per the directive's instruction to use the candidates the repository documents already identify rather than search anew:

1. **Sourcegraph** (`sourcegraph.com`) — HLRD §9's own named adapter.
2. **`chokevin/repograph`** — HLRD §10, explicitly listed as a "reference implementation... not a mandatory production dependency."
3. **`SillySerpent/Repograph`** — HLRD §10, same explicit framing.

TAD's own component tree (`codex.provider` package sketch) names only `SourcegraphAdapter` — not a separate `RepoGraphAdapter` — and TAD's ADR list carries a single, combined **ADR-006 "Sourcegraph/RepoGraph Integration Strategy"**, not two. This confirms the architecture already treats "which repository-graph source, if any" as one open decision spanning all three candidates, not three independent ones.

### K.2 Research performed this pass (2026-08-31)

All three candidates were re-researched directly (not re-trusted from the D5/D6-era `docs/research/provider-formats.md` notes), per the directive's licensing-gate requirement:

- **`chokevin/repograph`**: full `README.md` fetched directly from `raw.githubusercontent.com/chokevin/repograph/main/README.md` (reachable, HTTP 200). License confirmed **MIT** from the README's own "License" section (no separate `LICENSE` file found at that path — a 404 was returned for `/LICENSE`, so the README section is the license record).
- **`SillySerpent/Repograph`**: full `README.md` and `LICENSE` fetched directly from `raw.githubusercontent.com/SillySerpent/Repograph/master/`. License confirmed **AGPL-3.0-only** verbatim (`SPDX-License-Identifier: AGPL-3.0-only`) — matches the prior finding, now confirmed against the primary source rather than carried forward.
- **Sourcegraph**: `sourcegraph.com` re-tested directly (`curl -s -o /dev/null -w "%{http_code}" https://sourcegraph.com`) — returned `000` (connection failure), confirming the environment's egress proxy still blocks it as of this pass. The `sourcegraph/sourcegraph-public-snapshot` repo itself is reachable via `raw.githubusercontent.com` (HTTP 200) but is the open-sourced application codebase, not the hosted product's API/licensing terms.

### K.3 Candidate comparison

| Dimension | Sourcegraph | `chokevin/repograph` | `SillySerpent/Repograph` |
|---|---|---|---|
| Project purpose | Hosted code-search/navigation platform, SCIP-backed | Fast local CLI graph builder for LLM-agent context | Local repository-intelligence tool (static + optional runtime overlay) |
| Graph model | Precise, SCIP-backed code navigation + full-text search | 7 node types / 7 edge types (repo/dir/file/class/function/method/variable; contains/imports/calls/references/inherits/defines/method_of) | Files/folders/symbols/imports/calls/callback registrations/inheritance/variable flow/type-reference hints, plus dead-code/impact/community/co-change analysis on top |
| Languages | Many (via SCIP indexers) | Go, JS/TS, Python (plugin architecture) | Python, JavaScript, TypeScript (+ Flask/FastAPI/React/Next.js framework adapters) |
| Update mechanism | Continuous, server-managed | On-demand CLI rebuild (`repograph sync`), <2s target | `repograph sync --full`, static-first with optional runtime/coverage overlay merge |
| Output/interface | GraphQL API (live, hosted) | CLI text or `--format json`; Go library | CLI, Python API, MCP server — all over an internal Kuzu store, **no portable artifact file** |
| Consumable as a static artifact? | No — live API only | Yes in principle (`--format json`), but an informal, undocumented schema | No |
| License | Unclear — hosted product terms unreachable; OSS snapshot repo is Apache-2.0 but that isn't the product's terms | **MIT** (no obstacle) | **AGPL-3.0-only** (confirmed) |
| Would require Codex to depend on a running external process/service? | Yes (its GraphQL API, or self-hosting the full server) | Only if Codex invoked the CLI itself (avoidable in principle by requiring a pre-generated JSON artifact, mirroring the SCIP/SARIF pattern) | Yes for any live capability; a static "artifact" would mean reimplementing its whole pipeline, not reading one file |
| Overlap with Git/SCIP/CodeQL already in Codex | Its own "precise navigation/symbol info/cross-reference" are explicitly SCIP-backed per its own docs — largely duplicates D5; "code search"/"repository navigation" are retrieval-time concepts, not ingestion-time deterministic evidence | `contains`/`imports`/`references`/`defines`/`method_of` all duplicate SCIP (D5) or Git (D3); its one non-duplicate edge type is `calls` | `imports`/`calls`/`inherits`/`type-reference hints` duplicate SCIP; `co-change coupling` duplicates Git's `CO_CHANGE` (D3) exactly; its "test-reachability" and various demand-side analyzers are cross-cutting *analysis*, not raw provider evidence |

### K.4 Capability matrix (HLRD/TAD-style, directive §5)

| Evidence/Capability | Git | SCIP | CodeQL | D7 candidates |
|---|---|---|---|---|
| FILE identity | ✓ | ✓ | ✓ | ✓ (all three, but converges with existing `build_canonical_id`-based FILE identity already established by Git/SCIP — no new identity value) |
| Symbol definition | | ✓ | | ✓ (duplicate of SCIP) |
| Symbol reference | | ✓ | | ✓ (duplicate of SCIP) |
| CALLS | | | | **Only candidate-new capability** — proposed by both RepoGraph projects, unproven deterministic (see K.5) |
| CALLERS / CALLEES | | | | Same as CALLS — derived from the same unproven edge |
| EXTENDS | | | | `SillySerpent`'s "inheritance links" — same determinism concern as CALLS (syntactic, not type-checked, per its own docs) |
| IMPLEMENTS | | ✓ | | ✓ (duplicate of SCIP) |
| DEPENDS_ON | | | | Neither project deterministically establishes a package-level dependency claim beyond what `imports` already gives (duplicate) |
| IMPORTS | | ✓ | | ✓ (duplicate of SCIP) |
| DATA_FLOW | | | ✓ | Not offered by either RepoGraph project |
| HISTORY | ✓ | | | Not offered |
| CO_CHANGE | ✓ | | | `SillySerpent` offers "git co-change coupling" — **exact duplicate** of Git's existing `CO_CHANGE` (D3) |
| TESTED_BY | | | | `SillySerpent`'s "test-reachability maps" are analysis output (built from coverage/runtime data), not a primary deterministic provider fact in the sense D1-D6 established |
| Configuration relationships | | | | `SillySerpent`'s "config registries/config boundary reads" — cross-cutting analysis, not a canonical-graph relationship type in the current ontology |

### K.5 Provider overlap classification (directive §6)

| Proposed D7 capability | Classification | Reasoning |
|---|---|---|
| FILE identity | **DUPLICATE** | Already established by Git (D3) and SCIP (D5) via the same `build_canonical_id`; a D7 adapter would need to converge with, not duplicate, this identity — trivial to satisfy, adds no new evidence. |
| Symbol definition/reference, IMPORTS, IMPLEMENTS | **DUPLICATE** | SCIP (D5) already deterministically establishes all of these, with broader language coverage (any SCIP indexer) than either RepoGraph project's fixed plugin list. |
| CO_CHANGE | **DUPLICATE** | Git's `GitAdapter.CO_CHANGE` (D3) already deterministically establishes this from real commit history; `SillySerpent`'s version adds nothing Git doesn't already give directly from the repository itself. |
| CALLS / CALLERS / CALLEES | **UNSAFE** (as currently evidenced) | This is the one capability neither Git, SCIP, nor CodeQL currently gives Codex. But per HLRD/TAD's Class A/B/C evidence discipline (directive §11) and the exact same determinism standard D5's own research already applied to SCIP (where a bare reference occurrence was confirmed indistinguishable from a real call without full semantic analysis): both RepoGraph projects build this edge from a tree-sitter/syntactic pipeline with **no documented type-checking or cross-file semantic resolution** (`chokevin/repograph`'s own README: a `Scanner → Workers → Edge Resolution` pipeline advertised at "<2 seconds" for an entire repository — a time budget inconsistent with real semantic analysis across multiple languages; `SillySerpent/Repograph`'s README documents no call-resolution algorithm at all). Treating either project's `calls` edge as Class B (deterministically derived) rather than Class C (semantic inference) is not evidenced by anything either project publishes. Per directive §10/§11, this MUST NOT be implemented as currently evidenced. |
| EXTENDS (inheritance) | **UNSAFE** | Same reasoning as CALLS — `SillySerpent`'s "inheritance links" have no documented resolution algorithm distinguishing a real subclass relationship from, e.g., a same-named type in an unrelated module. |
| DEPENDS_ON, TESTED_BY, configuration relationships | **DERIVED at best, likely UNSAFE** | These are described in `SillySerpent`'s README as outputs of higher-level *analyzers* (impact analysis, config-boundary detection, test-reachability) built on top of its own graph — i.e., derived from lower-level evidence through inference, not asserted directly by any deterministic parser output. Per directive §6, DERIVED capabilities require explicit architectural approval before implementation; none of these were approved, and their derivation algorithms aren't published in enough detail to evaluate determinism regardless. |

**Result: no candidate D7 capability survives classification as UNIQUE or CORROBORATING with demonstrated determinism.** Every capability that would be *new* (not already DUPLICATE of Git/SCIP/CodeQL) falls into UNSAFE or unapproved-DERIVED.

### K.6 Independence analysis (directive §7)

Not reached in substance: since no D7 capability cleared the overlap gate (K.5), there is no D7 evidence to assign an `independence_group` to. Per the canonical rule (an omitted `independence_group` defaults to `NON_INDEPENDENT`), this is a non-issue rather than a gap — nothing is being silently double-counted because nothing is being produced.

### K.7 Licensing gate outcome (directive §4)

| Candidate | License | Safe to study | Safe to reuse code/tests | Safe as a runtime dependency | Research-only? |
|---|---|---|---|---|---|
| Sourcegraph | Unknown (hosted product terms unreachable; OSS snapshot is Apache-2.0 but governs different scope) | Yes, at a README level | N/A — no code proposed for reuse | **Unresolved** — licensing terms for API/self-host use not obtainable from this environment | Yes, pending terms |
| `chokevin/repograph` | MIT | Yes | Legally yes, but HLRD §10 explicitly frames it as reference-only, and D7's own determinism finding (K.5) makes it moot regardless | Not proposed | Yes |
| `SillySerpent/Repograph` | AGPL-3.0-only | Yes | **No** — copyleft; wrapping/vendoring would obligate Codex to AGPL-3.0 | **No** — same reason, and it exposes no artifact to consume without invoking the live (AGPL) engine anyway | Yes |

### K.8 STOP determination

This STOP is filed under directive §22, conditions **3** ("a D7 relationship that cannot be deterministically established"), **4** ("a licensing issue that prevents safe independent implementation" — for `SillySerpent/Repograph` and, pending resolution, Sourcegraph), and **12** ("an architectural decision that cannot be resolved from existing HLRD/TAD/ADRs" — ADR-006 is explicitly still open in `docs/architecture-conformance-audit.md`'s own §D and was never silently assumed closed).

**No D7 code, tests, or documentation beyond this research/analysis was written.** D1-D6 were not touched. See the Final Report for the exact issue, options, and recommendation.

---

## L. SourceLocation Contract Closure — 2026-08-31

Per the post-D7 directive's Phase A. Closes the one AMBIGUITY §J flagged (SCIP Item "Range boundary convention").

**Inspection performed before coding:** grepped HLRD.md and TAD.md for `SourceLocation` (zero matches — the type name itself appears nowhere) and for "source location"/"line number"/"column" (TAD §12 lists it as a `RepositorySymbol` field and TAD §71's traceability diagram names it as the last link in the evidence chain; HLRD only lists "source locations" as one of several things SCIP *may* provide). **Neither document ever states a numbering or boundary convention** — confirmed not a contradiction (nothing to reconcile), a genuine silent gap. Inspected every current populator: `SCIPAdapter._location_from_range` (the only code path that sets `RepositorySymbol.source_location`, confirmed by `grep -r source_location src/`); `GitAdapter` (no location data at all — `HISTORY` is a whole-file lifecycle fact); `CodeQLAdapter` (uses SARIF `region.startLine` internally, for codeFlow endpoint matching only — never attaches a `SourceLocation` to any entity, confirmed by inspection of `_resolve_location`/`_flow_endpoints`). Re-fetched the authoritative OASIS SARIF 2.1.0 schema's `region` definition directly to confirm its convention rather than assume it: `startLine`/`startColumn`/`endLine`/`endColumn` are all **1-based** (`"minimum": 1`), `endLine` is documented as *inclusive* ("the line number of the last character in the region"), and `endColumn` is documented as *exclusive* ("the column number of the character following the end of the region") — structurally the same half-open-at-the-end model SCIP already uses, offset by exactly 1 on all four numbers.

**Resolved as a specification clarification, not an ADR** (directive Phase A §6): no competing designs or tradeoffs exist to decide between — SCIP (the only current populator) already emits one specific, spec-derived convention, and adopting it as the canonical one requires no behavior change to any existing adapter. TAD §12 amended in place with an inline clarifying note (see TAD's own amendment log); no HLRD change was needed (HLRD never attempted to define a convention).

**Canonical convention:** 0-based, half-open (`[start, end)`), matching SCIP/LSP/tree-sitter. `start_column`/`end_column` optional together, never independently (a location may be line-only). No in-type "whole file"/"unknown" sentinel — `RepositorySymbol.source_location = None` already serves that purpose (used today for `EXTERNAL_LIBRARY` entities). `file_path` is repository-root-relative, `/`-separated, matching `build_canonical_id`'s `qualified_name` convention. `repository_id`/`revision` deliberately excluded from the type itself (always inherited from the containing `RepositorySymbol` — adding them would invent redundant, driftable state).

**Code change:** `codex.ontology.entities.SourceLocation` gained field-level bounds (`start_line`/`end_line`/`start_column`/`end_column` all `ge=0`, via pydantic `Field`) and a `model_validator` enforcing `end_line >= start_line`, both columns present-or-absent together, and `end_column >= start_column` when `start_line == end_line`. This tightens a previously fully-unvalidated type into a deterministic contract without changing any existing adapter's behavior — confirmed by all 275 pre-existing tests (including all SCIP tests, whose real-fixture-derived `SourceLocation` values already satisfy the new constraints) passing unmodified. No provider adapter code was changed — SCIP was already conformant by construction; CodeQL and Git don't currently populate this field at all, so there was nothing to convert yet, only a documented conversion rule for when a future capability does.

**Tests:** 12 new tests in `tests/test_source_location.py` — line-only validity, full-precision validity, zero-width (empty) range validity, multi-line ranges (no cross-line column-ordering constraint), rejection of `end_line < start_line`, rejection of `end_column < start_column` on the same line, rejection of partial column precision, rejection of negative coordinates (parametrized across all four fields), and deterministic structural equality for identical inputs. 287 tests total project-wide, 100% coverage on `codex/ontology/entities.py`, clean ruff/mypy.

**Cross-document effects checked (directive Phase A §6):** Entity Resolution (not yet built — will consume `SourceLocation` only as a tie-breaking/corroboration signal per Phase B below, never as primary identity); Evidence (does **not** carry a `SourceLocation` field at all — confirmed by inspection of `codex.evidence.model.Evidence`, which has `raw_reference` but no location field, so this closure has no direct effect there); `raw_reference` (a separate, already-closed URI-scheme contract, unaffected); source-context retrieval/Verification (both `NOT_IMPLEMENTED`, no effect yet); historical graph queries/`graph_version` (unaffected — `SourceLocation` carries no version-dependent state beyond what its containing `RepositorySymbol` already versions). No genuine architectural decision requiring a STOP was found.

---

## M. Entity Resolution — 2026-08-31

Per the post-D7 directive's Phase B. TAD component #7 ("Entity Resolution + Reconciliation Engine") is split into two directive phases; this section covers Entity Resolution only (§N covers Reconciliation).

**Pre-audit findings (directive Phase B §7), from direct inspection of `build_canonical_id`, `RepositorySymbol`, and all three provider adapters — not assumed from prior reports:**

- `FILE` is the **only** base type every current provider can produce: `GitAdapter`, `SCIPAdapter` (file references), and `CodeQLAdapter` all call `build_canonical_id(..., base_type=BaseEntityType.FILE, ...)`. Non-FILE (CLASS/METHOD/...) entities come from `SCIPAdapter` alone — "cross-provider symbol convergence" (directive §12) has no live case with the current three providers; recorded honestly as such rather than exercised with a fabricated second symbol-producing provider.
- `build_canonical_id` already gives *exact* convergence for byte-identical inputs (proven by existing D3/D5/D6 tests). The real, previously undemonstrated risk: each adapter derives its FILE `qualified_name` from its own provider format (`git`'s diff output; SCIP's `Document.relative_path`; SARIF's `artifactLocation.uri`, typed as an open `uri-reference` by the OASIS schema — nothing requires it to already be a bare, repo-relative string). Real fixtures inspected in this and prior phases are all already-canonical plain relative paths (no live defect observed), but nothing previously enforced this.
- `RepositorySymbol.provider_ids` (TAD's own field) is populated only by `GitAdapter` (`{"git": path}`) today; SCIP/CodeQL never set it — sparse, not a defect, but merge logic must not drop it.
- **A real, previously undiscovered defect found by this audit**: `IngestionPipeline._commit_entities` (D4) kept only the *last-committed* `RepositorySymbol` per `canonical_id` on a collision — silently discarding an earlier provider's `roles`/`provider_ids` contribution, directly contradicting D4's own documented "no evidence is ever discarded" invariant (`codex/ingestion/pipeline.py`'s own module docstring). Never triggered by any existing test (no two providers previously produced a colliding `canonical_id` in any test scenario), so it shipped unnoticed through D4-D6's own review passes. Fixed as part of this phase (see below) — this is the "concrete conformance defect" that justifies touching D4's `_commit_entities`/`_commit_relationships`, per the directive's own "unless an actual conformance defect is discovered" carve-out (directive top matter, and §25).

**Design (directive Phase B §8-11):** `codex.resolution.entity_resolver.resolve_entities()` — deterministic, no LLM/SLM/embeddings/fuzzy matching, no second identity system. Two match classes only:

1. **`EXACT_CANONICAL_ID`** — two provider entities already share a `canonical_id` (byte-identical inputs). → MERGE.
2. **`NORMALIZED_PATH_IDENTITY`** — a FILE/DIRECTORY entity's `qualified_name`, run through a new `codex.resolution.paths.normalize_repo_relative_path()` (strips `\`→`/`, leading `./`, leading `/`, collapses `//`), recomputes to a *different* `canonical_id` than another entity already holds. → MERGE, with the **recomputed, normalized** id as the definitive canonical id (never whichever raw entity happened to sort first — order-independence proven by a dedicated test). Non-path-shaped entities (SCIP symbols, `EXTERNAL_LIBRARY` package identities) are never renormalized — their own provider-computed id is trusted as-is, per "only use signals the architecture already supports."

No **ambiguous-match** or **conflicting-identity** case exists in the current three-provider set: `canonical_id` already includes `base_type`/`qualified_name`/`revision` in its hash, so two genuinely different entities (different name, different namespace, different revision, different base type) can never collide, and two entities that DO collide are, by construction, the same real entity — there is no case where the current deterministic signals produce a genuine ambiguity. This is recorded honestly as "structurally prevented, not yet exercised," not assumed to generalize to future providers without re-auditing.

**Merge semantics (directive Phase B §10, provenance-preserving):** `roles` — union, order-preserving, deduplicated. `provider_ids` — dict union (a real key collision with different values would be a genuine conflicting-identity signal; none currently possible since each provider uses its own provider-name key). `lifecycle_status` — a non-default (DELETED/RENAMED) value from either side wins over a plain default ACTIVE from the other, since only `GitAdapter` ever observes lifecycle and ACTIVE-by-default carries no information; proven order-independent (informative side may be either the first- or second-processed entity). `source_location` — first non-`None` wins (deterministic tie-break for a case no current provider pair actually produces two different locations for one entity — documented, not exercised by real data).

**`IngestionPipeline` integration:** `_commit_entities` now re-resolves the full accumulated entity set (previous + this batch) through `resolve_entities()` on every commit — O(N) per call (not O(N²): `resolve_entities` itself is a single dict-keyed pass), an accepted V1-scale cost per the directive's own "do not prematurely optimize without measurements" (§28). Returns a raw-id → resolved-id map; `_commit_evidence` now remaps `Evidence.subject`/`.object` through this map **before** keying `CanonicalRelationship`s (the stored `Evidence` record's own `subject`/`object` fields are never mutated — only the relationship key uses resolved ids), so a normalized-path merge that renames a raw provider id never leaves a relationship dangling on the old one. Proven end-to-end by `test_entity_resolution_converges_differently_formatted_paths_through_the_pipeline` (`tests/test_ingestion_pipeline.py`): two fake providers report the same real file with a canonical and a `./`-prefixed path; the pipeline resolves to one FILE entity, and the second provider's relationship evidence attaches to the *canonical* id, not the raw one.

**Graph version behavior (directive Phase B §13):** unaffected — `resolve_entities()` is a pure function of static entity fields (never mutates a previously-published `GraphVersion`); `IngestionPipeline._materialize_store()` still builds a fresh, immutable store/version per `run()` from the accumulator's state at that moment, exactly as D4 already established. No change to that invariant was needed.

**Tests:** 23 new tests in `tests/test_entity_resolution.py` (path normalization ×5, exact/cross-provider FILE convergence ×2, normalized-path convergence + order-independence ×3, non-merge cases — different directory/base-type/revision ×3, symbol-entity non-renormalization honesty ×1, external-library convergence + version-significance ×2, provenance preservation — provider_ids/roles/lifecycle ×4, source_location tie-break ×1, empty/single-entity edge cases ×2), plus 1 new `IngestionPipeline` integration test. 311 tests total project-wide, 100% coverage on `codex/resolution/`, clean ruff/mypy. `CanonicalRelationship` gained a `contradiction_score: float` field (default `0.0`, backward compatible) in `codex/evidence/model.py`, added now because Reconciliation (§N) needs somewhere to store TAD §38's formula output — not otherwise used by this phase.

No new ADR — this closes TAD component #7's Entity Resolution half using signals the architecture (canonical id hashing, `provider_ids`) already establishes; no genuinely new architectural decision was required. No cross-document contradiction found. D1/D2/D3/D5/D6 untouched; D4's `_commit_entities`/`_commit_relationships` were changed, justified by the concrete defect found above, not as an unrelated contract change.

---

## N. Evidence Reconciliation — 2026-08-31

Per the post-D7 directive's Phase C. Closes TAD component #7's second half.

**A genuine finding, flagged rather than silently resolved (directive Rule 0.2):** the directive's own §15 ("Reconciliation Status Taxonomy") states the canonical taxonomy is "`VERIFIED/PARTIALLY_VERIFIED/QUALIFIED/DISPUTED/INCONCLUSIVE/REJECTED`." Direct inspection of `docs/TAD.md` shows this is **not** the enum that governs `CanonicalRelationship.status` — that field's type is `EvidenceStatus` (TAD §18, `SUPPORTED/WEAKLY_SUPPORTED/DISPUTED/UNRESOLVED/CONTRADICTED/UNSUPPORTED`), already implemented since Phase 1 and closed. The six-value list the directive names is TAD §50's, a **different, later-stage** taxonomy for the Verification Engine's answer/claim-level output (component #9/DTD-05, downstream of LLM synthesis) — confirmed by reading both enum definitions directly (TAD line ~539 vs. line ~1338), not assumed. TAD is internally consistent here (two distinct, correctly section-scoped enums, with an existing C-3-reconciled mapping between them); the conflation is only in the directive's own paraphrase, not a genuine cross-document contradiction, so this did **not** trigger a STOP (directive §32 condition #5/#7 requires an actual contradiction between authoritative documents, and none exists). Resolved by using `EvidenceStatus` — TAD §18's own, purpose-built field — for this phase, and leaving TAD §50's taxonomy untouched and out of scope (correctly deferred to the not-yet-built Verification Engine, per the directive's own §23 instruction not to implement it prematurely).

**Pre-audit findings (directive Phase C §14):**

- `CanonicalRelationship.supporting_evidence_ids`/`.contradicting_evidence_ids` already exist (TAD §73) — populated, not invented.
- `Evidence.effective_independence_group` (TAD §16) already correctly implements "omitted group defaults to non-independent *within the same provider*" (`provider_default:{provider}` — different providers get different default groups, i.e. independent by default, matching TAD §16's own two-part text: `independence_group = provider_default_family`, "if omitted, SHALL be treated as non-independent"). Reused as-is.
- **`provider_authority` (TAD §38's own formula input) has no defined source anywhere in HLRD/TAD** — the same class of gap ADR-018 already resolved for `evidence_quality`/`cost_factor` (TAD §31). Resolved the same way (explicit, `[0,1]`-normalized, caller-supplied) but **not** added to `codex.registry.scoring.ProviderScoreProfile` — that type is scoped to TAD §31's formula and closed (D2/ADR-018); extending it would force every existing D2/D3/D5/D6 test that constructs one to pass a new field it has no reason to care about. Instead, `IngestionPipeline` gained its own `provider_authority: Mapping[str, float] | None` constructor parameter, defaulting missing entries to full trust (`1.0`) rather than raising — deliberately different from ADR-018's "no default" choice, because this factor weights *confidence* on an already-committed relationship rather than *selecting* a provider, so silently under-trusting an unconfigured provider would be a worse default than full trust, and this choice keeps every pre-existing test passing unmodified.
- **No negation mechanism exists anywhere in the ontology.** `RelationshipType` has no "does-not-X" counterpart, and `Evidence` has no boolean/flag for asserting a negative fact — confirmed by inspecting all three real adapters' `RelationshipType` usage (D5/D6 closure audits, §J). The directive's own DISPUTED example ("SCIP → CALLS, Provider-X → DOES_NOT_CALL") is therefore **not representable** by any current provider. This is an intentional, documented limitation, not a silent gap: the algorithm implements the full TAD §38 formula and all six `EvidenceStatus` values generically (proven with handcrafted evidence in `tests/test_reconciler.py`), but `IngestionPipeline._reconcile_relationships` always calls it with `contradicting=()` in production, since there is no deterministic signal available to populate it without inventing one — `DISPUTED`/`CONTRADICTED`/`UNSUPPORTED` are correct-by-construction but not reachable end-to-end with Git/SCIP/CodeQL today.

**Algorithm (`codex.reconciliation.reconciler.reconcile_relationship`):** a relationship whose subject or object isn't a currently-resolved entity is `UNRESOLVED` (checked first — directive §19-20's "missing target is an information/system gap, not a dispute"). Otherwise: `weight = evidence.confidence × provider_authority × freshness_decay` (the freshness term reuses D2/ADR-018's already-established `default_freshness_score`, directly addressing directive §17/§22's "stale evidence affects confidence" — not reinvented). `contradiction_score = Σcontradict_weight / (Σsupport_weight + Σcontradict_weight)`, exactly TAD §38's literal formula (verified against the arithmetic in a dedicated test, not just direction). Confidence combines *supporting* evidence with an independence-aware rule: same-`effective_independence_group` records collapse to their single highest weight first (no double-counting correlated evidence — directive §16/§22), independent groups combine via `1 - Π(1 - weight)` (a standard, deterministic probability-combination rule — not a learned/probabilistic *identity* mechanism, which is what the "no probabilistic matching" prohibition in directive §8 targets for Entity Resolution specifically). Status: both supporting and contradicting present → `DISPUTED` (or `CONTRADICTED` if contradiction dominates, `contradiction_score >= 0.5`, a documented calibration point, not derived from HLRD/TAD, mirroring ADR-018's own framing of its freshness half-life); contradicting-only → `UNSUPPORTED`; supporting-only → `SUPPORTED` (support weight `>= 0.5`, same calibration-point status) or `WEAKLY_SUPPORTED`; neither → `UNRESOLVED`.

**`IngestionPipeline` integration:** a new `_reconcile_relationships()` step runs once per `run()`, after all providers commit and before `_publish()`, recomputing every accumulated relationship's `status`/`confidence`/`contradiction_score` from its full `Evidence` records (fetched from the `EvidenceStore` by id) against the current resolved entity set — not incrementally per evidence commit, so a relationship's status always reflects everything known about it at publish time, consistent with D4's "one new, immutable, published `GraphVersion` per `run()`" model. Two pre-existing tests' assertions were updated to match the now-real behavior (`tests/test_ingestion_pipeline.py::test_multiple_providers_contradictory_evidence_all_preserved`'s stale comment "No Reconciliation Engine exists yet" replaced with the actual reason its relationship stays `UNRESOLVED` — its fake providers never report entities, so the relationship's endpoints are genuinely unknown, not merely unreconciled; `tests/test_codeql_adapter.py::test_contradictory_evidence_between_git_and_codeql_both_preserved`'s obsolete `UNRESOLVED` assertion replaced with `SUPPORTED`/`WEAKLY_SUPPORTED`, matching what Reconciliation now correctly computes from real Git+CodeQL evidence).

**Tests:** 16 new tests in `tests/test_reconciler.py` (missing-target → `UNRESOLVED` ×3, single-provider support ×2, independent-vs-correlated confidence combination ×4, support+contradiction → `DISPUTED`/`CONTRADICTED`/`UNSUPPORTED` ×3, exact TAD §38 formula verification ×1, provider-failure-never-contradicts ×1, staleness reduces confidence ×1, provenance preservation ×1), plus 2 new `IngestionPipeline` integration tests (real `SUPPORTED` status through the pipeline; `provider_authority` constructor parameter proven to reach the Reconciler). 329 tests total project-wide, 100% coverage on `codex/reconciliation/` and `codex/ingestion/pipeline.py`, clean ruff/mypy.

No new ADR — `provider_authority`'s sourcing gap is resolved the same way ADR-018 already established the pattern for, not a new architectural decision. No cross-document contradiction found in the authoritative documents themselves (only in the directive's own paraphrase, addressed above). D1/D2/D3/D5/D6 untouched; D4's `run()` gained one new step (`_reconcile_relationships`) and its constructor gained one new optional parameter — additive, not a breaking contract change, confirmed by all pre-existing tests passing with at most a comment/assertion update reflecting genuinely new (and correct) behavior, never a workaround.

---

## O. Remaining Gap Inventory + Provider Overlap Re-Audit — 2026-08-31

Per the gap-closure directive's Phases 1-3. Phase 1's dependency reconstruction confirmed a clean, forward-only import graph (`ontology`/`repository` → `evidence` → `provider` → `registry` → `resolution`/`reconciliation` → `ingestion` → `graph`, verified by direct grep of every cross-module import, not assumed) and found **zero** reverse dependencies (`Planner→LLM`, `Provider→Planner`, `Graph→LLM`, `Reconciliation→LLM`, `Verification→Provider`) — none of Query Understanding/Planning/Verification/LLM exist yet, so there is nothing to check beyond confirming no current module imports anything LLM/SLM-shaped (confirmed: zero non-docstring hits).

| Gap | Source | Severity | Blocks | Resolution |
|---|---|---|---|---|
| **A** — SCIP `"."` manager/version placeholder | D5 closure audit §J (DEFERRED) | Low (identity-string cosmetics, not a graph-correctness defect) | Nothing directly; left unfixed it would misrepresent an unset field as a literal manager named `"."` | **CLOSED.** Confirmed against the reference `scip` Rust crate's `bindings/rust/src/symbol.rs` (fetched directly, read only for documented format/parse behavior): `"."` means "field unset," round-trips to `""` in the reference implementation's own test suite. Fixed in `codex.provider.scip.mapping.parse_symbol()` (normalizes `"."` → `""`); 5 new tests; `docs/resources.md`'s "Fourth material finding." |
| **B** — Coverage/Completeness Engine | HLRD/TAD; new directive Gap B | High (negative-query safety is an explicit correctness boundary) | DTD-02+ (Query Understanding/Planning/Verification) would otherwise have no deterministic way to distinguish "nothing found" from "couldn't fully search" | **PARTIALLY CLOSED.** TAD §34's negative-query rule and the six capability/provider distinctions are fully specified and now implemented (`codex.coverage.engine`). TAD §33's `LOW ≥50%/MEDIUM ≥75%/HIGH ≥90%` thresholds are **not** implementable — TAD never defines the coverage *metric* (percentage of what?). Not invented; `CompletenessLevel` enum defined for vocabulary, only `EXHAUSTIVE` has a working check. **OPEN, non-blocking**: the LOW/MEDIUM/HIGH metric definition is a genuine missing formula requiring a human decision, not a STOP on the whole directive (nothing currently depends on graded completeness — only exhaustive/negative-query safety, which is fully implemented). |
| **C** — EvidenceCohort completeness | New directive Gap C | Low (verification only) | Nothing — already correct | **CLOSED.** All required fields already present and correctly populated since D1-D6 (verified by direct inspection, not re-trusted). The four distinctions (not supported/unavailable/failed/empty-success) are now also the Coverage Engine's own test matrix (`tests/test_coverage_engine.py`), which doubles as Gap C's verification. |
| **D** — Graph version consistency | New directive Gap D | Low (mostly N/A — no downstream consumer yet) | Nothing currently — flagged for when Retrieval/Verification exist | **CLOSED for what exists; correctly N/A for what doesn't.** `GraphVersion` immutability-after-publish already tested (D4, `test_graph_version_is_published_and_immutable`); concurrent-update handling is structural (never mutate a returned version) per D4's own documented Phase-1 simplification, not lock-based — unchanged, no defect found. Query-level caching, historical reconstruction consumption, and "verification consumes the same version as retrieval" are all genuinely inapplicable — those components (`NOT_IMPLEMENTED`) don't exist to audit yet; recorded honestly as N/A rather than fabricating a check against code that isn't there. |
| **E** — Evidence independence | New directive Gap E | Low (verification only) | Nothing — already correct | **CLOSED.** `Evidence.effective_independence_group`'s "omitted group defaults to non-independent *within the same provider*" behavior re-confirmed correct against TAD §16's own two-part text (§N, previous phase) and covered by `tests/test_reconciler.py`'s independence tests. No drift found, no new tests needed. |
| **F** — Reconciliation semantics | New directive Gap F | Low (one deliberately-deferred refinement, not a defect) | Nothing blocking — documented, not silently skipped | **CLOSED, with one documented, deliberate non-implementation.** Support weighting, independence-aware combination, contradiction detection/score, freshness, provider authority, confidence, and evidence status are all implemented and tested (§N). "Coverage" (this gap's own word) is **not** folded into the confidence formula — `EvidenceCohort.coverage_status` is tracked but Reconciliation does not discount confidence for `PARTIAL`-cohort evidence, since TAD defines no such discount formula; inventing one would violate Rule 0.2. Explicitly recorded as a deferred refinement, not silently missing. |
| **G** — SourceLocation provider→graph path | New directive Gap G | Low (verification gap, not a code defect) | Nothing — real data already conforms | **CLOSED.** Previously only verified via a handcrafted fixture (`_linked_index()`) at the adapter level. Added `test_real_artifact_source_location_flows_through_to_the_graph` (`tests/test_scip_adapter.py`): decodes the real `typescript_sample.scip` fixture, runs it through the real `IngestionPipeline`, and confirms a real `Greeter` class definition's `SourceLocation` reaches the `GraphStore` with the correct `file_path` convention and passes the model's own validator (would raise `ValidationError` on a malformed real-world range — it doesn't). Git/CodeQL confirmed to still not populate `SourceLocation` at all (unchanged, correctly out of scope). |

### Phase 3 — Provider overlap re-audit

No new provider was added or proposed since the D7 STOP; the capability matrix is unchanged from `docs/architecture-conformance-audit.md`'s §K:

| Capability | Git | SCIP | CodeQL | D7 |
|---|---|---|---|---|
| FILE identity | ✓ | ✓ | ✓ | deferred |
| Symbol definition/reference | | ✓ | | deferred |
| IMPORTS | | ✓ | | deferred |
| IMPLEMENTS | | ✓ | | deferred |
| CO_CHANGE | ✓ | | | deferred |
| DATA_FLOW | | | ✓ | deferred |
| CALLS | | | | deferred |
| EXTENDS | | | | deferred |

**D7 remains "DEFERRED — ARCHITECTURAL DECISION REQUIRED"** (ADR-006 still open). No Sourcegraph/RepoGraph/AGPL code was implemented, wrapped, or copied in this pass. Nothing in Gaps A-G creates pressure to reopen D7 — every gap closed above was resolved using signals Git/SCIP/CodeQL/the existing ontology already provide.

---

## P. D8 — Query Understanding Engine: Traceability Matrix + Pre-Implementation Audit — 2026-08-31

Per the D8 directive's Phases 0-3. Re-read HLRD §24-31 and TAD §22-29 in full directly (not from prior conversation summaries) before writing any code.

### P.1 Traceability matrix (directive Phase 1)

| Requirement | Source | Exact section | Required behavior | Existing implementation dependency | Status |
|---|---|---|---|---|---|
| Query input model | HLRD §24 | HLRD §24 | Progressive routing: Deterministic → SLM → LLM fallback | None (new) | Traced |
| QueryContract | TAD §27, HLRD §29 | TAD §27 (authoritative field list); HLRD §29 (conceptual sketch, consistent) | `intent, targets[], relationship_types[], constraints[], temporal_dimension, complexity, ambiguity, confidence, completeness_requirement, required_evidence[], token_budget, latency_budget` | `codex.coverage.CompletenessLevel` (reused for `completeness_requirement`), `codex.provider.capability.Capability` (reused for `required_evidence[]`), `codex.ontology.relationships.RelationshipType` (reused for `relationship_types[]`) | Traced |
| Intent vocabulary | HLRD §28 (broad categories: `CODE_LOOKUP, CALL_GRAPH, EXECUTION_FLOW, IMPACT_ANALYSIS, ARCHITECTURE, DEPENDENCY, TEST_COVERAGE, HISTORY, DEBUGGING, SECURITY, REFACTORING, CONCEPTUAL`); HLRD §30 (specific operations: `FIND_CALLERS, TRACE_EXECUTION, FIND_IMPLEMENTATIONS, FIND_TESTS, FIND_IMPACT, FIND_DEPENDENCIES, HISTORY_ANALYSIS, ARCHITECTURE_ANALYSIS`); TAD §23 (worked Tier-0 example uses `FIND_CALLERS` directly as an intent) | HLRD §28, §30; TAD §23 | See "Granularity note" below — **documented interpretation, not a contradiction** | None (new) | Traced, interpreted |
| Deterministic Tier-0 detection | TAD §23 | TAD §23 | `match_score ∈ [0,1]`; `>0.95` deterministic, `0.70-0.95` SLM disambiguation, `<0.70` SLM | None (new) | Traced |
| Deterministic match scoring | TAD §23 | TAD §23 | Structural patterns score highest; bare ambiguous keywords must not reach the deterministic band | None (new) | Traced |
| SLM escalation | TAD §22, §24-25 | TAD §22 (DTD-02 flow), §24 (SLM responsibilities), §25 (confidence policy) | SLM "not automatically invoked for every query"; produces intent/targets/relationships/constraints/complexity/ambiguity/temporal/completeness | New `SLMAdapter` Protocol (interface only, no real model — directive Phase 7) | Traced |
| Calibrated confidence | TAD §25 | TAD §25 | `confidence ∈ [0,1]`, "not raw logits"; `>0.85` execute, `0.50-0.85` execute+clarify, `<0.50` escalate to LLM | None (new) | Traced |
| Ambiguity handling | HLRD §27; TAD §26-27 | HLRD §27 (ranked alternatives, evidence-based disambiguation, session context, clarification); TAD §26 (`ambiguity` as a complexity factor); TAD §27 (`ambiguity` as its own QueryContract field) | Ranked candidate list; LLM never invoked merely for a deterministic ambiguity | None (new) | Traced |
| Session context | TAD §28; directive clarifications | TAD §28 | One repository; last 10 queries OR 30 minutes, whichever first; repo change resets; older context lower-weighted | None (new) | Traced |
| Temporal interpretation | TAD §27 (`temporal_dimension` field) | TAD §27 | Present as a QueryContract field and a complexity factor; no enum values given anywhere in HLRD/TAD | None (new) | Traced, interpreted (enum values invented as an **implementation detail** — directive Phase 19 category 1, not an architectural decision: NONE/POINT_IN_TIME/RANGE/HISTORICAL, since no downstream component depends on the exact label set yet) |
| Relationship interpretation | TAD §27 (`relationship_types[]`) | TAD §27 | List of relationship types the query concerns | `codex.ontology.relationships.RelationshipType` (reused, not duplicated) | Traced |
| Completeness requirement | TAD §27, §33 | TAD §27 (field exists); TAD §33 (LOW/MEDIUM/HIGH/EXHAUSTIVE, percentage thresholds) | QueryContract stores **which level is required**; TAD §33's percentage *metric* (what dimension 50%/75%/90% measures) is undefined and **not required to select a level** — see P.2 | `codex.coverage.CompletenessLevel` (reused) | Traced; TAD §33 metric gap confirmed non-blocking (P.2) |
| Reasoning requirement | TAD §26 (complexity factor only) | TAD §26 | `reasoning_requirement` is a complexity **input factor** — TAD §27's QueryContract struct does not list it as a separate top-level field | None (new) | Traced — **not** added as a standalone QueryContract field (would be inventing a field TAD's own struct omits); consumed only inside complexity computation |
| Token budget | TAD §27 | TAD §27 | `token_budget` field, no formula defined yet (formula lives in TAD §41, Planner territory) | None (new) | Traced |
| Latency budget | TAD §27 | TAD §27 | `latency_budget` field, same as above | None (new) | Traced |
| Negative-query semantics | TAD §34 (directive Phase 2 restates it) | TAD §34 | Belongs to the Planner/Coverage boundary (`codex.coverage.evaluate_negative_query_coverage`, already implemented) — Query Understanding's role is limited to correctly classifying a negative-shaped query's `completeness_requirement` as `EXHAUSTIVE` (per TAD §33's own definition), not evaluating coverage itself | `codex.coverage` (already implemented, unmodified) | Traced; correctly out of D8's own execution scope, already built |
| Query constraints | TAD §27 (`constraints[]`) | TAD §27 | List field, no schema given | None (new) | Traced, interpreted as `list[str]` (opaque constraint strings — no structured schema defined anywhere) |
| Security boundaries | Directive Phase 10 (new, not from HLRD/TAD directly — a sound implementation-security requirement consistent with HLRD's evidence-first philosophy) | — | User query text is data, never instructions; malformed contracts rejected | None (new) | Traced |
| Cache behavior | TAD §46 (`Query Understanding Cache`, referenced at TAD line ~1460) | TAD §46 | Not implemented in D8 — no cache exists anywhere in Codex yet (confirmed: `codex.graph`/`codex.evidence` have no cache layer); inventing one now would be scope creep ahead of the Planner/Retrieval layers that actually need it | None | **Correctly deferred**, not implemented |
| Graph-version semantics | Directive Phase 2 ("a query execution must operate against a fixed graph_version") | TAD §27's own `QueryContract` struct has **no** `graph_version` field; graph-version capture is explicitly the Planner's job (`RetrievalPlan`, TAD §29 — confirmed by the original Phase D directive's own "GRAPH VERSION LOCK" instructions: "At the beginning of Query Planning: 1. Capture graph_version. 2. Store it in RetrievalPlan") | Query Understanding never touches the graph (directive Phase 4's explicit constraint) — no graph_version exists for it to capture | None | Traced; **not** added to `QueryContract` (would invent a field neither TAD's struct nor the graph-version-lock instructions assign to this stage) |
| Planner boundary | Directive Phase 4 | — | Query Understanding must not perform retrieval, provider selection, ranking, traversal, LLM synthesis, verification, or mutation | `codex.provider`/`codex.registry`/`codex.graph` (must NOT be imported by the new package) | Traced, enforced by import-boundary tests |
| LLM boundary | HLRD §24, §26-27; TAD §22 | — | LLM is fallback-only, never the repository search planner; never invoked "merely because a deterministic ambiguity exists" | None (new; no LLM Gateway exists yet, correctly out of scope) | Traced |

**Granularity note (intent vocabulary):** HLRD §28 gives a broader classification (`CODE_LOOKUP`, `CALL_GRAPH`, ...) while HLRD §30 and TAD §23's own worked Tier-0 example use a more specific, operation-shaped vocabulary (`FIND_CALLERS`, ...). These are not in conflict — nothing in either document asserts one vocabulary excludes the other — but they describe intent at two different levels of granularity without an explicit mapping between them. Resolved as a **documented interpretation** (directive Phase 19, category 2 — not a STOP-worthy contradiction): `QueryContract.intent` uses the specific, operation-shaped vocabulary, because that is what TAD §23 concretely demonstrates flowing from Tier-0 into a contract, and because HLRD §30 explicitly frames it as what "the planner shall convert user intent into" — i.e., HLRD's own two lists already describe a category→operation relationship, and V1 skips the intermediate coarse category by detecting the specific operation directly wherever a deterministic pattern exists. HLRD §28's broader categories are not implemented as a separate field or enum in V1.

### P.2 Gap/contradiction audit (directive Phase 2-3)

Every specific clarification the directive restates (complexity weights, Tier-0/SLM thresholds, session context window, provider scoring weights, budget-pruning steps, negative-query rule) was checked directly against TAD's text, not assumed correct because a prior directive said so:

- **Complexity weights** (TAD §26): `intent_count 0.25, target_count 0.15, relationship_depth 0.25, ambiguity 0.15, temporal_dimension 0.10, reasoning_requirement 0.10` — **confirmed exact match**, sums to 1.0.
- **Tier-0 routing** (TAD §23): `>0.95` deterministic, `0.70-0.95` SLM disambiguation, `<0.70` SLM — **confirmed exact match**.
- **SLM confidence policy** (TAD §25): `>0.85` execute, `0.50-0.85` execute+clarify, `<0.50` escalate to LLM; "calibrated probability... not raw logits" — **confirmed exact match**.
- **Session context** (TAD §28): one repository; last 10 queries OR 30 minutes, whichever first; repo change resets; older context lower-weighted — **confirmed exact match**.
- **Provider scoring weights** (TAD §31): `capability_match 0.40, evidence_quality 0.20, availability 0.15, freshness 0.15, cost_factor 0.10` — **confirmed exact match against the already-implemented, closed `codex.registry.scoring.PROVIDER_SCORE_WEIGHTS`**; this is D2 territory, not touched or reopened by D8.
- **Budget pruning** (TAD §41-42, referenced by the directive): matches the already-established Planner-phase rules; **not implemented in D8** (D8 produces a `QueryContract` with `token_budget`/`latency_budget` fields populated from the query, but does not itself run pruning — that is explicitly the Query Planner's job, TAD §29, `NOT_IMPLEMENTED` and correctly out of D8's scope).
- **Negative-query rule** (TAD §34): unchanged, already implemented in `codex.coverage` (previous phase); D8 only needs to set `completeness_requirement = EXHAUSTIVE` for negative-shaped queries, not evaluate coverage itself.
- **TAD §33 completeness percentage denominator** (the one previously-flagged open gap): re-confirmed still undefined in the current `docs/TAD.md` (re-read directly, no change since the prior gap-closure pass). **Determined not to block D8**: Query Understanding only needs to select which `CompletenessLevel` a query requires (an enum classification, driven by query phrasing — e.g. a negative/exhaustive-shaped query selects `EXHAUSTIVE`), never to compute or verify an achieved percentage against a metric. Recorded per the directive's own instruction: **"OPEN — DEFERRED TO PLANNER/COVERAGE CALIBRATION."** Not invented, not silently resolved, does not block D8.

No genuine HLRD↔TAD contradiction, no QueryContract field defined differently across documents, no conflict with an already-approved D1-D6/Entity-Resolution/Reconciliation/Coverage contract, and no requirement forcing D7 to be reopened. **No STOP condition triggered.** Proceeding to implementation.

## Q. D8 — Query Understanding Engine: Cross-DTD Audit After Implementation — 2026-08-31

Package: `src/codex/query_understanding/` (`models.py`, `complexity.py`, `tier0.py`, `slm.py`, `session.py`, `engine.py`, `__init__.py`). Tests: `tests/test_qu_tier0.py`, `tests/test_qu_complexity.py`, `tests/test_qu_session.py`, `tests/test_qu_contract.py`, `tests/test_qu_engine.py`, `tests/test_qu_security.py`, `tests/test_qu_boundaries.py`, `tests/fake_slm_adapter.py`. 78 new tests, 100% coverage on all seven new modules. Validated in a fresh venv: `ruff check src tests` clean, `mypy src` clean (45 source files), full suite 435 passed.

### A. Requirements implemented in full

All ~22 items traced in P.1 as "Traced" are implemented: `QueryContract` (exact TAD §27 field list, no invented fields), the deterministic Tier-0 detector (`tier0.detect`, TAD §23 thresholds), the `SLMAdapter` Protocol boundary (TAD §24-25, interface only), session context (TAD §28, one-repository/10-query/30-minute/decay/repo-change-reset), the exact TAD §26 complexity formula and weights, `UnderstandingStatus` (RESOLVED/SLM_UNAVAILABLE/LLM_ESCALATION_REQUIRED) honestly representing the three real outcomes rather than fabricating a contract when no SLM is configured or confidence is below the escalate floor, `required_evidence` derivation per intent, and the security/validation requirements (query text treated strictly as data; malformed contracts rejected by pydantic validators).

### B. Requirements partially implemented

None. Every item traced as "Traced" in P.1 has a corresponding implementation and test; items explicitly out of scope (cache, budget pruning, negative-query coverage evaluation, graph-version capture) are full non-implementations, not partial ones — see C.

### C. Requirements intentionally deferred (not blocking D8)

- **Query Understanding Cache** (TAD §46) — no cache layer exists anywhere in Codex yet; would be scope creep ahead of the components that actually need one.
- **Budget pruning** (TAD §41-42) — Query Planner's responsibility (`NOT_IMPLEMENTED`), not Query Understanding's; D8 only populates `token_budget`/`latency_budget` from defaults.
- **Negative-query coverage evaluation** (TAD §34) — already implemented in `codex.coverage` from a prior phase; D8's only obligation is classifying `completeness_requirement`, which it does.
- **Graph-version capture** — explicitly the Planner's job per the original "GRAPH VERSION LOCK" instructions; Query Understanding never touches the graph, so it has no version to capture. `QueryContract` deliberately has no `graph_version` field (tested structurally in `test_qu_boundaries.py`).
- **TAD §33 completeness-percentage metric** — genuinely undefined in TAD itself (not a Codex gap); does not block D8 since D8 only selects a `CompletenessLevel`, never computes an achieved percentage.

### D. New ambiguities discovered during implementation

None beyond the one already resolved in P.1 (intent-vocabulary granularity, HLRD §28 vs. §30/TAD §23) and the one already flagged in P.2 (TAD §33 metric). Implementation did not surface any new HLRD/TAD gap not already anticipated pre-implementation.

### E. Cross-document contradictions found

None. HLRD and TAD remain mutually consistent on every point checked (P.2); the intent-vocabulary difference is a granularity difference, not a contradiction (P.1 Granularity note).

### F. Silent implementation choices avoided / documented interpretations made

Per directive Phase 19's classification scheme, all non-trivial choices were classified explicitly rather than made silently:
- **Implementation detail** (category 1): `TemporalDimension` enum values (`NONE/POINT_IN_TIME/RANGE/HISTORICAL`) — TAD names the field but not its values; `_REQUIRED_EVIDENCE` intent→Capability mapping table — TAD does not enumerate this mapping, and no downstream component depends on its exact contents yet; `DECAY_HALF_LIFE_QUERIES = 5.0` — a documented calibration point, same pattern as ADR-018's freshness half-life.
- **Documented interpretation** (category 2): intent-vocabulary granularity (P.1); `constraints[]` typed as `list[str]` (no schema given anywhere).
- No category-3 (ADR-required) or category-4 (STOP) decisions were needed for D8.

### G. Security concerns identified and how addressed

Query text is user-controlled and must never be interpreted as instructions. Addressed by construction: Tier-0 only ever produces a bounded enum (`Intent`) plus regex-captured substrings used solely as opaque `targets` strings — it cannot set `token_budget`, `latency_budget`, or `completeness_requirement` from text content (all three are fixed defaults or SLM-derived, never text-derived, and are covered by dedicated injection tests in `test_qu_security.py`). The SLM boundary is a typed Protocol returning a validated `SLMInterpretation` pydantic model — even a compromised/malicious SLM output is bounded by pydantic's field types and value ranges, and query text that impersonates a "system directive" but matches no Tier-0 pattern is inert, proven structurally by `test_injection_attempt_is_treated_as_ordinary_unmatched_text`. `QueryContract`'s own validators reject out-of-range confidence/complexity/ambiguity and non-positive budgets regardless of source.

### H. Dependency-boundary violations found

None. `test_qu_boundaries.py` programmatically parses every module's imports via `ast` and asserts none of `codex.provider.contract`, `codex.provider.git_adapter`, `codex.provider.scip_adapter`, `codex.provider.codeql_adapter`, `codex.provider.scip`, `codex.registry`, `codex.graph`, `codex.ingestion`, `codex.resolution`, or `codex.reconciliation` are imported anywhere in the package. The one deliberate exception, `codex.provider.capability.Capability`, is a stateless vocabulary enum with no provider-selection or extraction behavior, reused the same way `RelationshipType` and `CompletenessLevel` already are — documented inline in the boundary test itself.

### I. Tests proving each architectural invariant

- **No graph access**: `test_no_graph_access_result_never_carries_graph_data`, `test_query_contract_has_no_graph_or_provider_fields`.
- **No provider/registry/graph/ingestion/resolution/reconciliation imports**: `test_no_module_imports_graph_provider_registry_ingestion_resolution_reconciliation` (AST-based, structural, not convention-based).
- **No real model dependency**: `test_no_llm_or_slm_model_dependency_imported`.
- **No LLM answer generation**: `test_no_llm_answer_generation_llm_escalation_never_carries_an_answer`.
- **SLM not invoked for every query**: `test_slm_not_invoked_for_deterministic_query`.
- **Calibrated-probability semantics**: `test_slm_confidence_is_bounded_to_calibrated_probability_range`, confidence-band routing tests.
- **Determinism/repeatability**: `test_deterministic_repeatability_same_input_same_output`, `test_normalization_collapses_whitespace_variants_to_same_result`.
- **Six directive adversarial Tier-0 examples**: all present as named tests in `test_qu_tier0.py`, each asserting non-collapse into the wrong intent.
- **Session semantics** (10-query/30-minute/repo-isolation/decay/stale-context-non-domination): full suite in `test_qu_session.py`.
- **Security/prompt-injection resistance**: full suite in `test_qu_security.py`.

### J. Remaining blockers

None for D8 itself. The only standing, non-blocking open item is the TAD §33 completeness-percentage metric, explicitly deferred to the Planner/Coverage calibration phase (P.2, C above) — not a D8 blocker, and not to be silently resolved when that phase begins.

**D8 — READY FOR REVIEW.** Per the directive's explicit closing instruction, D9 (Planner/Retrieval) work is not started. Stopping after D8 for review.

## R. D9 — Query Planner/Retrieval: Pre-Implementation Audit — 2026-08-31

Read directly for this audit (not assumed from prior reports): `docs/HLRD.md` §30-40, `docs/TAD.md` §19-20, §29-42, §54-56, `docs/architecture-conformance-audit.md` §A-Q, `docs/architecture-reconciliation.md`, `docs/resources.md`, `docs/policy-external-references.md`, `PROGRESS.md`; and inspected directly: `codex.ontology.{entities,relationships}`, `codex.evidence.{model,store}`, `codex.graph.{store,memory_store,version}`, `codex.provider.capability`, `codex.registry.registry`, `codex.ingestion.{models,pipeline}`, `codex.resolution.entity_resolver`, `codex.reconciliation.reconciler`, `codex.coverage.engine`, `codex.query_understanding.models`, `codex.repository.models`.

### R.1 Traceability matrix

| # | Requirement | Source | Exact section | Input | Output | Existing dependency | Status |
|---|---|---|---|---|---|---|---|
| 1 | Query Planner (DTD-03) | TAD §29 | TAD §29 | `QueryContract` | `RetrievalPlan` | `codex.query_understanding.models.QueryContract` | Traced |
| 2 | Planner boundary | TAD §30 | TAD §30 | — | — | Enforced by import-boundary tests (no LLM/SLM) | Traced |
| 3 | Provider selection | TAD §31 | TAD §31 | `Capability`, `RepositoryMetadata` | ranked `ProviderEvaluation[]` | `codex.registry.registry.CapabilityRegistry.rank()` (D2, closed — reused, not duplicated) | Traced |
| 4 | Budget-aware planning | TAD §32 | TAD §32 | `token_budget`, `latency_budget_ms`, initial plan | pruned plan or `PLAN_BLOCKED` | `QueryContract.token_budget`/`.latency_budget_ms` (D8) | Traced; two calibration constants missing (R.2) |
| 5 | Completeness model | TAD §33 | TAD §33 | `completeness_requirement` | satisfied/not | `CompletenessLevel` (`codex.coverage`, D-gap-closure) | Traced; percentage denominator still undefined (R.2, non-blocking) |
| 6 | Negative query planning | TAD §34 | TAD §34 | required capability, `IngestionResult` | `NO_EVIDENCE_FOUND` / `INCONCLUSIVE` | `codex.coverage.engine.evaluate_negative_query_coverage()` (already implemented — consumed, not duplicated) | Traced |
| 7 | Retrieval Engine | TAD §35 | TAD §35 | `RetrievalPlan` | entities/relationships/evidence | `codex.graph.store.GraphReader`, `codex.evidence.store.EvidenceStore` | Traced; requires one additive `GraphReader` method (R.4) |
| 8 | Ranking Engine — signals | TAD §36 | TAD §36 | candidate entity/edge, `QueryContract` | 4 normalized `[0,1]` signals | `codex.ontology.relationships.RelationshipType`, graph traversal | Traced, verbatim |
| 9 | Ranking Engine — formula | TAD §37 | TAD §37 | 4 signals | weighted score | — | Traced; weights are undefined "calibration parameters" (R.2, non-blocking) |
| 10 | MSS | TAD §39 | TAD §39 | ranked evidence, completeness requirement | minimal sufficient subgraph | — | Traced |
| 11 | MSS Expansion | TAD §40 | TAD §40 | MSS, `SOURCE_CONTEXT` need | expanded MSS or `PARTIAL` | `RepositorySymbol.source_location`, `.lifecycle_status` | Traced, exact bounds (2 cycles / 50 nodes) |
| 12 | Dynamic Evidence Budget | TAD §41 | TAD §41 | `token_budget` | `max_nodes`/`max_edges` | — | Traced; two calibration constants missing (R.2) |
| 13 | Evidence Package | TAD §42 | TAD §42 | MSS + coverage + limitations | `EvidencePackage` | `GraphVersion`, `QueryContract` | Traced |
| 14 | Cache architecture | TAD §54 | TAD §54 | repository, graph_version, schema_version, policy_version, query identity | cache key | `GraphVersion` (already carries all four fields) | Traced |
| 15 | Query-level cache | TAD §55 | TAD §55 | fixed graph_version | locked execution, `CONCURRENT_UPDATE_DETECTED` telemetry | `GraphVersion.published` (immutable) | Traced |
| 16 | Graph Version Lock | TAD §19-20 | TAD §19-20 | `GraphReader` at plan time | `RetrievalPlan.graph_version` | `codex.graph.version.GraphVersion`, `codex.graph.store.GraphReader` | Traced |
| 17 | Planner→LLM boundary | Directive Part 15 | TAD §30, §1 invariants | — | — | No LLM/Verification module exists yet (nothing to import) | Traced |
| 18 | Candidate generation (graph search) | HLRD §33 | HLRD §33 | `QueryContract.targets` | candidate entities | None — `GraphReader` has no name-lookup method today (R.4) | Traced; additive gap, resolved not blocking |
| 19 | Retrieval policy (intent-specific modes) | HLRD §32 | HLRD §32 | `Intent` | traversal/lookup mode | `codex.query_understanding.models.Intent` | Traced (V1 implements graph traversal + entity lookup only — lexical/semantic/runtime retrieval explicitly out of scope, no embeddings per HLRD §34) |
| 20 | Evidence-aware ranking (HLRD framing) | HLRD §35 | HLRD §35 | — | — | Same as #8-9; HLRD's longer signal list (redundancy, provider agreement, runtime relevance, ...) is the superset TAD §36 narrows to V1's 4 deterministic proxies | Traced, interpreted (see Granularity note) |
| 21 | Minimum Sufficient Subgraph (HLRD framing) | HLRD §36 | HLRD §36 | — | — | Same as #10; "SHALL NOT remove evidence required to satisfy completeness merely because it has a lower relevance score" — enforced by MSS pruning order in `codex.planner.budget` | Traced |
| 22 | Retrieval stopping condition | HLRD §39 | HLRD §39 | coverage, marginal expected value, provider exhaustion | stop/continue | `codex.coverage.engine.classify_capability_coverage()` | Traced |
| 23 | Derived relationships at query time | TAD §14 | TAD §14 | `REACHES`/`TRANSITIVE_CALLS`/`INDIRECTLY_DEPENDS_ON` | computed edges, never persisted | `codex.ontology.relationships.DERIVED_RELATIONSHIP_TYPES` (already exists) | Traced |
| 24 | Planner telemetry | Directive Part 4 item 5, Part 7 | TAD §32 ("record: original estimate, pruned estimate, whether pruning occurred, reason"), §55 (`CONCURRENT_UPDATE_DETECTED`) | plan execution | `PlanTelemetry` | None (new) | Traced |

**Granularity note (ranking signals):** HLRD §35 lists a broader, aspirational signal set (`Semantic relevance, Structural relevance, Relationship relevance, Evidence quality, Intent relevance, Path relevance, Runtime relevance, Temporal relevance, Provider agreement, Redundancy`) with a `w1...wn` open sum; TAD §36-37 narrows this to the four V1 deterministic proxies with a closed 4-term formula. Same pattern as D8's intent-vocabulary granularity note: not a contradiction (TAD is explicitly the *technical architecture* narrowing HLRD's conceptual list to what V1 actually computes), so `codex.planner.ranking` implements exactly TAD §36-37's four signals and no others.

### R.2 Gap/contradiction audit (directive Parts 3, 6-8, 13)

Checked directly against the current `docs/TAD.md`/`docs/HLRD.md` text, not assumed:

- **QueryContract omissions (Part 3):** re-confirmed `reasoning_requirement` appears exactly once in TAD (§26, as a complexity *input* weight) and nowhere else; no later TAD section (§29-42) reads it as a standalone Planner input. `graph_version` is **not** sourced from `QueryContract` at all — TAD §20 explicitly assigns its capture to the Planner itself, fresh, at the start of DTD-03 ("At the beginning of DTD-03: graph_version = V182 is locked into RetrievalPlan.graph_version"), independent of anything Query Understanding produced. **Conclusion: category A (already correctly derivable/obtainable without touching QueryContract) — no field needs to be added to QueryContract, D8 is not reopened, no STOP.**
- **Ranking signals (Part 6):** all four V1 proxies are present in TAD §36-37, verbatim: `semantic_relevance` = BM25 over qualified names/symbols/paths/extracted query entities, normalized `[0,1]`; `structural_relevance` = `1.0` on primary-relationship match else `0.3`; `graph_proximity` = `0.9^d`; `query_constraint_match` = Jaccard similarity over paths/tags/roles/constraints (TAD's set is slightly broader than the directive's own paraphrase, which omitted "roles" — implemented per TAD's actual, broader definition). **No invention needed for the signals themselves.** The aggregation weights (`w1..w4`) are explicitly "calibration parameters" (TAD §37) with no V1 default given anywhere — this is the same class of gap as ADR-018's freshness half-life and D8's session-decay half-life: **resolved as a documented calibration point** (`RANKING_WEIGHTS = 0.25` each, equal weighting, explicitly labeled non-final), not a STOP, since the formula *shape* is fully specified and only the scalar weights are missing.
- **Budget cost estimation (Part 7):** TAD §41 gives the token-budget formula explicitly (`max_nodes = min(100, token_budget/average_node_cost)`, `max_edges = min(250, token_budget/average_edge_cost)`) but never defines `average_node_cost`/`average_edge_cost` numerically — again a formula-given/constants-missing gap, resolved as a documented calibration point. **Latency budget has no analogous formula anywhere in TAD** (§32 only describes the five-step pruning *procedure*, never how a plan's latency cost is estimated) — the only defensible non-inventive option that still lets `latency_budget_ms` function as an independent, testable budget (TAD §32 explicitly lists it as a co-equal Planner input) is to extend the *identical* average-cost-per-unit pattern TAD already specifies for tokens, with its own separately labeled calibration constants (`DEFAULT_AVERAGE_NODE_LATENCY_MS`, `DEFAULT_AVERAGE_EDGE_LATENCY_MS`). This is the single most judgment-laden call in this audit — flagged explicitly rather than silently applied; it is structural reuse of TAD's own specified pattern, not an unrelated invented cost model (e.g. no real wall-clock profiling or ML cost prediction), and remains fully swappable. **Not a STOP** under this reasoning, but recorded with maximum visibility for review.
- **Completeness percentage denominator (Part 8):** re-checked `docs/TAD.md` §33 directly — still gives only the four named thresholds (`LOW ≥50%, MEDIUM ≥75%, HIGH ≥90%, EXHAUSTIVE 100%+complete repository coverage`) with **no definition of what the percentage measures**. Re-confirmed genuinely open, not silently resolved. **Determined not to require the numeric value for D9**: `is_exhaustive_coverage()` (already implemented, `codex.coverage`) fully and deterministically satisfies the `EXHAUSTIVE` level without any percentage (it reduces to the same AND-gate as negative-query safety). For `LOW`/`MEDIUM`/`HIGH`, TAD §32's own pruning rule ("Exhaustive queries cannot be pruned below required coverage") implies these three levels are the ones *eligible* for budget pruning — D9 only needs to know *which* level was requested (already available as an enum) to decide whether pruning is permitted at all, never to compute a numeric satisfaction percentage. Retrieval's stop condition for all four levels uses HLRD §39's qualitative rule (stop when coverage requirement satisfied, or no further expected value, or providers exhausted) implemented via `codex.coverage.classify_capability_coverage()`'s deterministic AND-gate, not a percentage. **Recorded per the directive's own instruction: "OPEN — DEFERRED TO CALIBRATION," not blocking D9, not falsely marked closed.**
- **Cache scope (Part 13):** TAD §54-55 is what actually defines caching (the directive's own "TAD §46" citation names the Verification Engine section instead — the same kind of directive/document mismatch flagged repeatedly in this project's history, e.g. D8's intent-vocabulary and D7's taxonomy paraphrase; resolved as a documented interpretation, not a contradiction, since TAD §54-55 is unambiguous and internally consistent). §54's four cache-key dimensions (repository, graph_version, schema_version, policy_version) are already all present on the existing `GraphVersion` model plus `repository_id` — no new field needed. §55's "semantic contracts do not automatically invalidate merely because an unrelated file changed" principle is **not implemented as fine-grained partial invalidation in V1** — TAD defines no versioning granularity finer than the whole `GraphVersion`, so a sub-`GraphVersion` invalidation scheme would be an invented mechanism; V1's cache is correct-by-construction (a stale entry can never be returned because its key differs) at the `GraphVersion` granularity only, honestly documented as a V1 limitation rather than claimed as solved.

No genuine HLRD↔TAD contradiction found. No QueryContract field conflict. No requirement forcing D7/D8 to be reopened.

### R.3 Graph version lock verification (Part 4)

Direct inspection of `codex.graph.version.GraphVersion` and `codex.graph.{store,memory_store}` confirms the lock is safe **by construction**, requiring no new concurrency primitive: `GraphVersion` is immutable once `.published` (already enforced, `.publish()` returns a copy); `InMemoryGraphStore.__init__(self, version: GraphVersion)` binds exactly one immutable version to exactly one store instance for its entire lifetime — there is no method anywhere on `GraphReader`/`GraphStore` that could change `.version` after construction, and `IngestionPipeline.run()` (D4, unmodified) always materializes a **new** `InMemoryGraphStore` instance per call rather than mutating a previously returned one (already proven by D4's own tests: "a previously returned result is never mutated, including by a later failing run"). Therefore: **as long as the Planner and the Retrieval Engine share the exact same `GraphReader` object reference for the lifetime of one query, "silently switching versions" is structurally impossible** — there is no second version for it to switch to. No STOP; no invented locking/concurrency semantics needed.

One real, load-bearing gap found and resolved (not a STOP): **there is no "current/latest GraphReader for repository X" registry anywhere in the codebase** — `IngestionPipeline.run()` returns a fresh `IngestionResult`/`GraphStore` to its immediate caller and nothing persists a "latest version" mapping. TAD's own 18-component list has no "Graph Version Registry" component either — obtaining "the current graph for this query's repository" is an orchestration-layer concern that sits **above** the Planner (and above D9's scope), consistent with how every other cross-cutting dependency in this codebase is supplied (D1's `ProviderAdapter`, D2's `CapabilityRegistry` — all constructor/call-parameter injected, nothing globally looked up). **Resolved as an implementation detail (category 1): `plan_query()`'s public signature takes an already-selected `GraphReader` and the `IngestionResult` that produced it as explicit parameters** — the Planner's job per TAD §20 is only to *lock* (capture `.version` from) whatever reader it is handed, not to search for one. `CONCURRENT_UPDATE_DETECTED` telemetry (TAD §55) is recorded when a caller later re-invokes retrieval against a *different* `GraphReader.version.version_id` for the same in-flight `RetrievalPlan` — detectable structurally by comparing the locked `version_id` against whatever reader retrieval is actually asked to run against, with no external locking system required.

### R.4 Provider selection boundary (Part 5)

`codex.registry.registry.CapabilityRegistry.rank(capability, repository, *, now=None)` (D2, ADR-018-closed) already implements exactly the query-time ranking behavior TAD §31 specifies — ties broken deterministically by `provider_name`, `capability_match=0` exclusion built in via `providers_for()`. **D9 calls `rank()`/`providers_for()` directly and does not reimplement any scoring.** The ingestion-time/query-time distinction is preserved unmodified: `IngestionPipeline` (D4, untouched) still runs every usable provider so contradictory evidence reaches the graph; D9's provider selection only decides which already-ingested provider's evidence to *prioritize when ranking retrieved evidence*, never which provider gets to write to the graph — those are genuinely separate operations on separate objects (`CapabilityRegistry.evaluate()`-driven ingestion vs. `CapabilityRegistry.rank()`-driven retrieval-time preference), not collapsed.

### R.5 Retrieval execution (Part 11) — the one additive gap

Direct inspection of `codex.graph.store.GraphReader` confirms it exposes only `get_entity(canonical_id)`, `get_relationships(...)`, and `neighbors(canonical_id, ...)` — **no method exists to look up an entity by name/qualified_name, and no method enumerates all entities**. This blocks the single most basic retrieval operation TAD §35 itself lists ("entity lookup") and HLRD §33 requires ("Candidate Generation... Graph" as a source): turning `QueryContract.targets: list[str]` (free-text strings like `"authenticate"`) into starting graph entities is otherwise impossible through the existing interface. No prior D1-D8 component ever needed this (D1-D4 only *write*; D5-D8 never read the graph at all), so this was never previously discovered.

**Resolved as an additive, non-breaking extension, not a STOP and not a modification of existing D1-D8 behavior:** one new read-only method, `find_entities(*, name=None, qualified_name=None, base_type=None) -> list[RepositorySymbol]`, added to the `GraphReader` Protocol and implemented in `InMemoryGraphStore` (exact/substring string matching only — no embeddings, no fuzzy matching, fully deterministic, consistent with HLRD §34's "embeddings are optional... SHALL NOT become prerequisites"). `InMemoryGraphStore` is the only concrete implementer anywhere in the codebase (confirmed by direct search), so this is purely additive — no existing caller's behavior changes, no existing test needs updating. Documented here with maximum visibility per the "no silent architectural decisions" rule, exactly like D4's evidence-discard-bug fix and D8's `required_evidence` mapping-table addition were.

### R.6 STOP-condition disposition (Part 16)

All twelve enumerated STOP conditions checked explicitly:

1. HLRD/TAD contradiction — none found (R.2).
2. QueryContract D8/D9 conflict — none; resolved without reopening D8 (R.2).
3. GraphVersion cannot be locked consistently — resolved structurally, no invented concurrency (R.3).
4. RetrievalPlan requires unspecified fields — avoided; every `RetrievalPlan` field traces to R.1's matrix or TAD §20/29-32/34 directly (see R.7).
5. Ranking formulas undefined and required — signals are fully specified; only weights are calibration points (R.2), not blocking.
6. Budget estimation requires unspecified algorithm — token-budget formula is specified (constants missing, calibration point); latency-budget formula is extended by direct, labeled analogy to the token pattern (R.2) — the one judgment call flagged with maximum visibility, not silently resolved.
7. Completeness percentages required but undefined — re-confirmed undefined, determined non-blocking for D9 (R.2).
8. Negative-query safety unprovable via Coverage Engine — false; fully provable given an `IngestionResult` reference, supplied the same way `GraphReader` is (R.3 pattern).
9. D9 would duplicate D2 provider selection — avoided by direct reuse (R.4).
10. D9 requires LLM/SLM dependency — false; purely deterministic.
11. External implementation/code would need to be copied — false; no new external format/protocol involved in D9.
12. New ontology relationship required but unspecified — false; `DERIVED_RELATIONSHIP_TYPES` (`REACHES`/`TRANSITIVE_CALLS`/`INDIRECTLY_DEPENDS_ON`) already exists and exactly covers TRACE_EXECUTION/FIND_IMPACT's needs (TAD §14, already implemented as a vocabulary, computed at query time by D9's Retrieval Engine).

**No STOP condition triggered. Proceeding to D9 implementation (Parts 17-20).**

### R.7 RetrievalPlan schema (Part 10)

Every field below traces to R.1; no speculative field added:

| Field | Traces to |
|---|---|
| `query_identity` | Implementation detail — a stable key for telemetry/cache correlation; no TAD field name given, so a plain `str` (caller-supplied or derived) |
| `graph_version` | TAD §19-20 |
| `target_entities` | Resolved from `QueryContract.targets` via `find_entities()` (R.5) |
| `relationship_types` | `QueryContract.relationship_types` (TAD §27, D8) |
| `traversal_depth` | Derived from `QueryContract.complexity`/`intent` — TAD §29 lists "traversal depth" as a Planner output without a formula; implemented as a documented, bounded default (depth 1 for lookup-shaped intents, up to a small constant ceiling for traversal-shaped intents), not unbounded |
| `provider_requirements` | `QueryContract.required_evidence` (D8) resolved via `CapabilityRegistry.rank()`/`providers_for()` (R.4) |
| `completeness_requirement` | `QueryContract.completeness_requirement` (D8, `CompletenessLevel`) |
| `budget` | `QueryContract.token_budget`/`.latency_budget_ms` (D8) plus TAD §41's derived `max_nodes`/`max_edges` |
| `ranking_strategy` | Fixed V1 constant (TAD §36-37's four-signal formula — not user/query-selectable, TAD gives no selection mechanism) |
| `stop_conditions` | HLRD §39's three-way rule, implemented via `codex.coverage` |
| `negative_query_flag` | Derived from `Intent`/query phrasing already classified by Query Understanding (TAD §34) |
| `status` | `OK` / `PRUNED` / `PLAN_BLOCKED` / `PLAN_UNSUPPORTED` (TAD §32, §41's exact vocabulary) |
| `telemetry` | `PlanTelemetry` — graph_version, budget trace (original/pruned estimate, pruning occurred, reason — TAD §32's exact required fields), `CONCURRENT_UPDATE_DETECTED` flag (TAD §55) |

**Decision: proceed to implementation.**

## S. D9 — Query Planner/Retrieval: Cross-DTD Audit After Implementation — 2026-08-31

Package: `src/codex/planner/` (`models.py`, `provider_selection.py`, `budget.py`, `retrieval.py`, `ranking.py`, `cache.py`, `mss.py`, `planner.py`, `__init__.py`). Additive extension: `codex.graph.store.GraphReader.find_entities()` / `codex.graph.memory_store.InMemoryGraphStore.find_entities()` (§R.5). Tests: `tests/test_planner_{planning,graph_version,provider_selection,budget,completeness,negative_queries,ranking,mss,cache,boundaries,retrieval}.py`, `tests/planner_fixtures.py`, plus 6 new `find_entities` tests appended to `tests/test_graph_store.py`. 92 new planner tests + 6 new graph-store tests = 98 new tests. 100% coverage on all nine new `codex.planner` modules; `codex.graph.{store,memory_store}` unaffected in behavior (additive only, all pre-existing tests pass unmodified). Validated in a fresh venv: `ruff check src tests` clean, `mypy src` clean (54 source files), full suite 522 passed.

### A. Requirements implemented in full

`plan_query()` (DTD-03, TAD §29): graph-version lock (TAD §20), provider selection via unmodified D2 `CapabilityRegistry.rank()` (TAD §31), budget-aware five-step pruning (TAD §32) with `PLAN_BLOCKED`/`PLAN_UNSUPPORTED` distinguished per TAD §32/§41, negative-query safety via unmodified `codex.coverage` (TAD §34), completeness-level propagation (TAD §33). `execute_query()` (DTD-04, TAD §35): deterministic bounded traversal, TAD §36-37's four ranking signals with exact literal constants (`1.0`/`0.3`, `0.9^d`), TAD §39-40's MSS construction and bounded SOURCE_CONTEXT expansion (2 cycles/50 nodes, exact), TAD §42's `EvidencePackage`. TAD §54-55's query-level cache, keyed exactly on repository/graph_version/schema_version/policy_version/query-identity. `GraphVersionMismatchError` proves the graph-version lock structurally (§R.3's "holding the reference is the lock" claim is now enforced, not just asserted).

### B. Requirements partially implemented

None. Every R.1-traced item has a corresponding implementation and dedicated test category; scope exclusions (C below) are complete non-implementations honestly deferred, not partial stubs.

### C. Requirements intentionally deferred (not blocking D9)

- **TAD §33 LOW/MEDIUM/HIGH completeness percentage** — the denominator remains genuinely undefined in TAD; D9 propagates the requested `CompletenessLevel` and refuses to prune `EXHAUSTIVE` below required coverage (the one level with a full quantitative check, `codex.coverage.is_exhaustive_coverage`), without computing a numeric percentage for the other three, per R.2's reasoning.
- **Fine-grained (sub-`GraphVersion`) cache invalidation** — TAD defines no versioning granularity finer than the whole `GraphVersion`; V1's cache correctness is by construction at that granularity only (`cache.py`'s own docstring).
- **Actual source-text retrieval for `EvidencePackage.source_context`** — carries `SourceLocation`-bearing `RepositorySymbol`s, not fetched file content; no D1-D8 provider exposes arbitrary file-content-at-revision reads.
- **A "Graph Version Registry" / "current version for repository X" lookup** — deliberately not built; obtaining the `GraphReader` to plan against is an orchestration-layer concern above D9, matching how every other cross-cutting dependency in this codebase is supplied (explicit injection, never global lookup).

### D. New ambiguities discovered during implementation

None beyond the two already flagged pre-implementation (latency-budget cost-model extension by analogy, R.2; ranking-weight calibration point, R.2). No new HLRD/TAD gap surfaced only during coding.

### E. Cross-document contradictions found

None. One directive/document citation mismatch confirmed and resolved as a documented interpretation, not a contradiction: the directive's "TAD §46" for cache architecture actually names the Verification Engine section; the real cache sections are TAD §54-55 (R.2) — the same class of paraphrase mismatch flagged repeatedly across this project's history (D7's taxonomy, D8's intent vocabulary).

### F. Silent implementation choices avoided / documented interpretations made

Per directive Phase 19's classification: **implementation detail** (category 1) — `_BASE_DEPTH_BY_INTENT` (TAD §29 names "traversal depth" as a Planner output with no formula), `DEFAULT_AVERAGE_NODE_COST_TOKENS`/`DEFAULT_AVERAGE_EDGE_COST_TOKENS` (TAD §41 gives the formula, not the constants — ADR-018 precedent), `DEFAULT_LATENCY_MS_PER_DEPTH_LEVEL` (no TAD formula at all for latency; extended from the token pattern by direct, labeled analogy — the single most judgment-laden call in D9, R.2/R.6), `RANKING_WEIGHTS` equal 0.25 each (TAD §37: "weights are calibration parameters," no default given), `query_identity` field (no TAD name given; a deterministic content hash). **Documented interpretation** (category 2) — `query_constraint_match`'s vacuous-case rule (no constraints requested = trivially satisfied, `1.0`); `RetrievalPlan.constraints` added as a field beyond R.7's original checklist, needed to make `query_constraint_match` meaningfully exercised rather than trivially `1.0` on every real query (traces cleanly to `QueryContract.constraints`, TAD §27, flowing into TAD §36's own signal — not a speculative addition, a necessary wiring of two already-approved contracts). No category-3 (ADR-required) or category-4 (STOP) decisions were needed.

### G. Security concerns identified and how addressed

`QueryContract` is treated as trusted, already-validated structured input (D8's own boundary) — the planner never re-parses query text; `plan_query`/`execute_query` derive every decision from typed `QueryContract`/`RetrievalPlan` fields only. `resolve_targets`/`find_entities` perform deterministic substring matching with no code execution, no embeddings, no external calls. `execute_query` refuses (`GraphVersionMismatchError`) rather than silently executing against a mismatched graph snapshot — a concurrency-safety property, not just a functional one.

### H. Dependency-boundary findings

None. `tests/test_planner_boundaries.py` programmatically parses every module's imports via `ast` and asserts none of `codex.llm`, `codex.slm`, `codex.verification`, `codex.query_understanding.slm`, or the behavioral provider submodules (`codex.provider.contract`/`.git_adapter`/`.scip_adapter`/`.codeql_adapter`/`.scip`) are imported anywhere in `codex.planner`. A regex check confirms no hard-coded provider-name branching exists. A structural check confirms `select_providers()` is a thin pass-through to `CapabilityRegistry.rank()` with no local scoring function. `RetrievalPlan` carries no `answer`/`verification_status`/`claims`/`llm_response`-shaped field.

### I. Tests proving each architectural invariant

- **Graph-version lock**: `test_graph_version_is_captured_into_the_plan`, `test_concurrent_graph_update_does_not_change_an_in_flight_plan`, `test_execute_query_against_a_different_version_raises_concurrent_update`.
- **No provider-selection duplication**: `test_select_providers_reuses_registry_rank_not_a_new_algorithm`, `test_ingestion_time_runs_every_usable_provider_query_time_only_ranks`.
- **Budget pruning order + EXHAUSTIVE refusal**: full suite in `test_planner_budget.py`, including both blocking paths (latency, token).
- **Negative-query safety via `codex.coverage`, never bypassed**: full suite in `test_planner_negative_queries.py`, including a hand-crafted `PARTIAL` cohort and a stale-but-COMPLETE cohort proving staleness is not conflated with completeness.
- **Ranking determinism/tie-breaking/each signal**: full suite in `test_planner_ranking.py`.
- **MSS bounds (2 cycles/50 nodes) and PARTIAL reporting**: full suite in `test_planner_mss.py`, including a frontier-entity-skipped-once-cycle-is-full test.
- **Cache key isolation on all four TAD §54 dimensions**: full suite in `test_planner_cache.py`.
- **No LLM/SLM/verification/provider-specific dependency**: `test_planner_boundaries.py`.

### J. Remaining blockers

None for D9 itself. Standing, non-blocking open items carried forward unchanged: TAD §33's completeness-percentage metric (deferred to Planner/Coverage calibration, as it already was after D8); the latency-budget cost-model analogy (R.2/R.6) remains the one item most worth a human calibration pass if/when real latency data becomes available, but does not block D9's correctness today.

**D9 — READY FOR REVIEW.** Per the directive's explicit closing instruction, D10 (LLM Gateway/Answer Generation/Verification Engine) work is not started. Stopping after D9 for review.

## T. D9 Closure Audit + D10 Pre-Implementation Architecture Audit — 2026-08-31

Audit-only per explicit instruction: no production code was written or modified in this phase. Read directly (not assumed from prior reports): `docs/HLRD.md` §26, §41-46; `docs/TAD.md` §19-20, §29-55; `docs/architecture-reconciliation.md`; `docs/architecture-conformance-audit.md` §A-S; `PROGRESS.md`; and inspected directly: `src/codex/planner/*`, `src/codex/ingestion/pipeline.py` (step ordering), `src/codex/reconciliation/reconciler.py`, `src/codex/evidence/model.py`, `src/codex/graph/version.py`.

### T.1 Phase A — D9 Closure Audit (15 items)

| # | Item | Status | Evidence |
|---|---|---|---|
| 1 | QueryContract → RetrievalPlan → EvidencePackage traceable | IMPLEMENTED | `plan_query()`/`execute_query()` (`planner.py`); every `RetrievalPlan`/`EvidencePackage` field traced in §R.7/§S.F |
| 2 | graph_version immutable, consistent planning→retrieval | IMPLEMENTED | `GraphVersion.publish()` is copy-on-write (§R.3); `execute_query` raises `GraphVersionMismatchError` on any `graph.version.version_id` mismatch against `plan.graph_version.version_id` — verified structurally, not just asserted |
| 3 | Provider selection delegated to D2, no duplicate ranking | IMPLEMENTED | `provider_selection.select_providers()` calls `registry.rank()`/`.providers_for()` only; `test_provider_selection_module_only_calls_registry_rank_or_providers_for` proves no local `rank()`/`score()` function exists in the module |
| 4 | Ranking follows canonical V1 deterministic proxies exactly | IMPLEMENTED | `ranking.py`'s `bm25_scores`/`structural_relevance`/`graph_proximity`/`query_constraint_match` match TAD §36's literal constants (`1.0`/`0.3`, `0.9^d`) verbatim; re-verified against the current `docs/TAD.md` text in this audit, not assumed |
| 5 | Budget pruning cannot silently violate EXHAUSTIVE completeness | IMPLEMENTED | `plan_query()` checks `is_exhaustive` before every pruning step (depth reduction, relationship-type removal) and returns `PLAN_BLOCKED` instead — `test_exhaustive_query_is_blocked_not_silently_pruned_on_latency`/`_on_token_truncation` |
| 6 | Negative-query planning delegates to Coverage Engine | IMPLEMENTED | `plan_query()` calls `codex.coverage.engine.evaluate_negative_query_coverage()` directly; no local re-implementation |
| 7 | MSS expansion remains bounded | IMPLEMENTED | `mss.py`'s `MAX_EXPANSION_CYCLES=2`, `MAX_NODES_PER_CYCLE=50` — TAD §40's exact literal bounds, tested including the frontier-entity-skipped-once-full case |
| 8 | Evidence budgets derived from token budget, not arbitrary independent limits | IMPLEMENTED, with one clarification | `max_nodes`/`max_edges` (TAD §41's own "Dynamic Evidence Budget") are derived **purely** from `token_budget` via `token_derived_ceiling()`, exactly matching TAD §41's formula shape. `traversal_depth`'s separate latency-derived ceiling (`latency_derived_depth_ceiling()`) is a *different* knob — it bounds traversal depth, not evidence volume — and is TAD's own named co-equal budget input (§32) with no formula given at all, resolved as a calibration-point extension (§R.2/§R.6, flagged with maximum visibility, not silent). Re-confirmed: it is not an "arbitrary independent" evidence-volume limit; it does not compete with or override the token-derived ceiling. |
| 9 | Retrieval cache keys include every required invalidation dimension | IMPLEMENTED | `cache_key_for()` includes `repository_id`, `graph_version_id`, `schema_version`, `policy_version` (TAD §54's exact four) plus `query_identity` (a deterministic `QueryContract` content hash) |
| 10 | No planner/retrieval transitive dependency on LLM/SLM/verification | IMPLEMENTED | AST-verified (`test_planner_boundaries.py`); re-confirmed structurally impossible today since `codex.llm`/`codex.slm`/`codex.verification` do not exist anywhere in the codebase yet — there is nothing to transitively depend on |
| 11 | EvidencePackage contains sufficient provenance for downstream verification | **PARTIAL** | `EvidencePackage.relationships` carries full `CanonicalRelationship` objects (`.status`, `.confidence`, `.contradiction_score`, `.supporting_evidence_ids`, `.contradicting_evidence_ids` all present, TAD §51's traceability chain intact). **Gap found**: `EvidencePackage.evidence` (`collect_evidence()` in `retrieval.py`) resolves only `supporting_evidence_ids` — `contradicting_evidence_ids` are never resolved into actual `Evidence` records inside the package. A future Verification/Answer stage wanting to *explain* a `DISPUTED`/`CONTRADICTED` claim (HLRD §43: "explain conflict") would need the contradicting `Evidence` objects themselves, not just the `contradiction_score` float and the raw id strings already present in `CanonicalRelationship.contradicting_evidence_ids`. **Not blocking**: the ids are already present in the package (nothing is lost), and D10 can resolve them itself given the same `EvidenceStore` reference (matching D9's own explicit-injection precedent, §R.3) — the same pattern already used throughout D1-D9. Recommended, non-blocking fix: extend `collect_evidence()`/`execute_query()` to also resolve `contradicting_evidence_ids` into `EvidencePackage.evidence` before D10.2 needs it, so the package is self-sufficient per TAD §42's own framing ("the LLM's repository context boundary"). |
| 12 | Evidence independence groups survive complete D1→D9 pipeline | IMPLEMENTED | `Evidence` objects are never reconstructed by D9 — `collect_evidence()` returns the exact objects `EvidenceStore.get_evidence()` returns, so `.independence_group`/`.effective_independence_group` (TAD §16) survive unmodified, matching D4's "provenance preservation" invariant |
| 13 | Entity resolution and reconciliation semantics preserved | IMPLEMENTED | Re-verified directly: `IngestionPipeline.run()` calls `self._reconcile_relationships(...)` (line 218) **before** `_materialize_store(...)` (line 220) which produces the `graph_store` D9 reads — D9 structurally cannot see pre-resolution/pre-reconciliation state, it only ever sees the fully-resolved, fully-reconciled graph |
| 14 | GraphVersion and provider-version provenance survive into EvidencePackage | IMPLEMENTED | `EvidencePackage.graph_version: GraphVersion` carries the full object including `provider_versions: dict[str, str]` (TAD §19) unmodified; each individual `Evidence` record also independently carries `provider`/`provider_version` |
| 15 | No D9 implementation silently introduced semantics absent from HLRD/TAD | IMPLEMENTED | Every non-trivial choice already classified in §S.F (implementation detail vs. documented interpretation); re-confirmed no category-3/4 (ADR-required/STOP) decision was silently resolved |

**D9 closure disposition:** 14/15 IMPLEMENTED, 1/15 PARTIAL (item 11), **not blocking D10** — reasoning given above. No item is MISSING, CONTRADICTED, or AMBIGUOUS.

### T.2 Phase B — D10 Contract Reconstruction

Reconstructed directly from `docs/TAD.md` §43-52 and `docs/HLRD.md` §41-43 (re-read in full for this audit, not assumed):

| Responsibility | Authoritative source | Requirement |
|---|---|---|
| LLM boundary | HLRD §41 ("non-negotiable... LLM SHALL NOT be treated as an authoritative source of repository truth"); TAD §30 (Planner boundary, analogous), TAD §43 | LLM receives `Query + EvidencePackage + ResponseContract`; "does not receive unrestricted repository access" |
| Answer generation | TAD §43-44 | One structured LLM generation pass; JSON output `{answer, claims[]}` |
| Claim extraction | TAD §44 | Claims are part of the SAME structured output, not a separate extraction pass — "Do NOT build a separate extraction model" (directive Phase C.1) matches TAD's own single-schema design |
| Claim schema | TAD §44-45 | `{subject, predicate, object, claim_type}`; `claim_type ∈ {FACT, DERIVED, INFERENCE, UNKNOWN}` (§45) |
| Entailment | TAD §46-47 | DTD-05 pipeline: claim extraction validation → evidence mapping → entailment → contradiction detection → coverage verification → confidence → answer decision. Deterministic entailment: direct edge matching, path existence, bounded graph traversal, set membership, type hierarchy. Unsupported semantic assertions → `UNRESOLVED` |
| Contradiction handling | TAD §49 | `CONTRADICTED → REMOVE CLAIM → RE-SYNTHESIZE`, never a speculative rewrite. Maximum V1 re-synthesis: 1 |
| Verification score | TAD §48 | `V = 0.35 evidence_support + 0.20 evidence_quality + 0.15 evidence_independence + 0.10 coverage + 0.10 freshness + 0.10 provider_authority`; `V = min(V, 0.50)` if "significant contradictory evidence"; "weak contradiction may receive a small penalty" |
| Verification taxonomy | TAD §50 (canonical, 6-value) | `VERIFIED, PARTIALLY_VERIFIED, QUALIFIED, DISPUTED, INCONCLUSIVE, REJECTED` — already the single canonical internal model per the 2026-08-30 reconciliation (C-3); HLRD §42's 4-value labels and TAD §50's own routing-bucket table are **derived views**, not competing taxonomies |
| Re-synthesis | TAD §44 (invalid schema), §49 (contradicted claim) | Maximum 1 re-synthesis cycle (TAD §49's explicit number); whether the schema-invalid and contradiction-triggered paths share one counter or are independent is **not stated** — flagged in T.4 |
| Hallucination enforcement | HLRD §41 (Verification Layer "is the enforcement mechanism against hallucination"); TAD §47 (`UNRESOLVED` default) | Any claim without deterministic support defaults to `UNRESOLVED`, never silently `VERIFIED` |
| Graph-version handling | TAD §20 ("flows through DTD-04 → EvidencePackage → LLM → DTD-05"); TAD §55 | The locked `graph_version` must reach DTD-05 unchanged; re-reading "the current graph" during verification would violate §20 |
| Security boundaries | HLRD §41; TAD §30, §43 ("does not receive unrestricted repository access") | See T.5 |
| Audit/telemetry | TAD §32's telemetry pattern (already implemented in D9, `PlanTelemetry`), TAD §55's `CONCURRENT_UPDATE_DETECTED` | No D10-specific telemetry struct defined in TAD yet — an implementation detail for D10, following the D9 `PlanTelemetry` precedent |
| Token/latency budget | TAD §27 (already on `QueryContract`), §41-42 | D10 must respect the same budgets already carried through `RetrievalPlan`/`EvidencePackage` — no new budget concept introduced |
| Maximum retry/re-synthesis count | TAD §49 | Exactly 1 |

**Pipeline shape** (matches the directive's own diagram, TAD §43+§46 combined): `EvidencePackage → LLM Gateway → Structured Answer + Claims → Verification Engine (entailment/contradiction/coverage/confidence) → Verification Result → optional ONE re-synthesis → Final Answer/Abstention`. TAD's own component list (§6, D-phase's first audit) already keeps `LLM Gateway` and `Verification Engine` as two distinct components (#14, #15) — confirms the directive's "do not collapse these components" instruction matches TAD's own decomposition, not an external addition.

### T.3 Phase C — Verification of the Previously-Agreed D10 Decisions

1. **Claim extraction** — confirmed consistent with TAD §44 (single schema, no separate extraction model). No contradiction.
2. **Deterministic entailment** — confirmed consistent with TAD §47's literal list (direct edge matching, path existence, bounded traversal, set membership, type hierarchy — "deterministic role/name rules where explicitly defined" is the directive's paraphrase of "type hierarchy"/"set membership," not a new mechanism). `UNRESOLVED` default for unsupported semantic claims matches TAD §47 exactly. No contradiction.
3. **Contradicted claims** — TAD §49's `REMOVE CLAIM → RE-SYNTHESIZE` confirmed. The directive's elaboration ("if removal makes the answer incomplete/incoherent: retain only supported claims, mark the final result appropriately, annotate that the removed assertion lacked supporting evidence") is **not literally present in TAD's text** but is a non-contradictory operational elaboration consistent with HLRD §43's Answer Policy (`CONTRADICTED → explain conflict / downgrade confidence / possibly abstain`) — classified as a documented interpretation (category 2), not a silent invention, and not a contradiction.
4. **Contradiction penalty** — TAD §48's own text uses **qualitative** language only: "If **significant** contradictory evidence exists: V = min(V, 0.50). **Weak** contradiction may receive a small penalty." **Finding**: the directive's specific numeric thresholds ("confidence > 0.6" for significant, "<0.4" for weak) do **not** appear anywhere in `docs/TAD.md`, `docs/HLRD.md`, `docs/architecture-reconciliation.md`, or the conformance audit — confirmed by direct text search, not assumed. This is a **directive-vs-document mismatch**, the same recurring pattern flagged in every prior phase (D7's taxonomy, D8's intent vocabulary, D9's cache section-citation): the directive states these thresholds as "previously agreed," but no prior commit in this repository's history establishes them. **This does not block the audit** (Phase A-F require no code), but **does need resolution before D10.5 (contradiction handling) is implemented** — see T.7/Open Architectural Decisions. The qualitative rule itself (significant → cap at 0.50; weak → small penalty) is not in dispute, only the exact numeric boundary.
5. **Verification taxonomy** — confirmed: TAD §50 already contains both mapping tables (HLRD presentation mapping, pipeline routing bucket) verbatim, and TAD §50 states they will be "implemented as tested pure functions when the Verification Engine is built, never re-derived ad hoc." No new taxonomy needed; nothing to build here except the two small pure mapping functions themselves as part of D10.6.
6. **Graph version** — confirmed: TAD §20's explicit propagation chain (`DTD-03 → ... → DTD-04 → EvidencePackage → LLM → DTD-05`) already names DTD-05 (Verification) as a recipient of the same locked `graph_version`. D9's `EvidencePackage.graph_version` already carries the full, immutable `GraphVersion` object, so D10 has everything needed to avoid re-reading "the current graph" — this is an implementation obligation for D10 (never construct a fresh `GraphReader` lookup inside Verification), not a gap in what D9 already produced.

### T.4 Phase D — Cross-DTD Contradiction Analysis

Checked all 15 named categories directly against the current documents:

1. **Conflicting verification taxonomies** — none; TAD §50 is singular and canonical, HLRD §42 is an explicitly-marked derived view (reconciled 2026-08-30, C-3).
2. **Conflicting EvidenceStatus vs. VerificationStatus semantics** — **naming collision, not a contradiction**: `EvidenceStatus.DISPUTED` (TAD §18, relationship-level, already computed by the implemented Reconciliation Engine) and the canonical `VerificationStatus`'s `DISPUTED` (TAD §50, claim-level, not yet implemented) share a literal enum value name but are different types operating at different levels (edge vs. claim) with different producers (Reconciliation Engine vs. Verification Engine). This was already implicitly acknowledged during the Reconciliation phase ("TAD §50 governs the much later Verification Engine, untouched"). **Flagged for D10 implementation discipline**: `codex.verification`'s claim-level status must never be conflated with, or directly assigned from, a `CanonicalRelationship.status` of the same name — it is one *input signal* to deterministic entailment, not a shortcut answer. Not a document contradiction; a naming clash worth a code comment when D10 is built.
3. **Conflicting confidence formulas** — none; TAD §48 (Verification Confidence, claim-level) and TAD §37 (Ranking, entity/edge-level) are different formulas for different purposes, never presented as interchangeable.
4. **Conflicting contradiction formulas** — TAD §38 (Evidence Contradiction, already implemented in `codex.reconciliation`, relationship-level `contradiction_score`) and TAD §48's "significant/weak contradictory evidence" (claim-level, Verification Confidence penalty) are **related but distinct**: §48's V-formula penalty is expected to *consume* §38's already-computed `contradiction_score` (via the `CanonicalRelationship`s the claim's entailment maps to) rather than recompute contradiction from scratch — this is a reuse relationship, not a conflict, and is the natural reading of TAD §51's traceability chain. No contradiction; a design recommendation for D10.3/D10.4 to reuse `codex.reconciliation`'s existing `contradiction_score` rather than reinvent it.
5. **Conflicting graph-version semantics** — none (T.3 item 6).
6. **Conflicting completeness semantics** — none beyond the already-tracked, non-blocking TAD §33 percentage-denominator gap (T.8).
7. **Conflicting claim schema** — none; TAD §44-45 is the only claim schema defined anywhere.
8. **Conflicting LLM/planner boundary** — none; TAD §30 (Planner) and TAD §43 (LLM Gateway) are consistent, non-overlapping boundary statements, both already enforced/enforceable (D9's boundary tests; D10's forthcoming ones).
9. **Conflicting re-synthesis rules** — **one genuine open question, not a contradiction**: TAD names "Maximum V1 re-synthesis: 1" once (§49, contradiction-triggered) and separately says invalid/missing claims trigger `RESYNTHESIS` (§44, schema-triggered) without stating whether these share one counter. Both statements are individually clear; their *interaction* is unspecified. Flagged in Open Architectural Decisions (T.7) — recommended resolution given, not silently assumed.
10. **Conflicting evidence provenance requirements** — none; TAD §51's traceability chain is singular and consistent with every already-implemented D1-D9 provenance field.
11. **Conflicting retry limits** — same as #9; one number given, one interaction question open.
12. **Conflicting budget semantics** — none; T.1 item 8 already resolves the token-vs-latency-budget relationship as complementary, not conflicting.
13. **Conflicting meaning of PARTIAL** — checked directly: `CapabilityCoverage.PARTIAL` (TAD §17/§34, provider-run-level), `EvidencePackage.partial` (D9's own field, package-level "did not fully satisfy the plan"), and TAD §50's `PARTIALLY_VERIFIED` (claim-level) are three distinct concepts at three distinct levels, never asserted equivalent anywhere, and each already has a single unambiguous meaning within its own level. No contradiction; a terminology overlap worth naming explicitly in D10's own module docstrings (as `codex.coverage`/D9 already do for their own level).
14. **Conflicting meaning of UNRESOLVED/INCONCLUSIVE** — checked directly: `EvidenceStatus.UNRESOLVED` (TAD §18, relationship-level: "not yet reconciled" or "missing-entity endpoint," already implemented) vs. TAD §47's entailment `UNRESOLVED` (claim-level: "no deterministic rule could resolve this claim") vs. `NegativeQueryCoverage.INCONCLUSIVE` (TAD §34, coverage-level: "absence not trustworthy," already implemented) are three distinct, non-conflicting concepts at three levels — same pattern as #13, no contradiction.
15. **D9 implementation accidentally anticipating/constraining D10** — checked: D9 introduces `PlanStatus`, `NegativeQueryCoverage`, `CapabilityCoverage`, `EvidenceStatus` reuse, and `EvidencePackage.partial`/`.limitations` — none of these overlap with or pre-empt TAD §50's canonical verification taxonomy; `EvidencePackage.partial` is a boolean coverage-completeness flag, not a verification verdict, and nothing in D9 assigns a `VERIFIED`/`DISPUTED`/etc. value anywhere. No constraint found.

**No genuine HLRD↔TAD contradiction found anywhere in this sweep.** Two open questions surfaced (naming collision at #2, re-synthesis-counter interaction at #9/#11) — both are gaps requiring a small, explicit decision before the relevant D10 sub-phase, not contradictions requiring a STOP.

### T.5 Phase E — D10 Security Boundary Audit

No D10 code exists yet to AST-audit directly (`codex.llm`/`codex.verification` do not exist in the codebase — confirmed by directory listing). This section states the invariants D10's eventual implementation must satisfy, each traced to its authoritative source and to the existing precedent that already enforces the analogous invariant one layer up:

- **Repository text is untrusted data** — HLRD §41 ("LLM SHALL NOT be treated as an authoritative source of repository truth"); precedent already set by D8's `QueryContract` security tests (`test_qu_security.py`: user query text never modifies structured fields) and D9's boundary tests.
- **EvidencePackage is data, not instructions** — TAD §43 ("LLM receives Query + EvidencePackage + ResponseContract... does not receive unrestricted repository access"); D10 must construct the LLM prompt so that every EvidencePackage field (entity names, evidence text, source locations — all ultimately repository-derived strings) is passed as data content, never concatenated into instruction-bearing prompt segments the model could be steered by.
- **Retrieved source code cannot override system/developer policy** — same principle, applies specifically to `EvidencePackage.source_context` (`RepositorySymbol.source_location`-bearing entities) once real source-snippet retrieval is added (currently deferred, §S.C) — repository file content is exactly the class of string HLRD §41 forbids treating as authoritative.
- **Repository comments/docstrings cannot instruct the LLM to ignore verification** — same as above; also structurally reinforced by the fact that Verification (TAD §46-47) is deterministic and runs *after* the LLM call, over the LLM's own structured claims against the (separately, deterministically retrieved) evidence — a comment cannot talk its way past a check it never participates in.
- **LLM output cannot modify EvidencePackage** — architectural: `EvidencePackage` is D9's output, constructed before the LLM is ever invoked (TAD §43's own input ordering); D10 must not design any mutation path from LLM response back into the package.
- **LLM output cannot modify GraphVersion** — same; `GraphVersion` is locked by D9 (T.1 item 2) before D10 exists in the call chain; D10 must only *read* `EvidencePackage.graph_version`, never construct or request a new one.
- **LLM output cannot create evidence** — architectural: `Evidence` records only ever originate from `ProviderAdapter.normalize()` (D1) via `IngestionPipeline` (D4); D10's claims are typed `{subject, predicate, object, claim_type}` (TAD §45), never an `Evidence`-shaped object, and must never be upserted into `EvidenceStore`/`GraphStore`.
- **LLM output cannot promote a claim to VERIFIED** — TAD §46's own pipeline ordering (claim extraction → ... → confidence → answer decision) places verification-state assignment strictly after and independent of the LLM call; the LLM's own `claim_type` field (`FACT`/`DERIVED`/`INFERENCE`/`UNKNOWN`, TAD §45) is the model's *claim about itself*, never the verification verdict — D10 must keep these two enums structurally separate (also T.4 item 2's naming-collision discipline).
- **Verification is deterministic and authoritative over LLM assertions** — TAD §47 ("Complex semantic assertions default to UNRESOLVED unless deterministic rules exist") and HLRD §41 (Verification Layer as "the enforcement mechanism against hallucination") both state this directly.
- **The LLM cannot choose files/providers/evidence outside the supplied EvidencePackage** — TAD §43's closed input list (`Query + EvidencePackage + ResponseContract`) and the already-established Planner precedent (TAD §30, D9 §R.4: "the planner determines retrieval scope... never asks 'what files should I look at?'") extends identically to the LLM Gateway: it receives a pre-selected, bounded package and has no tool-call/file-access surface in V1's design (TAD §43 lists no such capability).
- **The planner cannot ask the LLM which files to inspect** — already true and tested today (D9 §R.11, `test_no_llm_or_slm_model_dependency_imported`, `test_no_module_imports_forbidden_dependencies`); re-confirmed as still true with no code change in this audit phase.
- **The verifier cannot ask the LLM whether its own claim is true** — TAD §47's deterministic-only entailment list contains no LLM invocation; TAD §46's pipeline diagram never routes back to the LLM Gateway from Verification except via the single explicit RE-SYNTHESIZE path (§49), which asks for a *new answer*, not a self-judgment of the old one.

No violation exists today (no D10 code exists). Every invariant traces to an authoritative source or an already-enforced precedent one layer up. **D10.9 (security/boundary tests) must include an AST-based import-boundary test for `codex.llm`/`codex.verification` matching the `codex.planner`/`codex.query_understanding` precedent, plus a prompt-construction test proving EvidencePackage content is never string-concatenated into a system/instruction-role prompt segment.**

### T.6 Phase F — D10 Failure Matrix

| Failure | Internal state | Verification state | Re-synthesis allowed? | Final answer allowed? | User receives | Telemetry event |
|---|---|---|---|---|---|---|
| LLM timeout | No structured output | N/A (no claims to verify) | No (not a schema/contradiction failure) | No | Abstention | `LLM_TIMEOUT` |
| LLM malformed JSON | Schema validation failed | N/A | Yes (TAD §44, counts toward the 1 max) | Only if re-synthesis succeeds | Answer, or abstention if retry also fails | `RESYNTHESIS_TRIGGERED` (reason=malformed_json) |
| Missing claims array | Schema validation failed | N/A | Yes (TAD §44) | Only if re-synthesis succeeds | Answer, or abstention | `RESYNTHESIS_TRIGGERED` (reason=missing_claims) |
| Invalid claim schema (per-claim) | Schema validation failed | N/A | Yes (TAD §44) | Only if re-synthesis succeeds | Answer, or abstention | `RESYNTHESIS_TRIGGERED` (reason=invalid_claim) |
| Unsupported predicate (not in `RelationshipType`) | Claim un-mappable to canonical ontology | `UNRESOLVED` (per-claim; TAD §47 default) | No (not a schema/contradiction trigger by itself) | Yes, with that claim excluded/qualified | Qualified answer | `CLAIM_UNRESOLVED` (reason=unsupported_predicate) |
| No evidence for a claim | Entailment finds no matching `CanonicalRelationship`/`Evidence` | `UNRESOLVED` (TAD §47) | No | Yes, qualified | Qualified answer or abstention if it's the only claim | `CLAIM_UNRESOLVED` (reason=no_evidence) |
| Insufficient evidence (found but below completeness) | Coverage below `completeness_requirement` | `PARTIALLY_VERIFIED`/`QUALIFIED` (TAD §50) | No | Yes, qualified | Qualified answer, limitations noted | `COVERAGE_INSUFFICIENT` |
| Contradictory evidence | `CanonicalRelationship.contradiction_score`/`.status` indicates conflict | `DISPUTED` (significant) or penalized `VERIFIED`/`PARTIALLY_VERIFIED` (weak, T.3 item 4's open threshold) | Yes if significant (TAD §49, max 1) | Yes after removal, or with explicit qualification if re-synthesis exhausted | Qualified answer noting the removed/contested claim | `CLAIM_CONTRADICTED`, `RESYNTHESIS_TRIGGERED` (reason=contradiction) |
| Stale graph (concurrent update mid-query) | `CONCURRENT_UPDATE_DETECTED` (TAD §55, already implemented in D9 as `GraphVersionMismatchError`) | Unaffected — verification still runs against the originally locked version | No | Yes, against the locked (now possibly outdated) version | Answer (reproducible against locked version), telemetry-visible staleness | `CONCURRENT_UPDATE_DETECTED` (already emitted by D9) |
| Stale evidence (old `Evidence.freshness`) | `freshness` factor lowers `V` (TAD §48) | Lower confidence, possibly `QUALIFIED` | No | Yes, qualified | Qualified answer | `LOW_FRESHNESS` |
| Provider failure (upstream, at ingestion time) | Already classified by `codex.coverage` (`FAILED`) | Reduces `coverage`/`evidence_quality` factors | No (D10 concern, not a D10-triggered retry) | Yes, qualified/abstain per coverage severity | Qualified answer or abstention (negative-query safety, TAD §34, already enforced in D9) | Already `PROVIDER_FAILURE`-classified upstream (D1-D4) |
| Partial EvidenceCohort | Already classified (`CapabilityCoverage.PARTIAL`) | Lowers `coverage` factor | No | Yes, qualified | Qualified answer, limitations noted | Already `COHORT_PARTIAL`-classified upstream |
| Incomplete negative-query coverage | `NegativeQueryCoverage.INCONCLUSIVE` (already computed by D9) | `INCONCLUSIVE` (TAD §50) — never `FALSE` | No | Yes, as an explicit "cannot determine" | Abstention-shaped qualified answer, never a false negative | Already emitted by D9 (`plan.negative_query_result`) |
| Token budget exhaustion | `PLAN_UNSUPPORTED`/`PLAN_BLOCKED` at D9, or exceeded during LLM generation | N/A if pre-empted at D9; `INCONCLUSIVE` if exhausted mid-D10 | No | No if D9 already blocked; qualified/abstain if exhausted in D10 | Abstention or qualified answer | Already `PLAN_UNSUPPORTED`/`PLAN_BLOCKED` (D9) or new `TOKEN_BUDGET_EXCEEDED` (D10) |
| Latency budget exhaustion | Same pattern as token budget | Same | No | Same | Same | `LATENCY_BUDGET_EXCEEDED` |
| Re-synthesis failure (2nd attempt also invalid/contradicted) | Retry budget exhausted (TAD §49's "Maximum V1 re-synthesis: 1") | `REJECTED` or `INCONCLUSIVE` depending on failure type | No (budget exhausted) | No, or abstention-only | Abstention/rejection, explicit reason given | `RESYNTHESIS_EXHAUSTED` |
| Repeated invalid LLM output (both attempts malformed) | Same as above | N/A | No | No | Abstention | `RESYNTHESIS_EXHAUSTED` |
| Security/prompt-injection content in repository | Detected structurally never — the content is inert data (T.5); no special-case "detection" state needed since it's never interpreted as instructions | Unaffected — verification runs on structured claims only | No | Yes, normally, if the claims are otherwise valid/verified | Normal answer; the injection attempt itself never surfaces as behavior | None needed beyond normal claim-level events (proven inert by construction, not by runtime detection) |
| Conflicting evidence from independent providers | `CanonicalRelationship.contradiction_score` (TAD §38, already computed, independence-aware) feeds the `evidence_independence` factor (TAD §48) | `DISPUTED` if the conflict is significant across independent sources | Yes if triggered (TAD §49) | Yes, qualified, noting cross-provider disagreement | Qualified answer | `CLAIM_CONTRADICTED` (reason=cross_provider) |

### T.7 Open Architectural Decisions

1. **Contradiction-penalty numeric thresholds** (T.3 item 4) — TAD §48 gives only qualitative "significant"/"weak" language; the directive's `>0.6`/`<0.4` values are not found in any authoritative document. **Recommendation**: resolve as a documented calibration point at D10.4/D10.5 time, following the exact ADR-018/D8-decay/D9-ranking-weight precedent (a clearly-labeled, swappable default, not a claimed-final value) — `0.6`/`0.4` are reasonable defaults consistent with the directive's stated intent and can be adopted as such **if the user confirms this is acceptable**, rather than silently invented.
2. **Re-synthesis counter interaction** (T.4 items 9/11) — does the schema-invalid path (TAD §44) and the contradiction path (TAD §49) share one global "maximum 1 re-synthesis" counter, or does each failure class get its own budget of 1? **Recommendation**: a single shared counter (one re-synthesis attempt total per answer, regardless of trigger reason) is the more conservative, TAD-consistent reading (TAD §49 says "Maximum V1 re-synthesis: 1" without qualifying it per failure type), and avoids an unbounded-retry loop where a malformed-then-contradicted-then-malformed-again cycle could otherwise consume multiple LLM calls.
3. **`ResponseContract`'s exact structure** (T.2) — TAD §43 names it as an LLM Gateway input but never defines its fields separately from §44's `{answer, claims[]}` schema. **Recommendation**: treat `ResponseContract` as the formal JSON Schema descriptor whose required shape TAD §44 already gives (i.e., the schema-as-artifact the Gateway constructs and passes to constrain generation) — not a new, additional structure.
4. **`EvidencePackage.evidence` should also resolve `contradicting_evidence_ids`** (T.1 item 11) — recommended as a small, non-blocking D9 addendum (or D10.2 prerequisite step) so the package is self-sufficient per TAD §42.

None of these four require an ADR (no genuine cross-document architectural conflict); all are calibration-point or interpretation-level decisions in the same class already resolved repeatedly throughout D1-D9, requiring only the user's confirmation before D10 code encodes them.

### T.8 Completeness-Metric Dependency Check (explicit, per directive instruction)

Re-confirmed: TAD §33's `LOW ≥50%/MEDIUM ≥75%/HIGH ≥90%` denominator remains undefined in the current `docs/TAD.md` text (unchanged since D8/D9's own checks). **D10 does not depend on this metric**: TAD §46's Verification pipeline's "coverage verification" step consumes `codex.coverage`'s already-implemented classifications (`CapabilityCoverage`, `NegativeQueryCoverage`, `is_exhaustive_coverage()`) exactly as D9 already does — D10 only needs to know whether the *requested* `CompletenessLevel` was structurally satisfiable (an enum-level, already-implemented check), never to compute an achieved percentage against an undefined denominator. `PARTIALLY_VERIFIED`/`QUALIFIED` (TAD §50) are qualitative verification states, not percentage-driven ones. **Not invented, not silently resolved — confirmed non-blocking for D10, same disposition as D8 and D9.**

### T.9 STOP/GO Decision

No HLRD/TAD contradiction found (T.4). No missing architectural decision materially blocks D10 (T.7's four items are calibration points/interpretations, each with a recommended, non-inventive resolution awaiting confirmation, not a STOP-worthy gap). The one PARTIAL closure item (T.1 #11) is non-blocking with a clear recommended fix. The completeness-metric dependency is confirmed non-blocking (T.8).

**D10 IMPLEMENTATION READY — AWAITING EXPLICIT APPROVAL.**

## U. D10 Implementation — 2026-08-31

Approved per explicit user directive: "APPROVED — PROCEED WITH D10 IMPLEMENTATION," which also locks four binding decisions (T.7's open items) before code: contradiction thresholds `>0.60`/`<0.40` (documented calibration constants, swappable); one shared re-synthesis counter across both trigger types; `ResponseContract` = TAD §44's own schema, no competing structure; `EvidencePackage.evidence` must resolve both `supporting_evidence_ids` and `contradicting_evidence_ids`.

### U.0 D9 addendum (T.1 item 11 fix)

`codex.planner.retrieval.collect_evidence()` now resolves `(*rel.supporting_evidence_ids, *rel.contradicting_evidence_ids)` instead of only the former — `EvidencePackage.evidence` is now the verifier's authoritative, self-sufficient evidence boundary per TAD §42, with no need to reach around the package into `EvidenceStore`. 5 new regression tests in `tests/test_planner_retrieval.py` prove: supporting evidence included, contradicting evidence included, independence-group provenance preserved unmodified, deduplication when an id appears in both roles, and an end-to-end `execute_query()` proof using a hand-crafted `DISPUTED` relationship. 100% coverage maintained on `codex.planner` (527 tests project-wide at this point), clean ruff/mypy.

### U.1-U.9 Implementation summary (D10.1-D10.9)

| Unit | Module(s) | TAD/HLRD source | Summary |
|---|---|---|---|
| D10.1 | `codex.llm.gateway` | TAD §43 | `LLMGateway` Protocol, `LLMRequest`/`LLMGenerationResult`, `GenerationStatus` (honest OK/MALFORMED_OUTPUT/TIMEOUT/BUDGET_EXCEEDED). No mandatory real LLM dependency; `FakeLLMGateway` is the deterministic test double. |
| D10.2 | `codex.llm.schema` | TAD §44-45 | `StructuredAnswer{explanation, claims[]}`, `Claim{subject, predicate, object, claim_type}`. `predicate` accepts `RelationshipType` or `DERIVED_RELATIONSHIP_TYPES` (a real gap found and fixed before D10.3: TAD §45's own "DERIVED: A REACHES C" example was otherwise unrepresentable). |
| D10.3 | `codex.verification.entailment` | TAD §47 | `entail_claim()`: direct edge matching (also covers "set membership"/"type hierarchy" — no separate mechanism named by TAD) and bounded path existence (the three derived types, BFS over `EvidencePackage.relationships` only). `SUPPORTED`/`UNRESOLVED` only — contradiction is a separate stage. |
| D10.4 | `codex.verification.confidence`, `.engine` | TAD §46-48 | TAD §48's exact six-factor V formula/weights; D10 Decision 1's thresholds (`>0.60`/`<0.40`/`0.40-0.60` intermediate, `V=min(V,0.50)` for significant only). `provider_authority`/`freshness` reuse existing D2/reconciliation patterns; one new, clearly-scoped calibration point (`CapabilityCoverage` → `[0,1]` for the `coverage` factor). |
| D10.5 | `codex.verification.contradiction` | TAD §49 | `handle_contradictions()`: partitions into retained/removed via D10.4's `is_significantly_contradicted`, generates deterministic REMOVE-only feedback text. |
| D10.6 | `codex.verification.state` | TAD §50 | Canonical `VerificationStatus` (6 values); `classify_claim`/`classify_answer`; `to_hlrd_label`/`to_routing_bucket` reproduce TAD §50's own two tables verbatim. Explicit `EvidenceStatus` vs `VerificationStatus` naming-collision discipline, with a regression test proving the `==` gotcha. |
| D10.7 | `codex.verification.resynthesis` | TAD §44, §49; D10 Decision 2 | `run_verification_loop()`: ONE shared re-synthesis budget across both malformed-output and contradiction triggers (`MAX_ATTEMPTS=2`). Both of the directive's own worked examples verified as named tests. |
| D10.8 | `codex.verification.answer` | HLRD §43, TAD §50 | `build_final_answer()`: HLRD's four-way policy via TAD §50's routing bucket, plus one explicit no-evidence-no-assertion override. Negative-query safety consumes D9's `negative_query_result` unchanged. |
| D10.9 | `tests/test_verification_security.py`, `tests/test_planner_boundaries.py` | directive D10.9 | 21 tests covering every named invariant; AST-based import-boundary checks extended to confirm `codex.planner` still forbids `codex.llm`/`codex.verification` now that they actually exist. |

### V. D10.10-D10.11 — Integration + Final Cross-DTD Audit — 2026-08-31

**D10.10**: `tests/test_d1_d10_integration.py`, 13 tests (scenarios A-M), the real D1-D9 pipeline throughout (nothing mocked except the LLM boundary) — see the commit message for the full scenario list. One real bug found and fixed while writing scenario G: `DeterministicFakeAdapter`'s default `predicate` is `CO_CHANGED_WITH`, not `CALLS` — a test-construction mistake (not a production defect), fixed before committing.

**D10.11 — final validation**: fresh venv, `pytest -q --cov=codex` → **651 tests passed**, 100% on every one of the 8 new D10 modules (`codex.llm.{gateway,schema}`, `codex.verification.{entailment,confidence,engine,contradiction,state,resynthesis,answer}` — 7 verification modules + 2 llm modules), 99% project-wide (pre-existing, unrelated gaps in `evidence/store.py`/`graph/memory_store.py`/`repository/manager.py`, untouched by D10). `ruff check src tests` clean. `mypy src` clean, 65 source files.

**Final cross-DTD contradiction re-check**: repeated the same 15-category sweep as §T.4 against the now-complete implementation (not just the pre-implementation plan) — no new contradiction found; the two previously-open items (T.4 #2 naming collision, #9/#11 re-synthesis counter) are now **closed by implementation**: the naming collision is guarded by a passing regression test (`test_verification_status_is_not_evidence_status`); the re-synthesis counter question is resolved by D10 Decision 2 and proven by both of the directive's own worked examples passing as named tests.

**Explicit verification of the 14 "did not accidentally" checks (directive D10.11), each checked against actual code, not assumed:**

1. **Move retrieval into the LLM** — false. `codex.llm`/`codex.verification` never import `codex.planner`/`codex.graph`/`codex.registry` (AST-verified, D10.9); `LLMRequest` carries only an already-assembled `EvidencePackage`.
2. **Move truth determination into the LLM** — false. `entailment.py`/`confidence.py`/`engine.py`/`contradiction.py`/`state.py` never import `LLMGateway`; `classify_claim` never reads `claim.claim_type` (proven: `test_llm_self_labeling_fact_never_forces_verified_status`).
3. **Allow the LLM to invent evidence** — false. `Claim` carries none of `Evidence`'s identity/provenance fields (proven: `test_claim_type_has_no_evidence_shaped_fields`).
4. **Allow the LLM to bypass EvidencePackage** — false. Every verification function's sole evidence source is the `package` argument it is handed.
5. **Allow the verifier to retrieve arbitrary new evidence** — false. No verification module imports `EvidenceStore`/`GraphReader`/`CapabilityRegistry`; `entail_claim`/`compute_factors` only ever read fields already on the `EvidencePackage`/`EntailmentResult` objects passed in.
6. **Merge EvidenceStatus with VerificationStatus** — false. Distinct enum types (proven, including the documented `==`-via-`StrEnum` gotcha: `test_verification_status_is_not_evidence_status`).
7. **Create a second verification taxonomy** — false. `VerificationStatus` (TAD §50, 6 values) is the only verification taxonomy. `EntailmentStatus` (`SUPPORTED`/`UNRESOLVED`) is a distinct, lower-level *intermediate* signal — never presented externally, never substituted for a `VerificationStatus` value, matching the same "different levels, not competing" pattern as `EvidenceStatus`/`CapabilityCoverage`/`NegativeQueryCoverage` already established across this project.
8. **Create a second contradiction formula** — false. `contradiction_score` is read exclusively from `CanonicalRelationship.contradiction_score` (TAD §38, computed once by the already-implemented Reconciliation Engine); D10 Decision 1's thresholds are classification *bands* on that existing score, not a new formula (verified via direct grep — every `contradiction_score` reference in `codex.verification` reads the field, none recomputes it).
9. **Create a second confidence model** — false. TAD §48's V formula is the only confidence model D10 introduces; D9's separate ranking `score()` (TAD §37, entity/edge relevance) was never touched during D10 (confirmed: `git log` shows no D10 commit modifies `codex/planner/ranking.py`).
10. **Introduce embeddings** — false (grep-verified, no embedding library imported anywhere in `codex.llm`/`codex.verification`).
11. **Introduce fuzzy semantic reasoning** — false. `entail_claim` is exact-match/BFS-only (D10.3, re-verified).
12. **Create unlimited re-synthesis** — false. `MAX_ATTEMPTS=2` is a hard module-level constant; every re-synthesis test (unit and integration) proves `attempts <= 2`.
13. **Weaken negative-query completeness** — false. `git log -- src/codex/coverage/` shows the last commit touching that module predates all of D10 (`9db28ff`, from the gap-closure phase) — `codex.coverage` is untouched by D10; D10.8 consumes `NegativeQueryCoverage` exactly as D9 produced it.
14. **Break graph-version reproducibility** — false. `EvidencePackage.graph_version` is the unmodified `GraphVersion` D9 already locked; D10.10 scenario L explicitly proves identity across `IngestionResult`/`RetrievalPlan`/`EvidencePackage`; no D10 code constructs a new `GraphVersion` or re-fetches a "current" graph.

**No genuine HLRD/TAD contradiction found anywhere in D10.** No ADR required. D10 Decisions 1-4 (approved by the user) are the only calibration/interpretation-level choices, all documented with maximum visibility per this project's entire established discipline.

**D10 — COMPLETE.** Per the directive's explicit closing instruction, D11+ (any further architectural stages) is not started. Stopping after D10.11 for review.

---

## W. D1-D10 Integration Hardening Pass — 2026-08-31

Per the "focused D1-D10 integration hardening pass" directive, closing the two findings recorded in `docs/architecture-truth-report.md` §12. **D11 was not started.** No HLRD/TAD text was modified, no D1-D10 architecture decision was reopened. Both HLRD/TAD and the truth report were re-read before any change.

### W.1 Finding 1 — symbol-level E2E gap

**Root cause (already diagnosed in the truth report, re-confirmed here):** `tests/fake_ingestion_provider.py`'s `DeterministicFakeAdapter` hardcoded `base_type=BaseEntityType.FILE` at all four entity/relationship construction sites — production code (`GraphReader.find_entities`, `codex.planner.retrieval.resolve_targets`, `codex.planner.retrieval.bounded_traversal`) was already proven correct for symbol-level entities via an out-of-suite manual construction; only the shared test fixture had never exercised it.

**Change made (test infrastructure only, `src/codex/` untouched):**

- `tests/fake_ingestion_provider.py`: `DeterministicFakeAdapter` gained two optional constructor parameters, `base_type: BaseEntityType = BaseEntityType.FILE` (applies to every constructed entity and a relationship pair's subject) and `object_base_type: BaseEntityType | None = None` (applies to a relationship pair's object only, defaulting to `base_type`). Both default to the fixture's pre-existing behavior exactly — **every one of the 651 pre-existing tests using this fixture is unaffected**, confirmed by the full suite passing unmodified.
- `tests/symbol_level_fixtures.py` (new): `build_symbol_level_graph()` registers four `DeterministicFakeAdapter` instances (FILE, FUNCTION, CLASS, METHOD — one real `IngestionPipeline.run()` call, the same multi-provider-coexistence pattern D6 already established for Git+CodeQL/SCIP+CodeQL) producing a mixed graph: 3 FILE entities, 3 FUNCTION entities with 2 real `CALLS` relationships (mirroring this directive's own worked example, "Which tests call `authenticate`?"), 1 CLASS entity, 1 METHOD entity with 1 real `CONTAINS` relationship.
- `tests/test_symbol_level_e2e.py` (new), 3 tests:
  1. `test_mixed_graph_contains_file_function_class_and_method_entities` — proves the fixture itself produces all four base types with real, non-fabricated relationships (not four disjoint islands).
  2. `test_symbol_level_query_end_to_end_via_real_query_understanding` — the full chain **driven by real Tier-0 text detection** (not a hand-built `QueryContract`): `"Which tests call authenticate?"` → `codex.query_understanding.tier0.detect()`/`understand_query()` (D8) → `plan_query()`/`execute_query()` (D9) → `LLMRequest`/`StructuredAnswer` (D10.1-2, LLM boundary faked) → `run_verification_loop()` (D10.3-7) → `build_final_answer()` (D10.8) — reaching `STRONG_ANSWER`/`VERIFIED` with 2 retained claims, 0 removed, no re-synthesis. This is the same query and pipeline shape as the prior audit's one-off, non-committed trace script, now a committed, repeatable regression test.
  3. `test_class_contains_method_relationship_resolves_through_real_retrieval` — a second symbol-level shape (`CLASS --CONTAINS--> METHOD`) via a hand-built `QueryContract` (Tier-0 has no class-containment pattern — an existing, unrelated D8 scope boundary, not changed by this pass), proving retrieval is not accidentally specialized to `FUNCTION`/`CALLS` only.

**One real fixture-naming bug found and fixed during this work, not a production defect:** the first draft named the METHOD entity `"AuthService.authenticate"`, which is a case-insensitive **substring** of the FUNCTION target `"authenticate"` — `GraphReader.find_entities()` is documented, correct substring-match behavior (`src/codex/graph/store.py`'s own docstring: "Deterministic... substring lookup"), so the query for `"authenticate"` was silently also matching the METHOD entity, pulling in an unrelated `CONTAINS` relationship (3 relationships instead of the expected 2). Fixed by renaming the fixture's METHOD entity to `"AuthService.check_credentials"` (no substring overlap) — **no change to `codex.graph`/`codex.planner`**, since the retrieval behavior was already correct and already documented; this was purely a fixture-construction mistake, diagnosed and fixed the same way D10.10 scenario G's predicate mistake was (directive §V).

**Explicitly did not change:** `codex.ontology` (no entity/relationship semantics touched — `BaseEntityType.FUNCTION`/`CLASS`/`METHOD` already existed, unused only because no fixture constructed them), `codex.graph`/`codex.planner` retrieval logic (zero lines changed — `find_entities`/`resolve_targets`/`bounded_traversal` are untouched, already correct), `tests/planner_fixtures.py`'s `build_graph()` (the FILE-only helper used by all pre-existing D9/D10 tests — left exactly as-is, a new, separate fixture module was added instead of modifying the shared one, so no pre-existing test's behavior could possibly change).

### W.2 Finding 2 — provider_authority gap

**Traced TAD §48's `provider_authority` back to D2, per the directive's explicit instruction:** TAD §48 names `provider_authority` as one of six Verification Confidence factors (weight 0.10) but never defines what it measures — confirmed by re-reading TAD §48 directly this pass (no other TAD/HLRD section defines "authority" either). D2's `ProviderScoreProfile.evidence_quality` (TAD §31, ADR-018) is the closest, already-canonical, already-required per-provider trust metric Codex has: normalized `[0.0, 1.0]`, supplied once per provider at `CapabilityRegistry.register()` time, never per query — matching `provider_authority`'s own required shape (`Mapping[str, float]`) exactly, with no invention needed.

**Checked for duplication before wiring it, since TAD §48's own formula already has a separate `evidence_quality` factor (weight 0.20) in the same formula:** re-read `codex.verification.confidence.compute_factors` directly. TAD §48's `evidence_quality` factor is `sum(e.confidence for e in evidence) / len(evidence)` — the average of the **retrieved `Evidence.confidence`** values for one query, a dynamic, per-record quantity computed only from `EvidencePackage.evidence`, never touching registry data. D2's `ProviderScoreProfile.evidence_quality` is static, per-provider registration metadata. The two factors would never read the same underlying numbers — reusing D2's value for `provider_authority` is not a duplication of TAD §48's own `evidence_quality` factor, despite the unfortunate shared field name (the same terminology-overload risk already flagged as **C-4**, now with one more instance recorded).

**Determination: D2 already contains sufficient authoritative data — wired, not invented.**

**Change made (smallest conformant wiring, both changes additive/backward-compatible):**

- `src/codex/registry/registry.py`: `CapabilityRegistry.provider_authority_map() -> dict[str, float]` — a new, read-only accessor returning `{provider_name: profile.evidence_quality}` for every provider with a registered profile. Unlike `rank()`, a provider with no profile is simply absent (not an error) — `provider_authority`'s own historical default (`1.0`, uniform trust) already handles an absent entry safely, and Verification must never fail merely because a provider used only for evidence (not for provider *selection*) lacks a scoring profile. 5 new tests in `tests/test_capability_registry.py`.
- `src/codex/verification/resynthesis.py`: `run_verification_loop()` gained one new optional parameter, `registry: CapabilityRegistry | None = None`. An explicitly-supplied `provider_authority` mapping always wins (every pre-existing caller's exact behavior is unchanged); when `provider_authority` is omitted and `registry` is given, it is derived via `registry.provider_authority_map()`; when both are omitted (the pre-hardening default, still used by every pre-existing test), behavior is byte-identical to before this pass. `engine.py`/`confidence.py` were **not** touched — the mapping is resolved once at the top of `run_verification_loop` and threaded through the existing `provider_authority` parameter unchanged, the smallest change that reaches the real formula. 4 new tests in `tests/test_verification_resynthesis.py`, including one proving the registry-derived value actually lowers `V` relative to the no-registry baseline (not just that the parameter is accepted), one proving explicit-mapping override precedence, and one proving a registry with no profile for the relevant provider safely falls back to the historical default rather than erroring.

**No production call path currently constructs and passes a `registry` argument** — as the truth report noted, no single "answer a query end to end" orchestrator function exists yet anywhere in `src/codex/` (D1-D10 are a set of composable stages, not yet wired behind one entry point; that wiring is implicitly D11+ territory, not started here). `tests/test_symbol_level_e2e.py::test_symbol_level_query_end_to_end_via_real_query_understanding` (§W.1) demonstrates the wiring is used correctly when a caller does have a registry (as any real orchestrator will), passing `registry=registry` into `run_verification_loop()`.

### W.3 Validation

Fresh `.venv-work` run: **663/663 tests passing** (651 pre-existing + 12 new: 3 symbol-level E2E, 5 `provider_authority_map`, 4 `run_verification_loop` registry-wiring), **99% project-wide coverage** (`registry.py`/`resynthesis.py` both still 100% after the additions; the only sub-100% module remains `repository/manager.py`, pre-existing, unrelated), `ruff check src/ tests/` clean, `mypy src/` clean (65 source files). No new pip dependency. No ADR created (no genuine new architectural decision — D2 gap 2 is a documented, non-inventive data-source reuse; the symbol-level fixture gap 1 required no architecture change at all, only a test-double extension).

**No genuine HLRD/TAD contradiction found.** Both HLRD.md and TAD.md, plus `docs/architecture-truth-report.md`, were re-read in full before any change per the directive's explicit instruction.

**W — COMPLETE.** D11 not started. Stopping here for review.

---

## X. D11 Pre-Implementation Audit — 2026-08-31

Per the explicit "Start D11 Pre-Implementation Audit, but DO NOT implement D11 yet" directive. **Audit-only: no production code, no test code, and no HLRD/TAD text was written or modified in this phase.** Read directly for this audit (not assumed from prior conversation summaries or from this document's own earlier sections): `docs/HLRD.md` in full (all 64 sections, unchanged since the SourceLocation-closure commit — confirmed via `git log`), `docs/TAD.md` in full (all 84 sections, same confirmation), `docs/architecture-conformance-audit.md` §A-W in full (this document, 1034 lines), `docs/architecture-truth-report.md` in full (356 lines), `PROGRESS.md` in full (147 lines). No standalone ADR files exist anywhere in the repository (confirmed: `find . -iname "*adr*"` returns nothing) — "all ADRs" means this document's own §D (ADR Audit) and §I (ADR-018 resolution), both re-read, plus `PROGRESS.md`'s own ADR table (§"Open Items", consistent with §D, no drift found between the two).

### X.1 TAD §33 completeness denominator

**Exactly what is undefined:** TAD §33 states `LOW ≥50%, MEDIUM ≥75%, HIGH ≥90%, EXHAUSTIVE 100%+complete repository coverage`, immediately followed by "these are initial benchmark-calibrated thresholds" — re-read directly, unchanged since every prior audit pass (P.2, R.2, S, truth-report §10). TAD never states the **denominator**: not "% of entities discovered vs. some expected total," not "% of required capabilities that succeeded," not "% of expected evidence records retrieved," not any other quantity. No other HLRD/TAD section supplies it either (HLRD §37-39 discusses coverage/completeness conceptually — "how much of the evidence required by the query has actually been obtained" — but gives no formula, only the qualitative concept `codex.coverage`'s `EXHAUSTIVE` check already fully satisfies without needing a percentage).

**Which future component requires it:** Not D11. `codex.coverage.engine.CompletenessLevel` already propagates LOW/MEDIUM/HIGH/EXHAUSTIVE as a requested-level enum through D8→D9→D10 without ever needing to *evaluate* an achieved percentage — confirmed unchanged by this pass (`codex.coverage.engine.py` module docstring re-read, `is_exhaustive_coverage()` is still the only quantitative check). Telemetry (D11's own candidate scope, X.5) only needs to **record** which `CompletenessLevel` was requested and whatever `codex.coverage` already classified (`CapabilityCoverage`/`NegativeQueryCoverage`, both already fully implemented) — it does not need to compute a percentage either. The actual consumer is **TAD §80 Phase 5 (Validation: benchmark repositories, ground truth, metrics, calibration)** — the percentage's denominator can only be meaningfully defined once real ground-truth data exists to decide what it should measure, which is explicitly a later phase than D11.

**Not invented.** No metric was proposed or implemented. **Non-blocking for D11**, re-confirmed a fourth time (P.2, R.2, S, truth-report §10, now this pass) with an identical conclusion each time — this stability across four independent audits is itself evidence the gap is well-understood and correctly parked, not overlooked.

### X.2 TAD §55/§64 CONCURRENT_UPDATE_DETECTED

**The missing capability, precisely identified this pass (a sharper finding than any prior audit recorded):** TAD groups two distinct failure modes under one name, and Codex's own code already treats them as two distinct things without saying so out loud:

1. **Retrieval-side version-mismatch detection — already fully built, D9.** `codex.planner.planner.execute_query` (`planner.py:343-348`) raises `GraphVersionMismatchError` — whose message literally reads `"CONCURRENT_UPDATE_DETECTED: plan locked graph_version ..."` — the moment a caller executes a `RetrievalPlan` against a `GraphReader` whose `version.version_id` no longer matches the plan's locked version. Tested (`tests/test_planner_graph_version.py:125`). This needs no new detection logic and no ADR.
2. **Ingestion-side concurrent-*writer* conflict detection — genuinely not built, and correctly not built yet.** `codex.ingestion.pipeline.py`'s own module docstring (lines 102-109, re-read this pass) explicitly states: *"Real concurrent-writer detection (TAD §64's `GRAPH_VERSION_CONFLICT`/`CONCURRENT_UPDATE_DETECTED`) is not implemented: this pipeline is synchronous/single-threaded... real concurrent-writer detection needs a persistent storage layer with its own transaction semantics (ADR-002 territory), out of D4's scope."* This is a genuinely different capability (detecting two `IngestionPipeline.run()` calls racing to publish) than #1 (detecting a stale reader), correctly deferred to real storage technology (ADR-002), not a D11 concern.

**A third, previously-uncatalogued finding surfaced by this pass:** `codex.planner.models.PlanTelemetry` (`models.py:59-66`) already has a `concurrent_update_detected: bool = False` field — but grep confirms **no code path anywhere ever sets it to `True`**. `execute_query`'s mismatch handling takes an entirely different control-flow path (raising `GraphVersionMismatchError`, `planner.py:344`) that never touches a `RetrievalPlan`/`PlanTelemetry` instance at all — the exception is raised and propagates up, bypassing the very field that was seemingly built to carry this signal. This field is currently **dead** (always `False`, structurally unreachable as `True`), not a defect in D9 (D9's own actual mechanism, the exception, is correct and tested) but a genuine schema/design loose end that D11 will need to reconcile — flagged here, not resolved, since deciding *how* (catch the exception at a call site above `execute_query` and populate a Telemetry record from it? repurpose/remove the dead field? have `execute_query` itself change its signature to return a flagged result instead of raising, which would reopen D9's already-approved contract?) is exactly the kind of implementation-shape decision this audit must not make unilaterally.

**Determination: D11 depends on capability #1 only, which is already satisfied.** D11's Telemetry Store needs to be able to **record** a `CONCURRENT_UPDATE_DETECTED` event; the detection itself (`GraphVersionMismatchError`) already exists and needs no new code. D11 does **not** depend on capability #2 (ingestion-side writer conflict) — that remains correctly deferred to ADR-002/productionization, and D11 must not attempt to build it. The dead `PlanTelemetry.concurrent_update_detected` field is a **required human decision** before D11 implementation touches this specific event (see X.6/X.8), not a blocker for D11's existence.

### X.3 D7 decision

**Kept deferred.** Re-checked the exact condition the directive sets ("unless a deterministic, independently implementable source-graph option is now justified"): the provider-capability matrix is unchanged since the D7 STOP and its two re-audits (§K, §O Phase 3, truth-report §9, re-confirmed a fourth time this pass by re-reading each concrete adapter's `supported_capabilities` — `GitAdapter={HISTORY,CO_CHANGE}`, `SCIPAdapter={SYMBOL_DEFINITION,SYMBOL_REFERENCE,IMPLEMENTATION,TYPE_RELATIONSHIP}`, `CodeQLAdapter={DATA_FLOW}`, zero overlap, zero new capability). No new candidate provider was researched or proposed in this pass (none was in scope — the directive's own instruction is "do not implement D7"). **D7 remains DEFERRED — ARCHITECTURAL DECISION REQUIRED, ADR-006 still open.** Nothing about D11 (Telemetry) creates pressure to reopen it — Telemetry observes whichever providers happen to be registered; it has no opinion on which providers exist.

### X.4 TAD §84 open ADRs — prerequisite classification

Re-read `docs/TAD.md` §84's status table and §77's 17 candidates, and this document's own §D, directly. Classified against the reconstructed D11 scope (X.5):

| ADR | Relation to D11 | Classification |
|---|---|---|
| ADR-001 (Graph Storage) | None — D11 doesn't touch the graph store | Later productionization |
| ADR-002 (Evidence Storage) | Named directly by the ingestion-pipeline docstring (X.2) as the prerequisite for **ingestion-side** concurrent-writer detection, which is explicitly **not** part of D11's scope | Later productionization, not a D11 prerequisite |
| ADR-003 (Artifact Storage) | Related only if Artifact Store (TAD component #17) is bundled into D11 — see X.5's explicit scoping question; Telemetry Store itself needs no artifact storage | Not a D11 prerequisite for the Telemetry-only scope; would become one only if Artifact Store is bundled in, and even then only for the *technology* choice, not the interface/schema |
| ADR-004/005 (SCIP/CodeQL) | Already resolved, unrelated to D11 | N/A |
| ADR-006 (Sourcegraph/RepoGraph) | Unrelated — D11 doesn't depend on provider count (X.3) | Not a D11 prerequisite |
| ADR-007/008 (SLM/LLM selection) | Unrelated — D11 observes LLM call counts/tokens as already-typed data (`LLMGenerationResult`), never needs to know *which* model | Not a D11 prerequisite |
| ADR-009 (Embedding strategy) | Unrelated | Not a D11 prerequisite |
| ADR-010 (Search/Ranking engine) | Unrelated (infra-only remainder, formula already closed) | Not a D11 prerequisite |
| ADR-011 (Cache Technology) | Adjacent but distinct: TAD §54-55's Query-Level Cache (`codex.planner.cache`, already implemented, in-memory) is a different logical store than Telemetry Store (TAD §53 lists them as separate boxes); D11 may *observe* cache hits (TAD §65's own field list names "cache hits") but does not need cache *storage technology* decided to do so | Not a D11 prerequisite |
| ADR-012/013 | Closed/superseded, unrelated | N/A |
| ADR-014 (Runtime Adapter) | Unrelated | Not a D11 prerequisite |
| ADR-015 (API Protocol) | Relevant only to *soliciting* user feedback over a real API (HLRD §46/TAD §60) — no API/UI layer exists anywhere in Codex yet (correctly deferred, TAD §69/HLRD §54); D11 can define the feedback *record shape* and accept it as a typed input without needing a live API to collect it from | Not a D11 prerequisite; blocks only the (out-of-scope) live feedback-collection surface |
| ADR-016 (Auth) | Unrelated to a first in-memory Telemetry Store; becomes relevant only once Telemetry is exposed over a real API/multi-tenant deployment | Later productionization |
| ADR-017 (Deployment) | Unrelated | Later productionization |

**No explicit "Telemetry Storage Technology" ADR is named anywhere in TAD §77's 17 candidates** — a genuine omission in TAD's own ADR list (TAD §53's Storage Architecture diagram lists "Telemetry Store" as one of five logical storage boxes, but the corresponding ADR was never enumerated alongside ADR-001/002/003/011 for the other four). This is not a contradiction (nothing asserts the opposite), just an incompleteness in TAD's own bookkeeping — flagged here for the record, not silently patched into TAD's text (per the explicit "no document rewriting" rule) and not blocking, since **every store Codex has built so far (`InMemoryGraphStore`, `InMemoryEvidenceStore`, `PlanCache`) was built as an in-memory reference implementation behind a Protocol well before its own storage-technology ADR was resolved** — this is V1's established, repeated pattern, not a new one D11 would be inventing.

**Conclusion: zero of the 17 ADR candidates are blocking prerequisites for D11 as scoped in X.5.** All remain correctly deferred to later productionization (TAD §80 Phase 6), exactly as TAD §84 itself frames them ("🟡 ADR... where we want to be before writing the actual implementation" of *technology* — not of the interface).

### X.5 D11 boundary reconstruction

**Component identified:** TAD §6's component list (re-read directly, all 18 named) has exactly three components with no code anywhere in `src/codex/`: **#16 Telemetry Store**, **#17 Artifact Store**, **#18 Offline Calibration Pipeline**. TAD §59's own pipeline diagram (*"Production Telemetry → Training Dataset → Offline Evaluation → Calibration → Shadow → Canary → Production"*) makes #18 explicitly downstream of #16 — Offline Calibration needs telemetry data to exist first, and Codex has never run against a real repository/real query traffic, so no such data exists yet regardless. **#18 is therefore out of D11's reach by construction**, not by choice.

That leaves #16 (Telemetry Store) and #17 (Artifact Store) as the two candidates. They are related (TAD §53 lists both as "Storage Architecture" siblings, both currently `NOT_IMPLEMENTED`) but serve different purposes: #16 is a cross-cutting *observability sink* (TAD §75: *"Telemetry → all runtime components"* — the one component TAD explicitly authorizes to depend on everything else); #17 is a narrow *artifact-reference resolution service* (TAD §52: resolving `Evidence.raw_reference`/`ExtractionResult.raw_reference` URIs, whose scheme validation is already done, D1, but whose actual resolution service does not exist). **This audit reconstructs D11's primary scope as #16 (Telemetry Store)**, per TAD §75's dependency rule making it the more architecturally central "next" component, and flags #17 as a **closely related, smaller, separable component** — whether to fold it into D11 or treat it as a distinct D12 is an open scoping question for the human reviewer (X.8), not decided here.

**Inputs (TAD §65/HLRD §52, cross-checked, X.6):** already-typed data every existing D1-D10 module already produces — `query_id`/`query_identity` (`codex.planner.cache.compute_query_identity`, exists), `repository_id`+`graph_version` (`GraphVersion`, exists), `QueryContract` (D8, exists), `RetrievalPlan` including its own `PlanTelemetry` (D9, exists — see X.2's dead-field finding), candidate/entity/relationship counts (`EvidencePackage`, exists), LLM call count/tokens (`LLMGenerationResult`, exists — token counts specifically are not yet a populated field anywhere, see X.6), `ClaimVerification`/`ResynthesisResult`/`FinalAnswer` (D10, exists), `ProviderRunOutcome` (D4, exists, already carries failure/skip/partial detail), user feedback (HLRD §46/TAD §60 — no producer exists anywhere yet, since no API/UI layer exists; D11 can define the typed shape without a live source).

**Outputs:** a `TelemetryRecord` (or a small family of typed records, one per lifecycle stage) persisted into a new `TelemetryStore` Protocol + in-memory implementation, following the exact `EvidenceStore`/`GraphStore`/`PlanCache` pattern already established three times over. Read-only for every consumer — nothing in D1-D10 reads from it (TAD §59: production doesn't perform unrestricted online learning; feedback/telemetry data only flows into the *offline*, not-yet-built Calibration Pipeline, #18).

**Dependencies:** reads (never mutates) the already-public types of `codex.ontology`, `codex.graph`, `codex.query_understanding`, `codex.planner`, `codex.llm`, `codex.verification`, `codex.provider`/`codex.ingestion` (for `ProviderRunOutcome`) — the one component TAD §75 explicitly authorizes to depend on the entire rest of the system. No existing module would import the new `codex.telemetry` package (verified as a design constraint, not yet as code — see X.7).

**Invariants (derived from HLRD/TAD text, not invented):**
- Never mutates the canonical graph/evidence store (TAD §62, invariants #6-8, extended by the same reasoning already applied to Verification/LLM).
- Must not become *"an uncontrolled shadow copy of repository source code"* (HLRD §53, verbatim) — captures the structured/summary fields TAD §65/HLRD §52 actually name, never raw file content or full `EvidencePackage` payloads.
- Feedback is *"a learning signal, not automatically ground truth"* (HLRD §46, verbatim) — stored as reported, never validated or promoted to fact by Telemetry itself.
- *"Feedback does not directly modify the canonical graph"* (TAD §60) / *"Learned intelligence SHALL NOT directly modify canonical repository truth"* (HLRD INV-008) — Telemetry has no write path back into `codex.graph`/`codex.evidence`, structurally, the same enforcement pattern D10.9's boundary tests already established for the LLM.
- Retention is configurable (HLRD §53) — the *policy* is a later concern; a first in-memory implementation with no retention enforcement is not a violation of this invariant, matching how `InMemoryGraphStore`/`InMemoryEvidenceStore` also don't yet enforce HLRD §53's graph-version retention policy either.

**Explicit non-goals for D11:**
- Offline Calibration Pipeline (#18) — downstream, needs real data D11 will only just start producing.
- Real storage technology selection (no ADR exists or is needed to be resolved first, X.4) — an in-memory reference implementation only, exactly like every prior store.
- A live API/UI surface to actually collect user feedback — HLRD §54/TAD §69 explicitly defer external API protocol to a later technical-design deliverable; D11 can accept a typed feedback record as a parameter without inventing a collection endpoint.
- Using telemetry data to alter runtime behavior (ranking weights, routing thresholds, provider selection, etc.) at query time — explicitly forbidden by TAD §59 ("production runtime does not perform unrestricted online learning"); any such use is Offline Calibration's job, and only after Shadow/Canary validation per TAD §59's own lifecycle diagram.
- Ingestion-side concurrent-writer detection (X.2 capability #2) — correctly deferred to ADR-002.
- Reopening or modifying `RetrievalPlan.telemetry`/`PlanTelemetry`'s existing schema, `execute_query`'s exception-raising contract, or any other already-approved D9/D10 decision (X.2's dead-field finding is reported as a decision *point*, not pre-resolved here).

### X.6 Cross-document audit affecting D11

Compared HLRD §52's telemetry field list against TAD §65's directly, field by field (both re-read in full this pass):

| HLRD §52 field | TAD §65 field | Assessment |
|---|---|---|
| Query ID | `query_id` | Match |
| Repository / Revision | `repository_id` / `graph_version` | Match (TAD's `graph_version` subsumes revision via its composite key, TAD §19 — not a conflict, a more precise successor field) |
| Query intent / Complexity | `query_contract` | Match (TAD's field is the whole typed object, HLRD names two of its members individually — narrower-vs-broader framing, same pattern as every prior D8/D9 "Granularity note" in this document, not a contradiction) |
| Query plan | `retrieval_plan` | Match (same concept, TAD's name matches the actual `RetrievalPlan` type) |
| Providers used | `selected_providers` | Match |
| Provider failures | `provider_failures` | Match |
| Retrieval operations | *(not separately named)* | HLRD-only; `candidate counts`/`retrieval_plan` partially cover it — genuinely narrower in TAD, not contradicted |
| Candidate counts | `candidate counts` | Match |
| Ranking signals | *(not named)* | **HLRD-only field TAD's §65 list omits**, even though TAD §36-37 fully specifies named ranking signals and `codex.planner.ranking` already computes them — a real, if minor, granularity gap worth flagging for X.8, not a contradiction (TAD's list is a narrower V1 baseline, not an exclusion) |
| Selected subgraph / Coverage | *(not named)* | Same pattern — `codex.coverage`'s types already exist and could populate this, TAD's §65 list just doesn't name it explicitly |
| Model calls / Token usage | `LLM calls` / `LLM tokens` | Match |
| Latency | `latency` | Match |
| Verification status | `verification result` | Match |
| Answer outcome | *(not named)* | HLRD-only; D10.8's `AnswerDecision` is a distinct field from `VerificationStatus` that TAD §65 doesn't separately name — same minor granularity gap |
| User feedback | `user feedback` | Match |
| *(not in HLRD)* | `MSS size` | TAD-only, more V1-specific than HLRD's higher-level list — not a conflict |
| *(not in HLRD)* | `unsupported claims` / `contradictions` | TAD-only, reflects D10's richer verification taxonomy — not a conflict |
| *(not in HLRD)* | `cache hits` | TAD-only, reflects the now-implemented D9 cache — not a conflict |

**Conclusion: no genuine HLRD↔TAD contradiction regarding Telemetry's field list.** Every difference is the same "TAD narrows HLRD's broader/aspirational list to a concrete V1 set" pattern this document has already documented three times (D8's intent-vocabulary Granularity note, D9's ranking-signals Granularity note, and now this one) — never a case where the two documents assert incompatible things. The handful of HLRD-only fields (ranking signals, selected subgraph/coverage, answer outcome) are readily available from already-implemented types with zero invention required, but per this project's own established discipline (P.1 explicitly excluded `reasoning_requirement` from `QueryContract` for exactly this reason — "would invent a field TAD's own struct omits") **whether to include them is a documented-interpretation decision for D11's actual implementation, not resolved here.**

Checked D1-D10 implementation against both documents for anything already telemetry-shaped that could conflict: `PlanTelemetry` (D9) and `PlanCache`'s `cache_hit` field are the only telemetry-*shaped* data structures that exist, and neither is a persistence layer — both are ephemeral, per-call, in-memory fields on already-returned objects (`RetrievalPlan.telemetry`), fully consistent with feeding into, not competing with, a future `TelemetryStore`. No conflicting or duplicate telemetry concept found anywhere in the codebase.

### X.7 Dependency-direction confirmation

Re-derived the same import-graph extraction method as the truth report's §7 (`grep -rhoE "^from codex\.[a-z_]+" src/codex/<pkg>/*.py`, all 13 existing top-level packages) — unchanged since that audit (no source file has been added to or removed from any package's import list by the hardening pass, confirmed by re-running the exact same command this pass and diffing against the truth report's recorded graph: identical). The existing chain remains a strict DAG with `verification` as its current terminal node (query_understanding → planner → llm → verification, TAD §1's authoritative pipeline order, HLRD §4's principle diagram).

A new `codex.telemetry` package, as scoped in X.5, would sit as a **strict successor** of every existing node — importing from `ontology`, `graph`, `query_understanding`, `planner`, `llm`, `verification`, `provider`/`ingestion` (for `ProviderRunOutcome`) to type its capture functions, while **zero existing package would import `codex.telemetry` back** (nothing in the D1-D10 chain has any reason to read telemetry data — TAD §59's "no unrestricted online learning" invariant forbids exactly that feedback loop at runtime). This preserves the DAG property by construction — telemetry can only be *appended* to the dependency graph, never *inserted into* the existing Query Understanding → Planner/Retrieval → EvidencePackage → LLM → Verification → Final Answer chain, because nothing in that chain would ever need to call into it to produce its own output. **Confirmed: D11 can be added without bypassing or reordering any existing stage.**

### X.8 Required human decisions (none pre-resolved by this audit)

1. **Scope: Telemetry Store alone, or Telemetry Store + Artifact Store together as D11?** (X.5) Both are small, related, currently-unimplemented storage siblings; TAD's own component numbering (#16 vs #17) and differing purposes support treating them separately, but nothing forbids bundling them either.
2. **How to bridge `GraphVersionMismatchError` into a `CONCURRENT_UPDATE_DETECTED` telemetry record, and what to do with the currently-dead `PlanTelemetry.concurrent_update_detected` field** (X.2) — catch-at-call-site, repurpose the field, or another shape; this touches D9's existing (approved, tested) contract at the margin and should not be decided silently.
3. **Whether to include the HLRD-only telemetry fields TAD §65 doesn't name** (ranking signals, selected subgraph/coverage, answer outcome — X.6) in D11's actual record schema, consistent with this project's "don't invent a field neither document's authoritative struct lists" discipline, or treat TAD §65's list as the closed baseline.
4. **Whether/when to also implement real feedback *collection*** (requires the still-deferred API/UI layer, ADR-015) versus only defining the feedback record *shape* now and leaving collection for later (X.5's non-goals).

None of these block starting D11's scoping or design — they are exactly the class of "flag with maximum visibility, do not silently resolve" decision points this project has handled the same way at every prior D-phase boundary (ADR-018, the D9 latency-budget analogy, D10 Decisions 1-4).

### X.9 Verdict

**GO WITH CONDITIONS.** No genuine HLRD/TAD contradiction was found (X.6). No blocking architectural decision prevents scoping or beginning D11 (X.4). D7 correctly stays deferred and does not gate D11 (X.3). TAD §33's gap is non-blocking for a fourth consecutive audit (X.1). TAD §55/§64's gap is precisely characterized and shown to be satisfiable within D11's own scope, with one genuinely out-of-scope sub-case (ingestion-side writer conflict) correctly excluded (X.2). D11's boundary — Telemetry Store, TAD component #16, a strict DAG successor of the entire existing pipeline with zero back-edges (X.7) — is reconstructed directly from TAD §6/§59/§65/§75 and HLRD §44-46/§52-53 text, not invented. Four items (X.8) require an explicit human decision before implementation proceeds; none are blocking in the sense of preventing D11 from being scoped, only in the sense of needing a decision this audit is not authorized to make. **D11 was not implemented.** No production code, test code, or HLRD/TAD text was written or modified in this phase. Stopping here for review.

---

## Y. D11 Implementation — Telemetry Store — 2026-08-31

Per the "D11 decisions approved" directive, closing X.8's four required decisions and implementing D11 accordingly:

1. **Scope: Telemetry Store only.** Artifact Store (TAD component #17) not implemented.
2. **`GraphVersionMismatchError` remains D9's detection mechanism.** D11 only maps an already-raised exception into a recordable event, at the (not-yet-existing) orchestration boundary — never re-derives or re-checks a version match itself.
3. **TAD §65 is canonical.** No HLRD-only field (ranking signals, selected subgraph/coverage, "answer outcome" as a field distinct from `VerificationStatus`) was added.
4. **Record/store infrastructure only.** No feedback-collection endpoint, no calibration logic.

Re-read TAD §56-65 and HLRD's telemetry sections (§44-46, §52-53) directly before writing any code (unchanged since every prior re-read this session, confirmed via `git log`), plus this document's own §X in full. **No cross-document contradiction found** — re-confirms X.6's field-by-field comparison with no new discrepancy surfaced during implementation.

### Y.1 Design — Protocol/data model first, `codex.telemetry`

Four new modules, `src/codex/telemetry/{models,store,mapping,__init__}.py`, following the exact `EvidenceStore`/`GraphStore` in-memory-behind-a-Protocol precedent (X.4's own justification for why no ADR blocks this):

- **`models.py`**: `FailureCode` (TAD §64's eleven codes, verbatim, no more/fewer), `FeedbackKind` (TAD §60's six examples, verbatim — not HLRD §46's slightly different superset, per decision 3's discipline extended to the feedback taxonomy too), `FeedbackRecord` (the record *shape* only, decision 4), `QueryTelemetryEvent` (TAD §65's field list exactly — `query_id`, `repository_id`, `graph_version` id, `query_contract`, `selected_providers`, `retrieval_plan`, `candidate_count`/`mss_size`, `llm_calls`/`llm_tokens`, `latency_ms`, `verification_result`, `unsupported_claim_count`/`contradiction_count`, `cache_hit`, `provider_failure_count`, `user_feedback`), `FailureTelemetryEvent` (TAD §64's generic per-failure record). Both event types have a `.build()` factory computing a **deterministic `event_id`** — a SHA-256 hash of the event's own defining fields (matching `build_canonical_id`'s precedent), never a random UUID: two events built from identical inputs, including an identical explicit `now`, are byte-for-byte identical.
- **`store.py`**: `TelemetryStore` Protocol (`record_query_event`/`record_failure_event`/`query_events`/`failure_events`) + `InMemoryTelemetryStore`. **Append-only by construction** — no update/delete/clear method exists anywhere on the class (proven by `test_no_update_or_delete_method_exists`, which enumerates the class's entire public surface); `query_events`/`failure_events` return a fresh filtered list each call, so mutating a returned list never affects the store (matching `InMemoryEvidenceStore`'s existing `get_evidence_for` behavior exactly).
- **`mapping.py`**: `failure_event_from_graph_version_mismatch(exc, *, plan, query_id, now=None) -> FailureTelemetryEvent` — the one explicitly-approved mapping (decision 2). Takes an *already-raised* `GraphVersionMismatchError` and the `RetrievalPlan` D9 already computed; reads already-known fields (`plan.graph_version.repository_id`/`.version_id`, `str(exc)`) into a `FailureTelemetryEvent(code=CONCURRENT_UPDATE_DETECTED, ...)`. Performs **no comparison of its own** — it never touches `graph.version`/`plan.graph_version` to decide whether they differ; that decision was already made by `execute_query` (`planner.py:343-348`, unmodified) before this function is ever called. This satisfies "Telemetry must not own detection/recovery" by construction, not by convention.

**Field-list honesty, documented rather than silently smoothed over** (`models.py`'s own docstring): TAD §65 names "candidate counts" and "MSS size" as two separate capture targets, but D9's `execute_query()` (unmodified, not reopened) exposes only the final `EvidencePackage` — no separate pre-MSS candidate set is a public artifact anywhere in D9. `candidate_count`/`mss_size` are therefore computed from the same source in this V1 implementation; both TAD-named fields are kept (not silently merged into one), with the reason recorded in the docstring rather than hidden. `llm_tokens`/`latency_ms` are `int | None`/`float | None` because no wall-clock timing or token-count instrumentation exists anywhere in D1-D10 (re-confirmed by grep this pass) — a caller with a real measurement supplies it; nothing here fabricates a value.

**Provider/version telemetry** (a required test category, per the directive): satisfied without inventing a new field — `QueryTelemetryEvent.retrieval_plan` embeds the full `RetrievalPlan`, which itself embeds the full `GraphVersion`, whose `provider_versions: dict[str, str]` (already existing since Phase 1) is therefore reachable at `event.retrieval_plan.graph_version.provider_versions` — proven by `test_build_carries_selected_providers_and_full_retrieval_plan`.

### Y.2 Dependency-direction and non-interference, enforced by tests

`tests/test_telemetry_boundaries.py` (5 tests) programmatically confirms, via `ast`-based import parsing (the same mechanism `test_planner_boundaries.py`/`test_qu_boundaries.py` already established):

- **Nothing upstream depends on telemetry.** All fourteen existing top-level `codex` packages (`ontology` through `verification`) are scanned; none imports `codex.telemetry` (`test_no_upstream_package_imports_telemetry`).
- **Telemetry legitimately depends on the pipeline** (TAD §75's explicit authorization) — confirmed as a real, exercised asymmetry (`codex.telemetry` genuinely imports from `codex.planner`/`codex.query_understanding`/`codex.verification`), not a coincidence of an unused package (`test_telemetry_may_depend_on_the_rest_of_the_pipeline`).
- **No mutation-surface import** — `codex.telemetry` never imports `codex.graph.memory_store`/`codex.evidence.store`/`codex.ingestion.pipeline` (`test_telemetry_never_imports_a_graph_or_evidence_mutation_api`); `InMemoryTelemetryStore` has no `upsert_entity`/`upsert_relationship`/`add_evidence`/`publish`-shaped method (`test_telemetry_store_has_no_write_path_back_into_the_graph`).

`tests/test_telemetry_integration.py::test_recording_telemetry_does_not_change_the_already_computed_final_answer` proves the same property behaviorally, not just structurally: a `FinalAnswer` computed from a real D10 run is compared before and after recording a `QueryTelemetryEvent` about it — byte-for-byte identical, confirming telemetry cannot retroactively (or prospectively) alter ranking, verification, answers, graph state, or provider selection.

### Y.3 Tests

34 new tests across four test files, 100% coverage on all four new `codex.telemetry` modules:

| File | Count | Covers |
|---|---|---|
| `tests/test_telemetry_models.py` | 13 | Normal event construction, deterministic `event_id` (identical inputs → identical id; different `query_id`/`now` → different id), provenance-field population from `GraphVersion`, conservative defaults, all-11-TAD-§64-codes representable, TAD-§60-only feedback vocabulary |
| `tests/test_telemetry_store.py` | 11 | Normal recording/retrieval, empty-store behavior, filtering by `repository_id`/`query_id`/`code`, query/failure logs independent, append-only (multi-event ordering preserved, mutating a returned list doesn't affect the store, no update/delete method exists) |
| `tests/test_telemetry_integration.py` | 5 | A **real** D8→D9→D10 pipeline result recorded as `QueryTelemetryEvent` (not synthetic fixtures); a real `PARTIAL`-cohort ingestion outcome recorded generically as `FailureTelemetryEvent`; the real `GraphVersionMismatchError` (D9, unmodified, actually raised via a real concurrent-update scenario — the same setup `test_planner_graph_version.py`'s own pre-existing test uses) mapped to a `CONCURRENT_UPDATE_DETECTED` event; confirmation the exception still propagates after mapping is available (D9's refusal-to-proceed behavior unchanged); confirmation recording telemetry never alters an already-computed `FinalAnswer` |
| `tests/test_telemetry_boundaries.py` | 5 | Dependency-direction proof (Y.2) |
| `tests/telemetry_fixtures.py` | — | Shared, non-test-prefixed fixture builders (`make_graph_version`/`make_contract`/`make_plan`), matching the `planner_fixtures.py`/`llm_fixtures.py`/`symbol_level_fixtures.py` precedent rather than importing between `test_*.py` files |

Every directive-required test category is covered: normal event recording, deterministic event structure, repository/query/graph-version provenance, provider/version telemetry, partial/failure events, `GraphVersionMismatchError` telemetry mapping, append-only behavior, no correctness dependency on telemetry.

### Y.4 Validation

Fresh `.venv-work` run: **697/697 tests passing** (663 pre-existing + 34 new: 13 model + 11 store + 5 integration + 5 boundary), **99% project-wide coverage** (`telemetry/models.py`, `telemetry/store.py`, `telemetry/mapping.py`, `telemetry/__init__.py` all 100%; the only sub-100% module remains `repository/manager.py`, pre-existing, unrelated), `ruff check src/ tests/` clean, `mypy src/` clean (69 source files, up from 65). No new pip dependency. No ADR created (X.4 already established none are prerequisites; none were touched). No HLRD/TAD text modified. No D1-D10 module modified — confirmed by `git status`/`git diff --stat` showing only new files under `src/codex/telemetry/` and `tests/`.

**No genuine HLRD/TAD contradiction found** during implementation, beyond what X.6 already characterized.

**D11 — COMPLETE for the approved scope (Telemetry Store only).** Artifact Store, Offline Calibration Pipeline, D7, and production feedback collection were explicitly not implemented, per the directive's own closing instruction. **D12 not started.** Stopping here for review.

---

## Z. D12 Pre-Implementation Audit — 2026-08-31

Per the explicit "Start D12 Pre-Implementation Audit only — do NOT implement yet" directive. **Audit-only: no production code was written or modified in this phase.** Re-read `docs/TAD.md` and `docs/HLRD.md` in full (unchanged since the SourceLocation-closure commit, `git log` re-confirmed), this document's §D/§I/§X/§Y, `docs/architecture-truth-report.md`, `PROGRESS.md`, and `docs/resources.md` directly. No standalone ADR files exist (unchanged finding, re-confirmed).

**Every "artifact" occurrence in TAD.md, found by direct grep, all read in full context this pass:** §6 (component #17), §52 (the defining section), §53 (Storage Architecture diagram), §54 (Artifact Cache), §61 (Security Boundary), §77 (ADR-003), §78 (V1 Component Priority — "Mandatory"). **Every "artifact" occurrence in HLRD.md: zero.** HLRD never mentions Artifact Store, "raw_reference", or any equivalent concept anywhere — confirmed by a full-file grep, not assumed. This single fact resolves item 8 up front: there is no possible HLRD↔TAD contradiction about Artifact Store, because HLRD is entirely silent on it, not because the two documents agree or disagree on anything.

### Z.1 Purpose, inputs, outputs, lifecycle, immutability, provenance

**Purpose (TAD §52, verbatim):** *"Raw provider artifacts are retained separately. `raw_reference` SHALL be a resolvable URI. ... The Codex Artifact Retrieval Service resolves these references."* A pure store-and-resolve capability for opaque, provider-produced byte content, addressed by a URI already required to be well-formed (one of `artifact://`, `s3://`, `file://` — TAD §52's own three examples).

**Inputs:** whatever a provider adapter's `extract()`/`normalize()` call already decided to reference — either a whole extraction run's artifact (`ExtractionResult.raw_reference`, TAD §9/§17/§52, currently always `None` in D3/D5/D6) or a specific evidence record's artifact slice (`Evidence.raw_reference`, TAD §15-16/§52, currently set only by D6 — see Z.5). Both fields, and their scheme validation (`RAW_REFERENCE_SCHEMES`/`validate_raw_reference`), already exist and are unmodified by this audit.

**Outputs:** raw bytes (or an equivalent readable handle) given a reference string. TAD specifies no schema for the *stored* value beyond "raw" — deliberately opaque, matching "raw provider artifacts" (not a structured, Codex-typed record like `Evidence`).

**Lifecycle:** TAD gives no explicit creation/retention/expiry lifecycle for artifacts specifically. The closest authoritative statement is HLRD §53 (Data Retention), which names Telemetry/graph-version retention as "configurable" and states *"Telemetry SHALL NOT become an uncontrolled shadow copy of repository source code"* — written for Telemetry, not Artifact Store (HLRD never names the latter), but the same underlying caution is directly relevant by extension: whatever D12 stores must not become an uncontrolled duplicate of repository source either. This is recorded as an informing analogy, not a literal requirement transplanted from one section to another without saying so.

**Immutability:** **Not stated by TAD anywhere.** This matters because the two closest existing precedents in this codebase *disagree with each other*: `InMemoryEvidenceStore.add_evidence()` is a dict-keyed **upsert** (a second write with the same `evidence_id` silently replaces the first); `InMemoryTelemetryStore` (D11, this project's own newest precedent) is deliberately **append-only**, with no update path at all. TAD's own invariant #4 ("graph versions are immutable once published") is scoped explicitly to `GraphVersion`, never generalized to every store. **Whether a second `store()` call for the same reference should overwrite, refuse, or be treated as a no-op is a genuine, undecided design question** — flagged for a human decision (Z.8), not silently resolved either way.

**Provenance:** Already fully satisfied structurally, requiring no new mechanism. `raw_reference` always lives on the same record (`Evidence`, or an `ExtractionResult`/`EvidenceCohort` pair) that already carries `provider`, `provider_version`, `snapshot_id`, `source_revision`, and `observed_at` (TAD §15, §17 — all already implemented, unchanged since Phase 1/D1). An artifact's provenance is therefore always recoverable through its containing record; Artifact Store itself needs no separate versioning or provenance scheme of its own (this directly answers item 4).

### Z.2 Relationship to EvidenceStore, GraphStore, TelemetryStore, EvidencePackage

- **EvidenceStore**: complementary, not overlapping. `Evidence`/`EvidenceCohort` are structured, canonical records; a `raw_reference` on one of them is an opaque pointer *out of* that structured world into Artifact Store's raw-byte world. Neither store's data lives inside the other.
- **GraphStore**: no direct relationship. `RepositorySymbol`/`CanonicalRelationship` never carry `raw_reference` directly — only `Evidence` does, one layer removed.
- **TelemetryStore (D11)**: an unrelated sibling, confirmed by TAD §53's own diagram (five separate boxes: Graph/Evidence/Artifact/Telemetry/Cache) and by TAD §65's telemetry field list never mentioning "artifact" anywhere (re-confirmed by grep this pass). No coupling exists or is implied.
- **EvidencePackage (D9's LLM-facing output)**: a real, already-existing fact worth stating precisely — `EvidencePackage.evidence: list[Evidence]` already structurally carries whatever `raw_reference` strings the underlying `Evidence` records happen to have set. Today that means: whenever a package includes CodeQL-sourced evidence, it already contains `artifact://sarif/...` strings, as inert data, exactly like every other string field the LLM already sees (`evidence_id`, `provider`, `snapshot_id`). This is unchanged by D12 and is not a new exposure — the string was already there; only *resolving* it into content is new capability D12 would add, and (Z.6) that resolution capability must never become reachable from `codex.llm`.

### Z.3 Artifact content type: raw provider artifacts, not repository source

TAD's own word choice is precise and was checked directly, not paraphrased: *"Raw **provider** artifacts"* (§52) — the output a provider adapter's `extract()` call produced or consumed (a whole SARIF file, a whole SCIP index, and by extension any future provider's native format), not repository source files, and not a general-purpose source-code cache. HLRD gives no artifact-specific guidance (it is silent, Z above), but its closest analogous statement (§53, "SHALL NOT become an uncontrolled shadow copy of repository source code," written for Telemetry) is directly relevant by extension: it reinforces, rather than contradicts, reading TAD §52's "provider artifacts" narrowly. **Conclusion: D12's V1 scope is provider-native-format artifacts (SARIF/SCIP-shaped, generalizable to any future provider), not raw repository source file content** — consistent with, not contradicting, HLRD's one nearby caution.

### Z.4 Identity/versioning relationship to source_revision/snapshot_id/graph_version/provider_version

Answered fully in Z.1's "Provenance" paragraph: no new identity scheme is needed. A `raw_reference` is an opaque string (scheme-validated only); the version context it belongs to is always already available on its containing `Evidence`/`ExtractionResult` record. `GraphVersion` never carries a `raw_reference` itself (confirmed: `GraphVersion`'s fields are `version_id`/`repository_id`/`repository_revision`/`provider_versions`/`schema_version`/`policy_version`/`created_at`/`published` — no artifact field, unchanged since Phase 1) — Artifact Store is reached only by following an `Evidence` record, never directly from a `GraphVersion`.

### Z.5 D5/D6 artifacts already requiring missing storage/retrieval semantics — a concrete, not hypothetical, gap

Checked directly, not assumed: every `ExtractionResult(...)` construction site in `git_adapter.py`, `scip_adapter.py`, and `codeql_adapter.py`'s own `extract()` methods sets `raw_reference=None` — none of the three adapters currently sets a whole-run artifact reference. At the per-`Evidence` level, however, **`codeql_adapter.py` already sets a real one**: every path-problem `Evidence` record gets `raw_reference=f"artifact://sarif/{flow.run_index}/{flow.result_index}"` (`codeql_adapter.py:578`), and the adapter's own module docstring, written at D6-implementation time, already says so explicitly: *"Every path-problem `Evidence` record's `raw_reference` points at `artifact://sarif/<run index>/<result index>` (resolvable later via the Artifact Store, TAD §52)."* D6's own code, shipped before D12 was ever scoped, already anticipated this exact gap by name.

**Determination:** no current runtime path *calls* a resolution function (none exists, so nothing can fail) — the full 697-test suite is green with zero dependency on artifact resolution. But real data already exists (D6's `artifact://sarif/...` strings, produced by every CodeQL path-problem test fixture) that is currently a dead end: TAD §51's traceability chain (Claim → Relationship → Evidence → Provider → Snapshot → Source Location) has one more link TAD §52 adds beyond it (raw artifact bytes), and for CodeQL evidence specifically that link is already asserted in data but not yet resolvable. This is why item 9's answer (Z.9) is nuanced rather than a flat yes/no: **not required for correctness of anything today; already partially manifested as unresolvable data, not purely hypothetical.**

### Z.6 Security/trust boundary

Two requirements, both directly grounded in TAD text, neither invented:

1. **Artifacts are untrusted, provider-produced content and must never be executed or interpreted as instructions** — the same "content is data, never instructions" discipline TAD/HLRD's LLM-boundary principle and D10.9's own boundary tests already established for query text and evidence fields, extended by the same reasoning to artifact bytes (a SARIF/SCIP file is, in principle, adversarially craftable the same way any other provider input is). D12's store/resolution capability must be purely byte-in/byte-out — it must never parse, execute, or semantically interpret what it stores.
2. **The LLM must never gain unrestricted access to artifact storage** — TAD §61, verbatim: *"The LLM must not have unrestricted access to: filesystem, repository, provider credentials, graph mutation APIs, **artifact storage**. It receives only the approved EvidencePackage."* This means the Artifact Retrieval Service (however it is eventually exposed) must never be reachable from `codex.llm` — the identical DAG-boundary pattern already tested for D10.9 (`test_llm_package_never_imports_provider_or_registry_modules`) and D11 (`test_telemetry_never_imports_a_graph_or_evidence_mutation_api`), extendable to D12 without inventing a new mechanism.

### Z.7 Protocol + in-memory implementation sufficiency

**Yes, clearly sufficient**, for the same reason established four times over in this project (`InMemoryGraphStore`, `InMemoryEvidenceStore`, `PlanCache`, `InMemoryTelemetryStore` — D11's own §X.4 already generalized this precedent explicitly). Storage *technology* (ADR-003, still open — see Z.10) is separable from storage *interface*; every prior store was built against a stable Protocol well before its own technology ADR was resolved, without ever needing to be reopened when that ADR eventually is. D12 should follow the identical shape: an `ArtifactStore` Protocol (`put`/`store` a reference→bytes mapping, `get`/`resolve` bytes by reference) + `InMemoryArtifactStore`.

### Z.8 Cross-document audit and required human decisions

**No genuine HLRD/TAD contradiction found.** HLRD is silent (not contradictory) on Artifact Store entirely (Z above). Within TAD itself, all ten "artifact" occurrences are mutually consistent; the only wording variance is TAD §52 naming *"the Codex Artifact Retrieval Service"* while §6/§53/§77/§78 all say *"Artifact Store"* — read as the same one logical component described by its storage aspect (§53/§77/§78's naming) and its resolution aspect (§52's own sentence) together, not two competing components. Every D1-D11 code reference to this gap (D1's scheme validation, D6's forward-reference, this document's own §H/§O-era ordered-plan notes, §X.5's D11-scoping discussion) is consistent with this reading — no contradiction found anywhere across HLRD, TAD, or the existing implementation.

**Required human decisions (none pre-resolved by this audit, matching D11's own §X.8 precedent):**

1. **Overwrite semantics.** Should a second `store()` call for an already-used reference overwrite (matching `EvidenceStore`'s upsert precedent) or refuse/no-op (matching `TelemetryStore`'s append-only precedent, this project's own newest store)? TAD is silent; the two existing precedents disagree (Z.1).
2. **Reference-string structure.** Should the Protocol treat `raw_reference` as a fully opaque string (simplest, most TAD-literal — TAD gives no fragment-parsing rule) or formalize fragment-addressing (`artifact://.../#offset`, one of TAD §52's own three examples) as a first-class concept? D6's own existing convention (`artifact://sarif/<run>/<result>`, path-segment style, not fragment style) already works as an opaque string today with zero parsing — recommend opaque-string-only as the conformant default, but this is a real design choice worth stating rather than assuming.
3. **Retention/lifecycle policy.** Genuinely undefined by TAD/HLRD (Z.1); the directive explicitly forbids inventing one. Should the Protocol even expose a retention-related parameter (e.g., an optional, currently-no-op TTL) for future extensibility, or omit the concept entirely until a real policy exists? Recommend omitting entirely — a knob nobody specified is itself a kind of invention.
4. **Whether D3/D5 should ever be revisited to populate `raw_reference`.** Not part of D12's own scope regardless of the answer — D12 builds the store/resolution *capability*, which works for any future `raw_reference` value without requiring any adapter change. Retrofitting `GitAdapter`/`SCIPAdapter` to start setting one would mean reopening D3/D5, explicitly forbidden by this directive; flagged only so a future directive doesn't silently conflate "build Artifact Store" with "make every adapter populate it."

### Z.9 Dependency and requirement summary (items 9-10)

**Item 9 — required by any existing runtime path, or purely infrastructure:** Purely infrastructure for correctness (nothing in D1-D11 calls a resolution function, none exists, the full suite is green without it) — but not purely hypothetical, since D6 already emits real, currently-unresolvable `artifact://` references (Z.5). Matches TAD §78's own framing: Artifact Store is listed "Mandatory" for V1 (alongside Telemetry, and alongside "At least one Repository Graph Adapter" — D7, which this project has already knowingly deferred via an approved STOP) — "Mandatory" in TAD's own usage means "V1 is incomplete without it eventually," not "blocks every other component until built," the same reading D7's own precedent already established.

**Item 10 — dependency on ADR-002/storage decisions:** None, for the interface/schema scope reconstructed here (Z.7). ADR-002 (Evidence Storage Technology) is unrelated to Artifact Store specifically — TAD §77 lists ADR-002 (Evidence) and ADR-003 (Artifact) as two separate candidate decisions, not one; D12 depends on neither being resolved, matching D11's own zero-ADR-prerequisite precedent (§X.4) exactly.

**Dependency-direction note, established for design (not yet implemented):** the same DAG-safety property this project has now proven twice (D9/D10's boundary tests, D11's `test_telemetry_boundaries.py`) should extend to D12: nothing in D1-D11 should ever import a future `codex.artifact` package, and `codex.artifact` itself must never be importable from `codex.llm` specifically (Z.6). This is a design requirement for whenever D12 is implemented, not something built or tested in this audit-only pass.

### Z.10 Verdict

**GO WITH CONDITIONS.** No genuine HLRD/TAD contradiction was found (HLRD is silent, not contradictory, on Artifact Store entirely). No blocking architectural decision prevents scoping or beginning D12 — zero of TAD's ADR candidates (including ADR-002/ADR-003 specifically, item 10) are prerequisites for the Protocol/interface scope this audit reconstructs (Z.7, Z.9). D12's contract is reconstructed directly from TAD §6/§52-54/§61/§77-78 text: a store/resolve Protocol for opaque, scheme-validated, provider-produced raw artifact bytes, with provenance already satisfied structurally through the containing `Evidence`/`ExtractionResult` record and requiring no new identity scheme (Z.1, Z.4). A concrete, already-existing consumer gap was found and precisely characterized, not left vague: D6's CodeQL adapter already emits real `artifact://sarif/...` references with no resolution service to answer them (Z.5). Four items (Z.8) require an explicit human decision before implementation proceeds — overwrite semantics, reference-string structure, retention-policy scope, and confirming D3/D5 stay untouched — none blocking in the sense of preventing D12 from being scoped, only in the sense of needing a decision this audit is not authorized to make. **D12 was not implemented.** No production code was written or modified in this phase. Stopping here for review.

---

## AA. D12 Implementation — Artifact Store — 2026-08-31

Per the "D12 decisions are approved" directive, closing Z.8's four decisions:

1. **Overwrite: immutable.** Same reference + same bytes = idempotent success. Same reference + different bytes = deterministic `ArtifactConflictError`. Never a silent overwrite.
2. **Reference: opaque.** D1's existing `validate_raw_reference`/`RAW_REFERENCE_SCHEMES` scheme validation is reused unchanged; no fragment/path-segment parsing or normalization was added.
3. **Retention: none.** No TTL, eviction, or deletion method exists anywhere on the Protocol.
4. (Item 4 of Z.8 — confirming D3/D5 stay untouched — is honored by construction: see AA.1.)

Re-read TAD §52-54, §61, §77-78 and this document's own §Z directly before writing any code (unchanged since the prior pass, `git log` re-confirmed). **No cross-document contradiction found** — re-confirms Z.8/§Z's own conclusion; nothing new surfaced during implementation.

### AA.1 Design — `codex.artifact`

Two new files, `src/codex/artifact/{store,__init__}.py`, following the exact `EvidenceStore`/`GraphStore`/`TelemetryStore` in-memory-behind-a-Protocol precedent:

- **`ArtifactStore` Protocol**: `store(reference: str, content: bytes) -> None`, `resolve(reference: str) -> bytes | None`. Nothing else — no delete, no list, no metadata query, matching decision 3 (no retention/lifecycle surface) literally.
- **`InMemoryArtifactStore`**: a single `dict[str, bytes]`. `store()` calls D1's own `validate_raw_reference()` unchanged (imported from `codex.evidence.model`, the only cross-package import this module needs) before touching the dict; a reference already holding identical bytes is a no-op success; a reference already holding different bytes raises `ArtifactConflictError` (a `ValueError` subclass, matching this codebase's existing exception-hierarchy convention — `GraphVersionMismatchError` also subclasses `ValueError`) naming the offending reference. `resolve()` is a plain dict lookup, matching `EvidenceStore.get_evidence`'s precedent (no read-side validation).
- **Content is always opaque bytes.** Nothing in `codex.artifact` parses, decodes, or interprets what it stores — proven behaviorally (AA.3), not just asserted.

**D12's own scope is deliberately the store/resolve *capability* only — `src/codex/provider/{git_adapter,scip_adapter,codeql_adapter}.py` were not opened, read for modification, or changed in any way.** This directly honors decision 4/Z.8 item 4 ("confirming D3/D5 stay untouched") by construction: nothing in `codex.artifact` depends on, imports, or requires a provider-adapter change, and none was made. Compatibility with D6's already-real `raw_reference` output is proven purely by an integration test constructing real adapter output and feeding it to the new store (AA.3) — never by touching the adapter itself.

### AA.2 Dependency-direction and security boundary, enforced by tests

`tests/test_artifact_boundaries.py` (5 tests), the same `ast`-based mechanism `test_planner_boundaries.py`/`test_telemetry_boundaries.py` already established:

- **Nothing upstream depends on it.** All fifteen existing top-level `codex` packages (`ontology` through `telemetry`, now including D11) are scanned; none imports `codex.artifact`.
- **`codex.llm` specifically never imports it** — checked directly, not merely inferred from the generic upstream sweep, since TAD §61 names this exact boundary by name (*"The LLM must not have unrestricted access to... artifact storage"*).
- **Minimal dependency surface** — `codex.artifact`'s own imports are limited to `codex.evidence.model` (for `validate_raw_reference`) — no pipeline type (`Evidence`, `RetrievalPlan`, `GraphVersion`, ...) is imported at all, since the store deals only in opaque `reference: str`/`content: bytes`, unlike `codex.telemetry` which legitimately needs pipeline types for its own field typing.
- **No write path into the graph/evidence store** — `InMemoryArtifactStore` has no `upsert_entity`/`upsert_relationship`/`add_evidence`/`publish`-shaped method.

### AA.3 Tests

21 new test functions across three files (23 test executions — `test_each_d1_scheme_is_accepted` is parametrized ×3 schemes), 100% coverage on both new `codex.artifact` modules:

| File | Functions (executions) | Covers |
|---|---|---|
| `tests/test_artifact_store.py` | 13 (15) | Normal store/resolve round trip (including full binary byte range), unknown-reference miss, all three D1 schemes accepted (parametrized), invalid scheme rejected with no partial write, idempotent re-store of identical content, deterministic `ArtifactConflictError` on differing content (never silently overwriting the original), no delete/update/retention method exists, content never parsed/executed (code-, JSON-, script-, and raw-binary-shaped payloads all round-trip byte-for-byte) |
| `tests/test_artifact_integration.py` | 3 (3) | The real, **unmodified** `CodeQLAdapter` run against the real `path-problem.sarif` fixture, producing the real `Evidence.raw_reference == "artifact://sarif/0/0"` already proven by `test_codeql_adapter.py`; the real SARIF file's bytes stored and resolved under that exact reference; a second identical run proven idempotent (matching D4's own repeated-ingestion precedent); a genuinely different real artifact (`valid-sarif.sarif`'s bytes) under the same reference proven to raise `ArtifactConflictError` |
| `tests/test_artifact_boundaries.py` | 5 (5) | Dependency-direction and TAD §61 security-boundary proof (AA.2) |

Every directive-required test category is covered: normal store/resolve, idempotent-on-identical-content, deterministic-conflict-on-differing-content, D1-scheme-validation reuse, no invented parsing, no retention surface, real-CodeQL-reference compatibility, dependency-direction/security boundaries.

### AA.4 Validation

Fresh `.venv-work` run: **720/720 tests passing** (697 pre-existing + 23 new executions from 21 new test functions: 13 store [15 executions] + 3 integration + 5 boundary), **99% project-wide coverage** (`artifact/store.py`/`artifact/__init__.py` both 100%; the only sub-100% module remains `repository/manager.py`, pre-existing, unrelated), `ruff check src/ tests/` clean, `mypy src/` clean (71 source files, up from 69). No new pip dependency. No ADR created (Z.4/Z.9/Z.10 already established none are prerequisites; none were touched). No HLRD/TAD text modified. **Zero D1-D11 modules modified** — confirmed by `git status`/`git diff --stat` showing only new files under `src/codex/artifact/` and `tests/`.

**No genuine HLRD/TAD contradiction found** during implementation, beyond what §Z already characterized.

**D12 — COMPLETE for the approved scope.** Offline Calibration Pipeline (TAD component #18), D7, and production feedback collection remain explicitly not implemented, per the directive's own closing instruction. **D13 not started.** Stopping here for review.
