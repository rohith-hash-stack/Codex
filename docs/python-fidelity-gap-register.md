# Python Index Fidelity — Gap Register

**Version:** 4
**Baseline:** `main`@`98fb59ca7d1ddf732523fcadfca15df6b4863b2f` (superseded by `8adaa5d` after GAP-11's fix merge, then `463a582` after GAP-12's, then `ba820af` after GAP-13's)
**Scope:** the SCIP provider's Python-language fidelity (`codex.provider.scip*`), plus its AST-provider counterpart (`codex.provider.ast_calls_adapter`) where they overlap. Indexer: `scip-python@0.6.6` (`docs/resources.md`).
**Method:** raw wire-format inventory against 5 real repositories (django, flask, click, pytest, requests), traced through decode → normalize → identity → graph → candidate → retrieval, per the acceptance-gate discipline in the Phase-2 directive.

This register is the single source of truth for Python-specific fidelity gaps. Every row must reach `FIXED + REGRESSION-LOCKED` or `ACCEPTED — outside contract` with explicit evidence before Python 100% may be declared. No row may remain `UNKNOWN`.

**Version 4 update:** GAP-13 is now FIXED / MERGED / REGRESSION-LOCKED (see row below), joining GAP-11 and GAP-12 — all three gaps this register originally opened are now closed. A new, separate finding (FND-1, below) was discovered incidentally while validating GAP-13 and is documented but **deliberately not fixed** in this cycle, per that cycle's own scope discipline. The Python 100% gate remains **NOT MET**: per the standing instruction under which GAP-13 was fixed, closing it does not by itself permit declaring Python 100% — the full exhaustive Python fidelity audit must still be re-run against this new merged baseline (`ba820af`) to verify none of GAP-11/12/13's fixes exposed or introduced new Python-specific information loss, and that re-audit has not yet been performed.

## Register

| GAP-ID | Layer | Evidence | Root Cause | Classification | Fix | Tests | Independent Validation | Status |
|---|---|---|---|---|---|---|---|---|
| GAP-11 | Planner (`planner.py` budget pruning) | **Measured retrieval recall = 0% on confirmed real data (pre-fix).** django `What references QuerySet?`: 38 real `REFERENCES` relationships exist in the graph among the query's own 80 resolved target entities (0 `IMPORTS`), retrieval returned 0. click `What references Command?`: 95 real `REFERENCES` relationships exist among targets, retrieval returned 0. Root-caused: `plan_query`'s truncation step kept only the alphabetically-first relationship type on overflow. Compounded by this audit's finding that `RelationshipType.IMPORTS` is **never populated** by scip-python (0 of 972,111 real occurrences ever carry the Import role bit — see ACC-2), and `IMPORTS` < `REFERENCES` alphabetically — so the kept type was deterministically always the empty one whenever truncation fired for `FIND_REFERENCES`. | Pre-existing, unrelated to any of GAP-5/7/8/9/10's diffs; confirmed untouched by all of them. | 4 — BUG (retrieval-completeness contract violation, not merely a theoretical edge case) | `_prioritize_relationship_types_by_evidence()` (`planner.py`): stable-partitions requested relationship types by "has any real edge in the traversal already computed" — types with evidence sort ahead of types without, each group keeping its original relative order. A first draft ranked by raw edge *count* instead of a has-evidence boolean; this broke `FIND_CALLERS` on real pytest data (`CALLS` 13 edges vs `REFERENCES` 82 edges — count-ranking substituted the wrong type) and was caught and corrected during this fix's own validation, before merge, with a dedicated regression test. Commit `a0d78a2`, merged `8adaa5d`. | 9 new tests in `test_planner_budget.py`: the real-world failure mode via `execute_query`, empty-does-not-suppress-populated, alphabetical tie-break preserved on all-zero evidence, the magnitude-vs-priority regression guard (unit + end-to-end), `FIND_IMPLEMENTATIONS`/`FIND_CALLERS` non-regression, negative-query classification unaffected, GAP-10 relationship-only-entity sanity check. Full suite 1096 passed, ruff/mypy clean. | django `QuerySet` 38/38 (100%), click `Command` 95/95 (100%) — repos used to design the fix. Independent repos not used to design the fix: pytest `Config` 170/170, pytest `Item` 83/83, flask `Response` 24/24, requests `Session` 125/125 — all 100% recall. pytest `What calls approx?` re-verified: kept CALLS (13/13), not REFERENCES. All reproduced a second time directly from the merged `main`@`8adaa5d`. | **FIXED / MERGED / REGRESSION-LOCKED** — CLOSED |
| GAP-12 | SCIP mapping (`infer_base_type`'s descriptor-suffix fallback) | Descriptor paths ending in `:` (scip-python's per-file "module identity" marker, e.g. `` `django.utils.crypto`/__init__: ``, exactly one per document — 3358 total across 5 repos) were never matched by any branch of `infer_base_type`'s fallback (`_SKIP_SUFFIXES`, `"()."`, `"#"`, `"."`), so it fell through to `return None`. Since `SymbolInformation.kind` is always `0` in real scip-python output (244,969/244,969 samples — see ACC-4), the kind-based path never rescued this. `_resolve_symbol` treated `None` from `infer_base_type` as "skip, no entity" unconditionally. Pre-fix real-data measurement: 31,411 non-Definition reference occurrences targeting these symbols were silently dropped (django 27,671; pytest 3,730; requests 10; flask/click 0). Each is a genuine "module X imports/references module Y" fact scip-python emitted that Codex discarded without a trace. | `infer_base_type`'s descriptor-suffix table was incomplete — it never enumerated `:` (`scip.proto`'s own `Descriptor.Suffix.Meta` kind), only `#`/`.`/`"()."`/`)`. Confirmed a real, documented SCIP descriptor kind, not malformed data — always exactly `__init__:`, never any other shape (exhaustively checked in django and pytest). | 4 — BUG (silent evidence loss, no upstream limitation — the data was fully present in the raw index) | One new branch, `trailing == ":" -> BaseEntityType.MODULE` (`codex/provider/scip/mapping.py`), reusing the pre-existing HLRD-specified `MODULE` type verbatim (already `_KIND_TO_BASE_TYPE`-mapped for kind 29) — no new entity type invented. Local/external gating (`locally_defined` / GAP-10's `_is_indexed_project_file`) lives entirely upstream of `infer_base_type` in `_resolve_symbol` and is untouched, so a genuinely external `:` symbol never reaches this branch. Commit `aed80d1`, merged `463a582`. | 9 new tests (`test_scip_mapping.py` ×2, `test_scip_adapter.py` ×7) covering all required invariants: decode/normalize/identity/canonical-ID determinism, distinct module identities never collapse, graph construction, candidate generation, byte-identical classification for every pre-existing descriptor shape, genuinely external module-identity symbols still resolve as `EXTERNAL_LIBRARY`. Full suite 1105 passed, ruff/mypy clean. | Perfect differential fidelity across all 5 repositories (raw colon-suffixed-symbol count == graph `MODULE`-entity count == distinct canonical_id count == distinct qualified_name count, every repo): django 2928=2928=2928=2928, flask 83=83=83=83, click 79=79=79=79, pytest 249=249=249=249, requests 19=19=19=19. click/requests used as the independent set (not used to design the fix); django/flask/pytest confirmed the same match. click's 0 relationships touching its 79 recovered entities verified as a genuine absence in its own raw index, not a residual defect. Reproduced a second time directly from merged `main`@`463a582`. | **FIXED / MERGED / REGRESSION-LOCKED** — CLOSED |
| GAP-13 | SCIP definition-location recovery (`scip_adapter.py`'s `_collect_definitions`), feeding the pre-existing, untouched identity key in `resolution/entity_resolver.py`'s `_symbol_location_identity_key` | AST-provided and SCIP-provided entities for the *same real method/function* fail to converge — two separate canonical IDs exist for one real symbol — whenever it is declared via the `@typing.overload` idiom (2+ stub signatures plus one real implementation, same name, same scope), or the structurally identical `@property`/`@x.setter` pair. Root cause fully isolated this cycle: scip-python emits exactly **one** Definition-role `Occurrence` per redefined symbol name — anchored to the *first* textual definition (the first `@overload` stub) — while every subsequent same-name redefinition, including the real implementation, carries only `ReadAccess`. `_collect_definitions()` took that single Definition-role occurrence's own range as the entity's `source_location`, so the SCIP identity anchored to the first stub's line; `AstCallsAdapter` independently anchors to the *last* textual definition (its own pre-existing, unconditional "last definition wins" convention, `_DefinitionCollector`'s dict-overwrite semantics). Since the identity key requires an exact `start_line` match, these never converged on real data. Reproduced and fixed against 3 repositories: flask (`stream_with_context`, `locate_app`), click (`command`, `group` — both in `src/click/decorators.py`), pytest (`raises`, `src/_pytest/raises.py`) — all now converge to one canonical entity at the true implementation line. | The SCIP adapter's definition-location recovery had no special case for a symbol with more than one real textual definition in the same file — it always trusted the lone Definition-role occurrence's own range, which scip-python only ever places on the *first* member of such a family, never the semantically-correct last one `AstCallsAdapter` already used. | 4 — BUG (identity-completeness contract violation: "identical symbols resolve to the same canonical identity" invariant, Phase 4) | `_redefinition_family_locations()` (`scip_adapter.py`): per document, for any symbol with more than one real `SymbolInformation` entry (the reliable "multiple textual definitions in this file" signal — confirmed never to fire for an ordinarily single-defined symbol), walks its Definition/ReadAccess occurrences forward from the earliest, folding in each subsequent occurrence within `_OVERLOAD_FAMILY_LINE_WINDOW` (25) lines of the previous one, and uses that last-in-family range in place of the lone Definition occurrence's own range. Mirrors `AstCallsAdapter`'s own convention rather than loosening the identity key itself; `entity_resolver.py` is completely untouched. Never applied to Parameter descriptors (trailing `)`), which never become entities regardless. Genuinely distinct same-named symbols are unaffected — the signal is scoped per-document and per-symbol-string. Commit `ab0bda5`, merged `ba820af`. | 11 new tests in `test_scip_adapter.py`: last-occurrence recovery, deterministic canonical-ID, AST/SCIP convergence via `resolve_entities()` directly, `IMPLEMENTS`-evidence continuity, distinct symbols in different modules/classes remain distinct (2 tests), byte-equivalence for non-overloaded functions, a far-away same-name reference never joins the family, external-symbol non-fabrication, GAP-9/GAP-12 non-regression. Full suite 1116 passed, ruff/mypy clean; all regression locks (GAP-5/7/8/9/10/11/12) re-verified. | flask (design repo): `stream_with_context` and `locate_app` both converge to 1 canonical entity each at the true implementation line. Independent repos not used to design the fix: click `command` (5-member family, `src/click/decorators.py`) converges to 1 entity at line 167 (0-indexed; source line 168, the real implementation); click `group` converges to 1 entity at line 292 (source line 293) **while a genuinely distinct, unrelated `group()` function in `examples/completion/completion.py` correctly remains a separate, unmerged canonical entity** (2 total `group` entities, as it must be); pytest `raises` (4-overload family, `src/_pytest/raises.py`) converges to 1 entity at line 103 (source line 104, the real implementation), with 52 real incoming relationships now correctly attributed to the one true identity instead of split/misattributed to a stub location no caller ever calls. Pre-fix vs post-fix measured directly on the merged baseline: click `command`/`group` and pytest `raises` each went from 2-3 distinct canonical IDs per family to exactly 1, with zero change to the distinctness of unrelated same-named symbols. All reproduced a second time directly from the merged `main`@`ba820af`. | **FIXED / MERGED / REGRESSION-LOCKED** — CLOSED |

## Newly discovered, not yet fixed (flagged during GAP-13's cycle, out of that cycle's scope)

| ID | Finding | Evidence | Classification | Why not fixed this cycle |
|---|---|---|---|---|
| FND-1 | SCIP descriptor-string collision between two unrelated nested closures that happen to share both a name and an enclosing method: django's `AbstractBaseUser.check_password`'s inner `setter` closure and `AbstractBaseUser.acheck_password`'s own, separate inner `setter` closure both serialize to the identical descriptor `` AbstractBaseUser#setter() `` — scip-python's descriptor grammar has no room to distinguish two same-named local closures nested inside two different sibling methods of the same class. Confirmed present and **unaffected** by the GAP-13 fix: `canonical_id` and `qualified_name` for the resulting single merged entity are byte-identical before and after GAP-13's change (only its recovered `source_location` shifts, exactly as GAP-13's own fix intends for a genuine same-symbol redefinition family — this pre-existing collision is a different mechanism, a genuine descriptor-grammar ambiguity between two *actually-different* symbols, not a redefinition-family split of one real symbol). | Direct raw-index inspection of django's `AbstractBaseUser.check_password`/`.acheck_password` (`django/contrib/auth/base_user.py`), confirmed both inner `setter` closures resolve to the one descriptor `` AbstractBaseUser#setter() ``. | 4 — BUG (identity-completeness contract violation: two real, distinct symbols incorrectly share one canonical identity) — but scoped to nested nested-closure naming collisions specifically, not the `@overload`/redefinition-family pattern GAP-13 addressed. | Outside GAP-13's explicit scope ("do not re-fix GAP-11/12, do not redesign the entire entity resolver... if another defect is discovered, classify it in the fidelity gap register and STOP rather than fixing it in this cycle"). Needs its own dedicated fix cycle: likely requires disambiguating nested-closure descriptors by enclosing-method identity (not just enclosing-class), a change to SCIP-side descriptor-path construction/consumption distinct from GAP-13's redefinition-family recovery. |

## Accepted / out-of-contract findings (not gaps — documented with evidence, no fix needed)

| ID | Finding | Evidence | Classification | Acceptance rationale |
|---|---|---|---|---|
| ACC-1 | `Index.external_symbols` (wire field 3) is decoded (`ScipIndex.external_symbols`) but never consumed by `SCIPAdapter`. | 208–1444 entries per repo (3359 total). 100% have `kind=0` and zero relationships in all 5 repos — carries no metadata beyond the bare symbol string. 20–127 per repo are unreachable via any Occurrence/Relationship elsewhere (bare whole-module `import os`-style targets scip-python never gives an Occurrence). | 3 — Intentionally unsupported / correctly filtered by contract | `external_symbols` is an `Index`-level field with **no per-Document attribution** in the wire format itself — structurally incapable of producing a correctly-attributed `subject=file` REFERENCES/IMPORTS relationship without violating the no-attribution-contamination requirement. Any package-level signal it might imply is already more authoritatively covered by `PyprojectDependencyAdapter` (declared deps from `pyproject.toml`/requirements). Matches the adapter's own pre-existing documented rationale for leaving `DEPENDENCY`/`DATA_FLOW` unimplemented for SCIP ("SCIP's Import role is a symbol-level fact, not the package-level claim DEPENDS_ON implies"). |
| ACC-2 | `Occurrence.symbol_roles` never carries the Import bit (`0x2`) in real scip-python output. | 0 of 972,111 real occurrences across 5 repos. Only bits `1` (Definition) and `8` (ReadAccess) ever observed — never WriteAccess(`4`), Generated(`16`), Test(`32`), ForwardDefinition(`64`). | 2 — Not emitted upstream | Codex's `_IMPORT_ROLE = 0x2` check is implemented correctly per the real `scip.proto` spec; scip-python 0.6.6 simply never sets this bit. Verified this is the root *amplifier* of GAP-11 (see GAP-11 row) rather than a separate defect — Codex faithfully reports what the indexer emits. |
| ACC-3 | `Relationship.is_reference` / `is_type_definition` / `is_definition` flags are never `True` in real scip-python output — only `is_implementation` ever fires (12,992/12,992 real relationships, 5 repos). | See §C.2. | 2 — Not emitted upstream | The `TYPE_RELATIONSHIP` capability's `is_type_definition`-sourced contribution to `REFERENCES` is real code, correctly implemented, but currently unexercised by this indexer version — not a Codex defect. `REFERENCES` overall is *not* dead (it's also populated from plain non-Definition Occurrences), only this one sourcing path is. |
| ACC-4 | `SymbolInformation.kind` is always `0` (`UnspecifiedKind`) in real scip-python output. | 244,969/244,969 samples, 5 repos, zero exceptions. | 2 — Not emitted upstream | `infer_base_type`'s kind-based table (`_KIND_TO_BASE_TYPE`) is correctly implemented and unit-tested, but is dead code against this indexer version in practice — 100% of real classification happens via the descriptor-suffix fallback. Recorded here because it reframes the fallback from "rare edge case" (as GAP-9/10's own comments characterized it) to "the only path that matters" — relevant context for any future change to that fallback, including GAP-12's fix. |

## Summary counts (raw inventory, 5 repositories: django, flask, click, pytest, requests)

| Metric | Total |
|---|---|
| Documents | 3358 |
| Occurrences | 972,111 |
| SymbolInformation entries | 244,969 |
| Relationships | 12,992 (100% `is_implementation`) |
| External symbols (Index-level) | 3359 |
| Local (function-scoped) symbols | 100,156 |
| Distinct `SymbolInformation.kind` values observed | 1 (`{0}`) |
| Distinct `Occurrence.symbol_roles` values observed | 2 (`{1, 8}`) |
| Distinct descriptor-path suffix shapes observed | 4 (`` ` # . ) : ` ``) |

Full raw counters: `/tmp/claude-0/-home-user/3b795297-2a2e-5a02-ae81-b4114757a603/scratchpad/python100/raw_inventory.json` (per-repo breakdown).

## End-to-end fidelity matrix

| Category | Raw emitted | Decoded | Normalized | Graph | Candidate | Retrieved | Loss / Classification |
|---|---|---|---|---|---|---|---|
| Definition occurrences (`#`/`.`/`"()."` suffix shapes) | Yes | Yes | Yes | Yes | Yes | Yes | None found |
| Definition occurrences (`:` suffix — module identity) | Yes | Yes | **No — `infer_base_type` returns `None`** | **No entity** | **No** | **No** | **GAP-12 — BUG, 31,411 real reference occurrences lost across 5 repos** |
| Non-Definition reference occurrences (ReadAccess, bit `8`) | Yes | Yes | Yes | Yes | Yes | Yes (when not over budget) | None at graph layer; retrieval-layer loss is GAP-11 |
| Import-role occurrences (bit `2`) | **Never emitted by scip-python** | n/a | n/a | n/a | n/a | n/a | ACC-2 — not emitted upstream |
| `is_implementation` relationships | Yes | Yes | Yes | Yes | Yes | Yes (when not over budget) | None found — 100% endpoint resolution measured |
| `is_type_definition` / `is_reference` / `is_definition` relationships | **Never emitted by scip-python** | n/a | n/a | n/a | n/a | n/a | ACC-3 — not emitted upstream |
| Relationship-only symbols (no Definition, no SymbolInformation) | Yes | Yes | Yes (GAP-10 fix) | Yes | Yes (measured: 3/175 for flask `Index`) | Yes | Fixed, merged, regression-locked (GAP-10) |
| `SymbolInformation.kind` | **Always `0`** | Yes | Falls back to descriptor-suffix inference | — | — | — | ACC-4 — not emitted upstream; descriptor-suffix fallback is the *only* live path |
| `Index.external_symbols` | Yes (208–1444/repo) | Yes | Not consumed | — | — | — | ACC-1 — intentionally unsupported, no per-file attribution possible |
| Cross-provider identity (AST FUNCTION/METHOD vs SCIP FUNCTION/METHOD, same real symbol) | Yes (both sides) | Yes | Yes — converges via recovered last-in-family location (GAP-13 fix) | Yes — one entity | One candidate | Evidence unified on one node | Fixed, merged, regression-locked (GAP-13) — confirmed in 3 repos (flask, click, pytest); one separate, out-of-scope descriptor-collision finding remains (FND-1) |
| FIND_REFERENCES / multi-relationship-type retrieval under budget truncation | Yes (real, in graph) | Yes | Yes | Yes | Yes | **No — pruning keeps the empty type** | **GAP-11 — BUG, measured 0% recall (django `QuerySet`: 38→0; click `Command`: 95→0)** |
| Local (function-scoped) symbols | Yes (100,156 total) | Yes | Correctly filtered (`is_local_symbol`) | No entity (by design) | n/a | n/a | Category 1 — correctly filtered by contract |
| Parameter descriptors (`)` suffix) | Yes (65,259 total) | Yes | Correctly filtered (`_SKIP_SUFFIXES`) | No entity (by design) | n/a | n/a | Category 1 — correctly filtered by contract |

## Completeness counters (measured, 5 repositories)

| Repo | Raw occ. | Raw def. occ. | Raw distinct symbols | Raw relationships | Graph entities | Graph relationships | Endpoints resolved | Identity collisions | Colon-suffix entities (expect 0) | AST/SCIP converged | AST-only entities |
|---|---|---|---|---|---|---|---|---|---|---|---|
| django | 772,518 | 115,005 | 116,216 | 12,295 | 65,504 | 69,988 | 100.00% | 0 | 0 | 10,313 | 13 |
| flask | 20,313 | 3,991 | 4,393 | 105 | 2,055 | 1,471 | 100.00% | 0 | 0 | 194 | 8 |
| click | 33,627 | 5,493 | 5,904 | 174 | 2,520 | 2,352 | 100.00% | 0 | 0 | 358 | 13 |
| pytest | 137,429 | 19,407 | 20,395 | 354 | 8,401 | 9,837 | 100.00% | 0 | 0 | 2,013 | 30 |
| requests | 8,224 | 1,071 | 1,368 | 64 | 943 | 635 | 100.00% | 0 | 0 | 153 | 112 |

Raw JSON: `identity_fidelity.json`, `completeness_counters.json` in the scratchpad `python100/` directory (see report for full path). `AST-only entities` is a **mix** of GAP-13 (`@overload` split, confirmed present) and legitimate scip-python indexing gaps AST independently fills (not a bug) — not yet separated per-repo; `requests`' notably higher ratio (112/153) needs that separation before scoping GAP-13's fix.

## Scope and depth disclosure (honest accounting, not overclaiming)

- **Fully measured with real data**: upstream symbol/occurrence/relationship-flag inventory (Phase 2), identity collision counts (Phase 4), relationship endpoint resolution (Phase 5), GAP-11's retrieval recall (Phase 5/7), candidate generation for GAP-10-recovered entities (Phase 6), AST/SCIP convergence rates (Phase 8).
- **Partially measured**: candidate-generation completeness for large pools (cites pre-existing GAP-8 measurements, not independently re-measured this pass); external/unresolved semantics (cites GAP-9/10's own 15-test regression suite plus this audit's own false-positive re-verification, not new dedicated tests written this pass).
- **Not done this pass**: dedicated mutation testing (Phase 11's "deliberately alter descriptor decorations/remove SymbolInformation/etc." battery), a repository outside the existing 5 (django/flask/click/pytest/requests — all already used during prior gap-fix cycles, so the "at least one independent repository" requirement for *future fixes* is not yet satisfied and must be honored when GAP-11/12/13 are fixed), per-repo separation of GAP-13's two contributing causes.
- **Status as of Version 4**: GAP-11, GAP-12, and GAP-13 — every gap this register originally opened — are now FIXED / MERGED / REGRESSION-LOCKED. One new, separate, deliberately-unfixed finding (FND-1, a descriptor-string collision between unrelated nested closures, unaffected by and unrelated to GAP-13's own fix) is documented above. **This does not itself mean Python 100% may be declared.** Per the explicit instruction under which GAP-13 was closed, the full exhaustive Python fidelity audit that produced this register must be re-run in its entirety against the new merged baseline (`main`@`ba820af`) to verify that none of the three fixes exposed or introduced additional Python-specific information loss — that re-audit has not yet been performed. Only after it reaches zero UNKNOWN and zero OPEN Python-specific fidelity gaps, with complete raw→graph→candidate→retrieval fidelity confirmed again on the new baseline, may Python 100% be declared.
